/target

# 模块根下的编译产物（R-3 / 0.67.0 起）：目录自带一行 `*` 的 .gitignore，
# 这里再加一条是**双保险**——`courses/set-theory`、`course/` 本身就是模块根。
.sokonanoda/
.DS_Store

# editor/vscode（npm / vsce / 扩展测试产物）
node_modules/
*.vsix
.vscode-test/
learner/

# 打包时 stage 的 LSP 二进制（scripts/stage-lsp.js 生成，不进仓库）
editor/vscode/bin/

# 本地 agent 会话记忆（工作台私有，不入库）
.workbuddy/

# python 工具（courses/set-theory/tools/check.py、scripts/gap.py、gen-site-data.py）
__pycache__/
*.pyc

.cache
# 第三方 agent 技能（scripts/fetch-agent-skills.py 生成，配方在 dsh/agent-skills.json）
.dsh/skills/

# 原子写失败留下的临时目录（`.<name>.<pid>.<uuid>.tmpdir/`）——**别提交**：
# 里面的 `*.tmp` 常常是**真文件的硬链接** ⇒ 编辑真文件会让这些垃圾跟着变，
# 于是"改一个文档"变成"改一个文档 + 一堆 .tmp"（历史上有 5 个这么进过库，
# 2026-09-24 清理并加这条兜住）。
.*.tmpdir/
.*.tmp
# 项目产物原子写的残留 `*.json.tmp-<pid>`（2026-09-26 实测：受限沙箱**拒绝仓库内
# rename** ⇒ 产物落不了盘、每次编译留一个 28–89 KB 的 tmp ✗，仓库里清出 26 个
# / 1.2 MB）。`.sokonanoda/` 那条已经兜住它们所在的目录，这条是**与位置无关**的兜底。
*.tmp-*
# 编辑器 / 合并残留（2026-09-26 文档瘦身：与上面的 .tmpdir 同一类"防复发"；
# 仓库里当前 **0 个**被跟踪的这类文件 —— 加了这条，将来也不会被提交）。
*.orig
*.rej
*~
dsh-ci-time-*.md
