{"id": "G-14", "title": "宇宙层级 binder 只吃一个花括号组：{u v}（空格）与 {u} {v}（两组）都解析失败", "kind": "language", "severity": "nice", "status": "fixed", "found": "2026-09-18", "found_by": "course-agent（写课程标准库时）", "where": {"area": "crates/front/src/parser.rs 的声明 binder 解析（`{u}` 分支只吃一个名字）", "note": "与 H6-C 修过的「inductive 不吃多名字 binder 组」同族；`(a b : α)` 已支持，`{u v}` 没有"}, "repro": "docs/gaps/repro/G14-single-universe-binder.sokonanoda", "today": "实测（0.58.0，2026-09-18 复核）：def idc {u, v} (α : Sort u) (β : Sort v) … **可以**（checked）；{u v}（空格分隔）与 {u} {v}（两组）都报 parse unexpected-token。即限制是「一个组、逗号分隔」，不是「只能一个宇宙」——台账早先的 today 不准确，已更正。", "expected_lean": "官方 Lean 4：{u v}、{u, v}、{u} {v} 三种写法等价（都是 binder 组）。我们只支持中间一种 ⇒ 跨宇宙引理写成 {u, v} 即可。", "workaround": "写 {u, v}（逗号分隔）即可跨宇宙；lib/Logic.sokonanoda 的 congrArg 因此可以升级成跨宇宙版（待做）。", "blocks": ["课程标准的跨宇宙引理（congrArg、复合、像/原像的跨类型版本）"], "wo": "docs/gaps/WO-009-single-universe-binder.md", "fixed_in": "0.59.0", "notes": "课程标准库 lib/Logic.sokonanoda 的 congrArg 因此只能同层；写库时第一时间撞到。｜勘误（2026-09-18）：WO-009 作者实测发现 {u, v} 可过，本条原 today/severity 过重，已降为 nice。｜**已修（0.59.0）**：{u v}（空格）与 {u} {v}（两组）都接受，与 {u, v} 等价；复现件已翻成\"已修后形状\"（.sh 复现 exit 1）。"}
{"id": "G-15", "title": "query check 的 failed[] / warnings[] 只给裸字节 offset——没有行列、没有单位", "kind": "tooling", "severity": "painful", "status": "fixed", "found": "2026-09-18", "found_by": "course-agent（bisect 找错时连修错两次）", "where": {"area": "kernel 阶段错误 → 命令 span 的归因（front 的 error 装配；与 0.58.0 修过的 `warning_cmds` 同族）", "note": "warning 那边已经用 `CompileOutput.warning_cmds` 平行数组修过；error 这边疑似还有同类偏差"}, "repro": "docs/gaps/repro/G15-query-check-bare-offsets.sh", "today": "已修（0.59.0 / WO-010）：`query check` 的 `failed[]` / `warnings[]` 每条自带 1 基 `start_line`/`start_col`/`end_line`/`end_col`（与 `grade --json` 的 `diagnostic.span` 同源同值）；`start`/`end` 仍是**字节** offset，坐标空间 = **入口文件**——依赖模块的病只以入口 `import` 行的 `import-dependency-failed` 进 `failed[]`（要看依赖走 `query project` 或 `grade --json` 的 `file`/`module`）。原记的「span 漂到别的声明」是**字节 offset 当字符下标**切字符串造成的假象：事件 span 一直是精确的。", "expected_lean": "span == 出错命令的源码范围（起点 = 首 token、末端 = 值表达式末 token），且对外通道（`query check` 的 `failed[]`/`warnings[]`、事件、MCP 工具）**自带单位与坐标空间**，消费者不需要猜/换算。这不是 Lean 语义，是查询通道的契约。", "workaround": "需要行列时改用 grade --json（事件流的 span 带行列）；query check 只当计数用。", "blocks": ["agent 依据 query check 自动定位并修复（现在只能拿到裸 offset）", "课程门禁失败输出的可读性"], "wo": "docs/gaps/WO-010-kernel-error-span.md", "fixed_in": "0.59.0", "notes": "教训：`offset` 一律按**字节**解释，别拿 Python 的 `src[:off]` 字符切片去量（`docs/LESSONS.md` 有一条同型条目）。守护：front `reports_kernel_rejection_with_span`（收紧为字节切片逐字等于坏命令）与 `kernel_rejection_span_is_the_failing_command_range`（三条声明、坏的夹中间 ⇒ 不溢出）；CLI `crates/cli/tests/query.rs::query_check_failure_positions_are_typed_and_match_grade`（`failed[]` 行列 ≡ `grade` 的 span，`warnings[]` 同理；依赖坐标空间 = 入口 1:1）。复现 `docs/gaps/repro/G15-query-check-bare-offsets.sh` 已重写（修前 exit 0 / 修后 exit 1，双二进制对照）。边界（有意不做）：只加字段不改 `start`/`end` 语义与 schema 号、不动 `split_report` 与三个平行数组、不动内核；UTF-16 列与 LSP `pos_at` 的字节列是**另外两条**缺口（先有 repro 再开 WO）。"}
{"id": "L-01", "title": "prelude 缺 Lean core 的逻辑骨架（True/False/And.elim/Or.elim/Not/absurd/Iff）", "kind": "library", "severity": "blocker", "status": "fixed", "found": "2026-09-18", "found_by": "course-agent（写卷 I 标准库时）", "where": {"area": "prelude（crates/front/src/compile/prelude.rs 目前只装 Nat/Bool/Eq）", "note": "入门课把这批东西当教学内容让学习者公理/定义，卷 I 不该再教一遍"}, "repro": "docs/gaps/repro/L01-l1-prelude-logic-skeleton.sh", "today": "13 条全部要在课程库里手写一遍（见 courses/set-theory/lib/Logic.sokonanoda 的 L-01…L-13 标记）：True、True.intro、False、False.rec、False.elim、And.elim、Or.elim、Not、Not.intro、Not.elim、absurd、Iff、Iff.intro、Iff.mp、Iff.mpr", "lean_names": ["True", "True.intro", "False", "False.elim", "And.elim", "Or.elim", "Not", "Not.intro", "Not.elim", "absurd", "Iff", "Iff.intro", "Iff.mp", "Iff.mpr"], "owner": "prelude", "expected_lean": "Lean 4 core 里这些全部现成（`Init.Core` / `Init.Logic`），任何教程直接使用；`And`/`Or` 是 inductive、`Not` 是 `A → False` 的 def", "workaround": "课程标准库 lib/Logic.sokonanoda 自带一份（26 条声明，已判卷全绿）", "blocks": ["卷 I 全部单元（每个单元都 import 它）"], "wo": "docs/design/prelude-l1-proposal.md", "wo_planned": null, "fixed_in": "0.59.0", "notes": "与 G-02 相关：And 走 axiom 族、Or 走 inductive（构造子裸名），进 prelude 时要一并定形。｜课程侧已由 courses/set-theory/lib/Logic.sokonanoda 兜住（26 条，判卷全绿）；正式修法见 docs/design/prelude-l1-proposal.md（提案，未实现）。｜P1/P2/P3 已落地（docs/design/prelude-l1-proposal.md）：PRELUDE_L1_SRC + 族让位 + 30 个 PRELUDE_NAMES；P4（课程仓 74 处 inl/inr 改点号名）未做，课程库 28 条保留为自足兜底。｜**已修（0.59.0）**：L1 prelude 落地：True/False/And/Or/Not/absurd/Iff 等族（30 个名字）随 Full prelude 自带；两处 GOLDEN 据实重算（unit2 checked 2→3）。｜P4 课程仓跟随（2026-09-19）：courses/set-theory/lib/Logic.sokonanoda 退化成只有注释的空壳（0 条声明）、34 个 import lib.Logic 未改；课程侧 65 处项位裸名 inl/inr 改点号名 Or.inl/Or.inr；课程门禁 329 checked · 99 open · 0 判负。"}
{"id": "L-02", "title": "prelude 缺 Eq 的核心引理（Eq.symm / Eq.trans / congrArg）", "kind": "library", "severity": "blocker", "status": "fixed", "found": "2026-09-18", "found_by": "course-agent", "where": {"area": "prelude 的 Eq 三件套（Eq/Eq.refl/Eq.subst）", "note": "Lean core 里 symm/trans/congrArg 是核心引理"}, "repro": "docs/gaps/repro/L02-eq-core-lemmas.sh", "today": "课程库手写 3 条；写集合论证明时随时要用（`Eq.symst` 的方向写反是最高频错误）", "lean_names": ["Eq.symm", "Eq.trans", "congrArg"], "owner": "prelude", "expected_lean": "Lean 4 core 现成；`Eq.symm h` / `Eq.trans h1 h2` / `congrArg f h` 是所有证明的日常词汇", "workaround": "spike 的 lib/Logic.sokonanoda 里手写（congrArg 受 G-14 限制只能同宇宙）", "blocks": ["卷 I 全部单元", "入门课单元②其实已经教过（学习者手写）——卷 I 不该重复"], "wo": "docs/design/prelude-l1-proposal.md", "wo_planned": null, "fixed_in": "0.59.0", "notes": "入门课把 symm/trans/cong 当**教学内容**（单元②），所以语言仓一直没把它们当缺口；对卷 I 它们是**基础设施**。｜课程侧已由 courses/set-theory/lib/Logic.sokonanoda 兜住（26 条，判卷全绿）；正式修法见 docs/design/prelude-l1-proposal.md（提案，未实现）。｜Eq.symm/Eq.trans/congrArg 进 prelude（B7 族，随 Eq prelude 让位）；课程库三条保留为兜底。｜**已修（0.59.0）**：Eq.symm / Eq.trans / congrArg 进 prelude（B7 族）。｜P4 课程仓跟随（2026-09-19）：courses/set-theory/lib/Logic.sokonanoda 退化成只有注释的空壳（0 条声明）、34 个 import lib.Logic 未改；课程侧 65 处项位裸名 inl/inr 改点号名 Or.inl/Or.inr；课程门禁 329 checked · 99 open · 0 判负。"}
{"id": "L-03", "title": "prelude 的 Eq.subst 只支持 Prop motive ⇒ Type 层重写不可表达（Eq.mp/Eq.mpr/Eq.rec/cast）", "kind": "library", "severity": "painful", "status": "fixed", "found": "2026-09-18", "found_by": "course-agent", "where": {"area": "prelude 的 `axiom Eq {u}`：Eq 是**公理**（内核不为它派生消去子），加上两条语法边界（`inductive` 头部不吃宇宙 binder；层级语法没有 `u+1`）", "note": "内核的 large-elimination 规则本身**给** Eq 形状（`EqT.rec.{1}` 的 motive 落 `Type 0`，实测）——所以修法是把消去子作为 prelude 公理装上，不是动内核"}, "repro": "docs/gaps/repro/L03-eq-type-level.sokonanoda", "today": "实测：`def Eq.mp {u} (α : Sort u) (a b : α) (h : Eq.{u} α a b) : a -> b := fun ha => Eq.subst.{u} α (fun x => x) a b h ha` → `kernel-expected-sort`（motive 落在 Type）；而 α = Prop 时同一写法可过", "lean_names": ["Eq.mp", "Eq.mpr", "Eq.rec", "cast"], "owner": "prelude", "expected_lean": "Lean core 里 `Eq.rec` 的 motive 可以是任意 `α → Sort v`，于是能在类型层搬运（`Eq.mp`、`cast`）", "workaround": "**0.60.0 起 prelude 自带**（B8：`Eq.rec`/`Eq.mp`/`Eq.mpr`，Full 模式直接可用）；`cast` 与 `Eq.mp` 同义故不另立名字，`Eq.mp`/`Eq.mpr` 是 Type 0 实例，通用的 `Eq.rec` 任意层级可用", "blocks": ["卷 II+（结构/商/等价类型）", "任何要在类型层重写的证明"], "wo": null, "wo_planned": null, "fixed_in": "0.60.0", "notes": "若要改，属于 prelude 的签名变更（不是内核语义）——但会牵动既有 golden，需设计先行。｜**L-01/L-02 落地后（0.59.0）仍 open**：B7 只装了同宇宙的 `Eq.symm`/`Eq.trans`/`congrArg`，`Eq.subst` 的 motive 仍是 `α -> Prop`，所以 `Eq.mp`/`Eq.mpr`/`Eq.rec`/`cast` 照旧写不出来（设计 §5 的「不做」清单）。｜**已修（0.60.0）**：L1 新增 **B8 族** = `axiom Eq.rec {u, v}`（签名与 Lean core 的 `Eq.rec.{u, v}` 逐字同形）+ 由它定义的 `Eq.mp`/`Eq.mpr`（Type 0 实例）；三件套 = `PRELUDE_L1_SRC` + `L1_FAMILIES`（B8，`deps=[\"EQ\"]`）+ `PRELUDE_NAMES` 42 → 45 + front 单测 2 条（`eq_rec_transports_at_type_level` / `eq_rec_family_yields_when_the_file_declares_it`）+ 复现件 `docs/gaps/repro/L03-eq-type-level.sokonanoda`（`scripts/soko grade` **exit 0 · 5 checked · 0 diagnostic**）。｜**实测根因**（设计 `docs/design/eq-type-level-rewriting.md`）：内核的 large-elimination 规则**给** Eq 形状——`inductive EqT (α : Type) (a : α) : α -> Prop` + `ctor EqT.refl` 派生的 `EqT.rec.{1}` 的 motive 落 `Type 0`（对照：`Exists.rec` 0 个宇宙参数、`#check Exists.rec.{1}` → `elab-universe-arity`）；堵路的是 ① prelude 的 `Eq` 是**公理**、内核不为它派生消去子（`Eq.rec` → `elab-unknown-constant`）；② `inductive` 头部不吃宇宙 binder（立不出宇宙多态的 Eq 归纳块）；③ 层级语法没有 `u+1`（`Eq.{u+1}` → parse `found Plus`）。｜**残留边界**（不硬凑）：`Eq.mp`/`Eq.mpr` 是 Type 0 实例（宇宙多态版要层级算术，另立设计）；`cast` 与 `Eq.mp` 同义、不另立名；`Eq.ndrec` 不装；不把 `Eq` 改成归纳块（会丢宇宙多态）。**内核零改动**（硬规则 1）。｜**0.61.0 扩族（层级算术 `u+1` 落地，2026-09-19 同轮）**：`Eq.mp`/`Eq.mpr` 从 Type 0 实例改成**宇宙多态**（`{u} (α β : Sort u) (h : @Eq.{u+1} (Sort u) α β)`，签名与 Lean core 的 `Eq.mp`/`Eq.mpr`/`cast` 逐字对齐），并装上 `cast`（Lean core 里 `cast h a` 就是 `Eq.mp h a`）与 `Eq.ndrec`（非依赖消去子）⇒ B8 = 5 条、`PRELUDE_NAMES` 45 → 47；复现件扩到 **`scripts/soko grade` exit 0 · 10 checked · 0 diagnostic**（多态 Eq.mp/Eq.mpr/cast + Eq.ndrec）；本行 `workaround` 字段的「cast 不另立名字」「Eq.mp/Eq.mpr 是 Type 0 实例」两条已被本轮取代（该字段未改，以本条 notes 为准）。"}
{"id": "L-04", "title": "课程标准库缺 Set 的「定义展开」引理（subset_def / mem_union / mem_inter / mem_diff / mem_power_iff / not_mem_empty / mem_singleton_iff）", "kind": "library", "severity": "blocker", "status": "workaround", "found": "2026-09-18", "found_by": "course-agent（写 lib/Set.sokonanoda）", "where": {"area": "课程标准库（将来的课程仓 lib/Set.sokonanoda）", "note": "Mathlib 里这批都是 `rfl` 级或一行；没有它们，每道题都要手写展开"}, "repro": null, "today": "8 条在 spike 里手写：Set.subset_def、Set.mem_union、Set.mem_inter_iff、Set.mem_diff、Set.mem_power_iff、Set.not_mem_empty、Set.mem_singleton_iff、Set.mem_singleton_self（外加 Set.ext 这个**公理**）", "lean_names": ["Set.ext", "Set.subset_def", "Set.mem_union", "Set.mem_inter_iff", "Set.mem_sdiff", "Set.mem_powerset_iff", "Set.notMem_empty", "Set.mem_singleton_iff", "Set.mem_empty_iff_false", "Set.mem_ofPred_eq"], "owner": "course-lib", "expected_lean": "Mathlib `Mathlib/Data/Set/*` 全都有；`Set.ext` 由 `propext` + `funext` 推出（我们不能，所以作公理）", "workaround": "spike 的 lib/Set.sokonanoda 已备好（40 条，判卷全绿）", "blocks": ["卷 I 单元 1–4"], "wo": null, "wo_planned": null, "fixed_in": null, "notes": "Set.ext 作为公理是**语言级决策**（要不要给 propext/funext）——比 L-01/L-02 更值得单独立项。｜**名字必须照 Loogle 取证版**（2026-09-18 复核）：我们的试做稿用了 `Set.not_mem_empty`（真名 `Set.notMem_empty`，大写 M）、`Set.mem_power_iff`（真名 `Set.mem_powerset_iff`）、`Set.mem_diff`（真名 `Set.mem_sdiff`）、`Set.subset_refl/trans/antisymm`（真名 `Set.Subset.refl/trans/antisymm`）；`Set.eq_empty_iff_forall_not_mem` 真名 `Set.eq_empty_iff_forall_notMem`。课程仓的 lib/ 一律用真名，试做稿的名字作为别名保留在对照表里（docs/design/course-stdlib.md §3.1）。｜课程侧已由 courses/set-theory/lib/Set.sokonanoda 的 11 条展开引理兜住；语言/标准库层仍未提供。"}
{"id": "L-05", "title": "「内容型」集合引理被误放进库（16 条）——它们应当是**练习**而不是基础设施", "kind": "library", "severity": "painful", "status": "fixed", "found": "2026-09-18", "found_by": "course-agent（第一遍写库时把教科书要证的定理也写进了 lib）", "where": {"area": "分层判据：docs/design/course-stdlib.md 的 L1/L2/L3", "note": "L2 = 定义展开（库），L3 = 有数学内容的定理（练习）。第一遍把 L3 混进了 L2"}, "repro": null, "today": "spike 的 lib/Set.sokonanoda 里有 16 条属于 L3：subset_refl/trans/antisymm、subset_antisymm_iff、empty_subset、subset_empty_iff、singleton_subset_iff、subset_union_left/right、union_subset_iff、inter_subset_left/right、subset_inter、subset_inter_iff、diff_subset、power_mono、union_comm、inter_comm、inter_union_distrib_left", "lean_names": ["Set.Subset.refl", "Set.Subset.trans", "Set.Subset.antisymm", "Set.empty_subset", "Set.subset_union_left", "Set.inter_subset_left", "Set.union_subset_iff", "Set.inter_comm", "Set.inter_union_distrib_left"], "owner": "exercise", "expected_lean": "Mathlib 当然有它们（所以 Lean 教程不练）；但**我们的课程**的练习正是「把标准事实证一遍」——判据是：它有没有数学内容，而不是 Mathlib 有没有", "workaround": "课程已建在语言仓内 courses/set-theory/：**第一刀已落**——subset_refl/trans/antisymm、empty_subset、subset_empty_iff、eq_empty_iff_forall_notMem 六条已移出 lib、进单元②（judged 10 checked / 0 failed）；其余 10 条（并/交/分配/幂集单调/交换）属单元③④，写那两个单元时一起移。", "blocks": ["课程练习的设计（否则学习者在做题时发现答案已在库里）"], "wo": null, "wo_planned": null, "fixed_in": "0.59.0", "notes": "这条是**方法学结论**：Mathlib 有 ≠ 我们不该练；Mathlib 有 **且没有数学内容**（纯展开）才归库。｜**已修（0.59.0）**：第二刀核实——lib/Set 已无内容型引理（只剩 12 定义 + 1 公理 + 10 展开引理）；唯一真缺口 subset_inter_iff 补为单元③ 练习 7；残留记在文件头（subset_antisymm_iff、Image 模块的第三刀）。"}
{"id": "G-13", "title": "axiom 不吃 binder 参数表（def/theorem 吃），照 Lean 4 习惯写会被拒", "kind": "language", "severity": "painful", "status": "fixed", "found": "2026-09-18", "found_by": "settheory-survey-agent", "where": {"area": "crates/front/src/parser.rs parse_axiom（与 parse_decl_binders 的组感知机制不一致）", "note": "def/theorem/example 都走 push_binders；axiom 直接读整条箭头类型，遇到 LParen 即 `expected axiom type`"}, "repro": "docs/gaps/repro/G13-axiom-binder-params.sh", "today": "`axiom Foo (α : Type) : Prop` → parse `expected axiom type, found LParen`（grade exit 1）；柯里化 `axiom Foo : (α : Type) -> Prop` 正常；`def Bar (α : Type) : Type := α -> α` 正常", "expected_lean": "官方 Lean 4 里 `axiom Foo (α : Type) : Prop` 完全合法，与柯里化写法等价；四种声明形式的 binder 语法应当一致", "workaround": "axiom 一律柯里化（现有 course 与共享库都是这么写的，所以一直没被发现）", "blocks": ["共享库的可读性（axiom 族要写成箭头链）", "照 Lean 4 习惯写库的作者"], "wo": "docs/gaps/WO-008-axiom-binder-params.md", "fixed_in": "0.59.0", "notes": "调研探针首次发现（docs/notes/settheory-survey/repro/P1-axiom-params.sokonanoda）；加重情节：同一文件走 query check 报 ok:true + 全零计数（G-10），MCP 通道连 parse 错误都看不见。｜**已修（0.59.0）**：axiom 接受 binder 参数表（与 def/theorem 一致）；复现件已翻成\"已修后形状\"（.sh 复现 exit 1）。"}
{"id": "G-12", "title": "相对路径入口 + 祖先清单 ⇒ 模块根退化成空路径 ⇒ 所有 import 报找不到", "kind": "language", "severity": "blocker", "status": "fixed", "found": "2026-09-18", "found_by": "course-agent", "where": {"area": "crates/front/src/project/manifest.rs:58 find_manifest + :112 module_root（配合 resolve.rs 的 exact_lookup）", "note": "相对入口目录向上走到 `\"\"` 时 `Path::new(\"\").join(\"sokonanoda.toml\")` 命中，清单路径没有目录分量 ⇒ parent() = Some(\"\") ⇒ 模块根 = 空 PathBuf ⇒ fs::read_dir(\"\") ENOENT"}, "repro": "docs/gaps/repro/G12-relative-entry-ancestor-manifest.sh", "today": "从仓库根跑 `scripts/soko grade units/u.sokonanoda`（文件里有 import）报 import-not-found（期望路径渲染成 `lib/Lib.sokonanoda`，query project 的 root = ''）；同一文件改用绝对路径即 1 checked / 0 failed", "expected_lean": "（非 Lean 语义，是模块根发现）相对路径与绝对路径必须给出同一个模块根；模块根 = 最近祖先清单所在目录，永不退化成空路径", "workaround": "判卷一律传绝对路径（课程仓的 scripts/check-course.py 已这么做）；或把清单放在与入口同目录（现有 course/unit11-project 的形状，所以一直没暴露）", "blocks": ["课程仓的 units/ + lib/ 布局（含 G-06 修好后的课程聚合）", "AGENTS.md 里 `scripts/soko grade <相对路径>` 的文档写法"], "wo": "docs/gaps/WO-002-module-root-absolute.md", "fixed_in": "0.59.0", "notes": "修法方向：在 plan_project 里把入口目录绝对化（例如 std::path::absolute / canonicalize）再上溯；顺带给 module_root 加「空 parent 视为 `.`」的护栏。修好前课程仓只能用绝对路径。｜形状②（2026-09-18 实测补充）：入口用**裸文件名**、cwd 在清单的**子目录**里时（`cd courses/set-theory/units && scripts/soko grade unit05-pairs-products.sokonanoda`），上溯还没走到清单就先在空分量 `\"\"` 处断掉（`Path::new(\"units\").parent() == Some(\"\")`），零配置退路抢跑 ⇒ 模块根 = 入口目录、3 × `import-not-found`。复现脚本 `docs/gaps/repro/G12-relative-entry-ancestor-manifest.sh` 同轮补了形状②的自断言；修法 WO-002：entry 与 root_override 一起词法绝对化 + 两处空分量护栏。｜**已修（0.59.0）**：入口与 root_override 一起词法绝对化；模块根不再退化成空路径；复现件已翻成\"已修后形状\"（.sh 复现 exit 1）。"}
{"id": "G-11", "title": "启动器在非 Rust 仓库（课程仓）没有版本源，拒绝运行且只报裸 ENOENT", "kind": "tooling", "severity": "blocker", "status": "fixed", "found": "2026-09-18", "found_by": "course-agent", "where": {"area": "scripts/soko repoVersion()（只读 <repo>/Cargo.toml 的 version）", "note": "文件头承诺的解析链是「显式覆盖 → 仓库构建 → 版本匹配缓存 → 扩展自带 → 版本锁定下载」，但版本来源只有 Cargo.toml；文档承诺「缓存过期就拒绝运行并提示」，实测是裸 OS 错误"}, "repro": "docs/gaps/repro/G11-launcher-version-source.sh", "today": "课程仓（有 sokonanoda-version.txt）能解析出自己的版本钉：version --json 的 version=钉、version_source=version.txt；marker 对不上或没有版本源时启动器拒绝 exec 缓存（exit 3 + 人话，不再看到旧二进制的裸 ENOENT）", "expected_lean": "（非 Lean 语义）课程仓必须能自钉版本：SOKONANODA_VERSION 环境变量 → <repo>/sokonanoda-version.txt → <repo>/sokonanoda.toml 的 requires → <repo>/Cargo.toml；版本对不上时给人话提示", "workaround": "不再需要：版本钉源链（SOKONANODA_VERSION → sokonanoda-version.txt → requires → Cargo.toml）已让课程仓自举；SOKONANODA_BIN 仍是显式兜底", "blocks": ["独立课程仓的建仓（P0.0）", "课程仓 CI 的 setup 步骤"], "wo": "docs/gaps/WO-001-launcher-version-source.md", "fixed_in": "0.59.0", "notes": "独立仓库形态（用户 2026-09-18 定调）的入场券；WO-001 已实现：启动器版本源链 + 无期望版本绝不 exec（G-16）。复现 G11-launcher-version-source.sh 契约已翻成 exit 1；等主线 bump 版本后跑 `python3 scripts/gap.py close G-11 --version <新版本>` 写 fixed_in（本 WO 不动版本号）。｜**已修（0.59.0）**：启动器版本源链落地：SOKONANODA_VERSION → sokonanoda-version.txt → sokonanoda.toml requires → Cargo.toml；复现件已翻成\"已修后形状\"（.sh 复现 exit 1）。"}
{"id": "G-10", "title": "query check 对解析失败的源文本返回全零 + ok:true + 退出码 0", "kind": "tooling", "severity": "blocker", "status": "fixed", "found": "2026-09-18", "found_by": "course-agent", "where": {"area": "front::query::QueryDoc::check（crates/front/src/query/mod.rs:272）", "note": "注释在 :594 承诺「check 会带着 parse 诊断返回」，实现没有读 self.parse_error（该字段只在 set_text :118 / set_cached_entry :206 被写）"}, "repro": "docs/gaps/repro/G10-query-check-parse-error.sh", "today": "counts 全 0、failed: []、ok:true、exit 0；同一份文本 grade 报 unexpected-token + exit 1", "expected_lean": "解析失败必须作为诊断出现；agent 查询通道的 ok:true 只表示「问出来了」，不能吞掉 parse 错误", "workaround": "课程脚本一律用 grade 或比对 decl 计数，不要只看 ok / failed 为空", "blocks": ["卷 I 全部单元（课程体检脚本）", "任何用 MCP check 做判卷的 agent"], "fixed_in": "0.59.0", "notes": "MCP 工具 mcp__sokonanoda__check 转发同一条通道，因此 DSH 侧同样假绿｜0.59.0 修（WO-003）：check 把 parse_error 合成进 failed[]（name:null、start/end=span offset）、counts 保持全 0、ok 保持 true、退出码 1，与 grade 同口径；复现脚本已重写成修后形状（exit 1）｜**已修（0.59.0）**：query check 对解析失败的文件现在带 parse 诊断 + exit 1（与 grade 同口径）；复现件已翻成\"已修后形状\"（.sh 复现 exit 1）。", "wo": "docs/gaps/WO-003-query-check-parse-error.md"}
{"id": "G-01", "title": "开练习的签名不做类型检查（签名写错与「还没做」无法区分）", "kind": "language", "severity": "blocker", "status": "fixed", "found": "2026-09-18", "found_by": "course-agent", "where": {"area": "elab 开练习路径 open_goal（crates/front/src/compile/goals.rs）", "note": "值位 sorry 时只走 AST 走查登记 OpenExercise，不回填类型检查结果"}, "repro": "docs/gaps/repro/G01-open-exercise-signature.sokonanoda", "today": "theorem t9 : 3 := sorry → exercise_open=1、failed=[]；query goals 给 ⊢ 3", "expected_lean": "theorem 的签名必须能 elaborate（`3 : Nat` 不是 Prop ⇒ 报类型错误），签名错误不能因为值位是 sorry 而免检", "workaround": "把签名复制进一个带真实值的 checked 声明人肉跑一遍（不可扩展到 >100 题）", "blocks": ["卷 I 全部单元", "任何依赖 golden 计数发现签名腐烂的课程"], "fixed_in": "0.59.0", "notes": "判据要盯 failed/diagnostic，不要盯 exercise_open 计数——这正是 §6.4 要防的假绿｜修后 repro = exit 1 + kernel-expected-sort；判据三层见 docs/gaps/WO-004-open-exercise-signature.md｜**已修（0.59.0）**：开练习签名受检（elab 不吞错 + 内核两步终审）；`theorem t : 3 := sorry` 现在报 kernel-expected-sort + Failed，不发 exercise.open。｜**期望方向**：本条的「修好」就是判红——复现件钉的是「签名写错必须被拒」这个契约，所以台账写显式 `repro_expect: \"rejected\"`（gap.py 的 status 推导对它不适用，见 scripts/gap.py 的 judge()）。判红的具体形状（diagnostic kernel-expected-sort、无 exercise.open）由 crates/cli/tests/protocol.rs 与课程门禁 G1–G5 双钉。", "wo": "docs/gaps/WO-004-open-exercise-signature.md", "repro_expect": "rejected"}
{"id": "G-02", "title": "构造子没有命名空间，且构造子名全局唯一", "kind": "language", "severity": "blocker", "status": "fixed", "found": "2026-09-18", "found_by": "course-agent", "where": {"area": "elab 归纳块安装（crates/front/src/compile/elab.rs）", "note": "构造子以裸名进环境；Lean 4 里构造子自动带类型前缀（Pair.mk / Or.inl / Exists.intro）"}, "repro": "docs/gaps/repro/G02-ctor-namespace.sokonanoda", "today": "Pair.mk → unknown identifier `Pair.mk`；两个 inductive 各写 ctor mk → duplicate declaration mk", "expected_lean": "构造子进入类型自己的命名空间：Or.inl / Pair.mk / Subtype.mk 并存不冲突（当前 unit9 用裸名 inl，改名需兼容旧写法）", "workaround": "构造子用假唯一名（prod_mk / exists_intro）或直接避免命名冲突；与真实 Lean 4 写法脱节", "blocks": ["卷 I 单元 3（序对/积）", "共享库 lib/（Prod/Subtype/Exists 都要 mk）"], "fixed_in": "0.59.0", "notes": "改动会碰既有课程（Or.inl 裸名）与 golden，建议「裸名保留别名 + 新增前缀名」；设计先行｜**已修（0.59.0）**：构造子命名空间落地（规范名 Ind.ctor + 裸名解析别名，重名报 elab-ambiguous-ctor-alias）；课程零改动。", "wo": "docs/gaps/WO-005-ctor-namespace.md"}
{"id": "G-03", "title": "Prop 结果 + Type 参数 + 单构造子 + 自有字段的 inductive 被内核断言拒绝", "kind": "language", "severity": "blocker", "status": "fixed", "found": "2026-09-18", "found_by": "course-agent", "where": {"area": "front 递归子派生 vs 内核 init_k_target", "note": "怀疑 crates/front/src/compile/elab.rs:2437-2490（is_k_target/is_prop_block_ty/small_elim）与 crates/kernel/src/inductive.rs:1268 的 large-elimination 判据不一致；内核是冻结快照，优先在前端修"}, "repro": "docs/gaps/repro/G03-prop-type-param-inductive.sokonanoda", "today": "kernel-rejected: assertion `left == right` failed / left: 1 / right: 0", "expected_lean": "这就是 Exists 的形状，Lean 4 完全合法（inductive Exists {α : Sort u} (p : α → Prop) : Prop | intro (w : α) (h : p w)）", "workaround": "Exists 立成公理三件套（course/unit8-quantifiers.sokonanoda:101-103，从 0.4x 绕到今天且从未进 backlog）", "blocks": ["卷 I 单元 8（基数 Ⅱ / Cantor）", "共享库 lib/Exists.sokonanoda 由公理升级为归纳"], "fixed_in": "0.59.0", "notes": "已附五条通过/失败的边界对照（repro 文件注释），可直接二分；若必须动内核走 docs/architecture.md §6｜**已修（0.59.0）**：递归子宇宙参数镜像内核 large_elim_test（前端修，内核冻结）；Exists 形状的归纳现在可用。", "wo": "docs/gaps/WO-006-prop-type-param-inductive.md"}
{"id": "G-04", "title": "没有 notation / infix（用户自定义记法）", "kind": "language", "severity": "blocker", "status": "fixed", "found": "2026-09-18", "found_by": "course-agent", "where": {"area": "parser 命令集 + 词法符号类 + elab 记法展开（crates/front/src/{token,parser,ast}.rs、compile/elab.rs）", "note": "设计 docs/design/notation-subset.md（N1–N7）；四条命令 infix/infixl/infixr/notation 进入教学语法子集，符号是独立 token，展开在 elab 内做 source-to-source 重写"}, "repro": "docs/gaps/repro/G04-notation.sokonanoda", "today": "0.58.0 实测三层现象（第一刀已修）：① 没有字符串 token；② 数学符号是标识符字符；③ 记法展开没人补前导类型参数 α。**第二刀（0.60.0）**：`prefix:N`/`postfix:N` 落地、声明驱动的词法让 `𝒫`/`ᶜ`/``/`⁻¹'`/`×ˢ` 读得出来、被导入模块的记法在入口直接可用", "expected_lean": "`infix:N` / `infixl:N` / `infixr:N` / `prefix:N` / `postfix:N` / 零元 `notation` 可用（声明之后生效；被导入模块声明的记法在入口从文件头可用）；点名形式继续可用（两种写法判卷一致）；省 α 的点名写法仍被内核拒绝（护城河）", "workaround": "（已消除）第二刀之后卷 I 的五个集合论符号随 `import lib.Set` 直接用，不必再写前缀点名形式", "blocks": [], "wo": "docs/gaps/WO-011-notation.md", "fixed_in": "0.59.0", "notes": "按硬规则 3「三件套」（课程 + 测试 + 白名单）落地；建议先做最小子集（infix + 常量记法），binder 记法（∃ x, …）另立｜**唯一还没有工作单的 blocker**：下一步开 WO-011（记法设计：∈/⊆/∪/∩/∅ 属 Lean core 级记法、𝒫//⁻¹'/×ˢ 属 Mathlib 级；见 docs/notes/settheory-survey/prior-art-report.md §1.3）。｜**第一刀已落地（0.59.0）**：Lean core 级记法 `∈`/`⊆`/`∅` + 通用 `infix`/`infixl`/`infixr`/`notation`；设计 docs/design/notation-subset.md；三层测试 = front 单元（token/parser/elab/semantic）+ CLI e2e `crates/cli/tests/notation.rs` + 课程用例（真单元②的记法变体判卷一致）。**课程零改动**（画布仍写点名形式，由 `the_shipped_course_still_uses_the_pointful_spelling` 钉住）；复现件已翻成「已修后形状」（补了使用行 `def use … := a ∈ A`）。**第二刀（未做）**：`𝒫`(U+1D4AB)/`ᶜ`(U+1D9C) 是 Unicode 字母不是符号、`''`/`⁻¹'`（`'` 是标识符续字符）、`×ˢ`、跨 import 的记法、binder 记法（`∃ x,`）、记法重载（重复符号现为 error）。｜**已修（0.59.0）**：Lean core 级记法落地（∈/⊆/∅ + 通用 infix/notation）；点名形式永久可用，两者判卷一致。｜**第二刀已落（0.60.0）**：设计 `docs/design/notation-subset.md` §10–§12。落地项 = ① `prefix:N`/`postfix:N`（与 infix 族同诊断；`prefix:N` 的操作数按 `parse_operators(N)`、`postfix:N` 在爬升里 `N >= min_precedence` 才吸收 ⇒ **N 越大绑得越紧**，两个方向都有 `by rfl` 钉住）；② **声明驱动的词法**（`scan_notation_symbols` + `tokenize_with_symbols`：声明过的符号在本文件里读成 `Sym`，最长匹配、标识符内部断开 ⇒ `𝒫 A`/`Aᶜ`/`f  A`/`f ⁻¹' B`/`A ×ˢ B`；`'` 单独出现升级为 `Sym` ⇒ 未声明符号这条专用诊断）；③ **跨 `import` 传播**（闭包加载器先收 import 边、先访问依赖，再把依赖导出的记法表当继承表解析自己；记法表按 import 边合并、没 import 的不泄漏；重声明继承来的符号是 parse 错误；配套 `project::is_project_source` 让「入口单独 parse 失败但写了 import」也走闭包）；④ 课程库 `lib/Set.sokonanoda` 末尾声明五个符号（`𝒫` prefix:100 / `ᶜ` postfix:100 / `` `⁻¹'` `×ˢ` infixr:80），单元③⑧ 各一条 `example` 演示。**顺带修掉三条既有缺陷**（都写了回归测试）：`notation_prefix_args` 的 `j - missing` 下溢（`Set.image` 有 2 个前导参数，debug 下 panic）；`by.rs::atom_text` 的括号清单漏 `Notation`（`by rfl` 在记法操作数上整条判红）；`elab_notation` 读目标签名用 `judge_infer` 会在目标本身是函数时剥错 binder（改用新加的 `judge::judge_type_of`）。另外**修掉第一刀遗留的边界**：`by` 块目标里的记法现在判得动（判卷通道从前缀 `FolFile` 收记法声明）。**仍未做**（设计 §12 逐条给理由）：binder 记法（`∃ x,`）、记法重载、`scoped`、集合字面量 `{a}`、源码级 print-back、一元记法在实参位免括号。验收：课程门禁 **36 目标 · 329 checked · 99 open · 0 判负**（与改前逐项相同）、`cargo test --workspace --locked` exit 0、`gap.py check` exit 0。｜**第三刀已落（0.60.x）**：设计 `docs/design/notation-subset.md` §13–§14。落地项 = ① binder 记法 `binder_notation \"∃\" => Exists`（`∃ (x : α), p` ⇒ `Exists α (fun (x : α) => p)`；两段式 `∃ x ∈ s, p` ⇒ `Exists α (fun (x : α) => And (x ∈ s) p)`、`∀ x ∈ s, p` ⇒ `∀ x, x ∈ s -> p`；binder 类型只从标注或 guard 反解，不做元变量/合一；命令拼写是 `binder_notation`——`notation-binder` 里的 `-` 不是标识符字符，拼不出来）；② 记法重载（同符号同形状多目标，按期望类型的结果类型选候选；`elab-notation-ambiguous` / `elab-notation-no-candidate`；重声明 import 来的符号仍是错误）；③ `scoped` / `open scoped`（作用域名 = 声明点所在 namespace 的全前缀；`open scoped` 只开记法不开名字；随 import 传播但挂起）；④ 集合字面量 `{a}` / `{a, b}`（新语法，展开成 `Set.singleton` / `Set.pair`；`set-literal-shape` / `elab-set-literal-unknown-target`）；⑤ 前缀记法实参位免括号 `f 𝒫 A`（后缀明确不做：`f Aᶜ` 今天读作 `(f A)ᶜ`）。**仍未做**（§13 逐条给理由）：源码级 print-back（内核冻结下销不掉，任务要求不做）、`notation3`/依赖 binder、一般隐式实参推断、`open scoped` 子命名空间、编辑器词表同步。课程侧只加演示（单元⑧ `example`），门禁 **36 目标 · 329 checked · 99 open · 0 判负** 逐项不变。"}
{"id": "G-05", "title": "没有 namespace / open", "kind": "language", "severity": "painful", "status": "fixed", "found": "2026-09-18", "found_by": "course-agent", "where": {"area": "parser 命令集", "note": "P7 backlog 里已有 namespace/open（docs/HANDOVER.md §3 G 末尾、ROADMAP.md §10）"}, "repro": "docs/gaps/repro/G05-namespace-open.sokonanoda", "today": "parse unexpected-token", "expected_lean": "namespace Foo … end Foo + open Foo（短名可用）", "workaround": "声明名允许带点（def Set.mem … 实测可判卷），库里用「伪命名空间」点名；缺的是 open/短名与作用域", "blocks": ["共享库 lib/ 的人体工学（非硬阻塞）"], "wo": null, "fixed_in": "0.60.0", "notes": "已在语言自身 backlog（P7），本台账只留指针，不重复记账｜**已修（0.60.0）**：`namespace <name>` / `end <name>` / `open <name>` 三条**源级作用域命令**落地（设计 `docs/design/namespace-open.md`，as-built）；声明名在 parser 里加前缀（`namespace A` 里 `def mem` ⇒ 全局名 `A.mem`，带点则拼接 `A.Set.mem`），引用按「命名空间链从内到外 → 精确名 → open 的前缀」解析；三条命令零事件、不进声明表（与 import/记法同族），`end` 错配与未闭合各有专用 parse 码。作用域文件内（open 不跨 import），但被导入模块的全局名可见 ⇒ 入口 `open Set` 对依赖的 `Set.mem` 有效（e2e 钉住）。课程迁移：`courses/set-theory/lib/Set.sokonanoda` 包进 `namespace Set`（`def Set` 类型本体留根上），全局名零改动、外部引用零改动，课程门禁改前/改后同计数（36 目标 / 329 checked / 99 open / 0 判负）。实测边界：`by` 引擎的 `apply` 按文本对齐，源里短名与内核 pp 全名不同 ⇒ 用了 namespace/open 的文件里根目标先过一遍内核 pp（`judge_render_type`），没碰命名空间的文件零额外开销；未做 `open … in` / `hiding` / `renaming` / `export` / `section` / 跨文件传播。｜**第二刀（0.60.x）**：上面「未做」清单里的 `open Foo (a b)` / `hiding` / `renaming` / `open Foo … in <命令>`（局部，限叶子命令）/ `export`（文件内与 open 同判、额外**跨 import**，单元切换时重放导出表）/ 遮蔽 warning（`open-shadowed-name`，语法级、只看本文件，不是 error）已落（设计 §N7/N8 + §9 as-built）；`section`/`variable` 实测**做不动**（本语言应用逐位显式、无隐式参数插入：`#check id Nat` ⇒ `Nat -> Nat`，auto-bound 落不出 Lean 的体验，理由在 §N9）；`namespace` 跨文件传播**已澄清：无需做**（namespace 本来就是全局前缀、闭包 known 扁平，Lean 的 namespace 同样是文件内词法作用域）；`open scoped Foo in …` 明确不做（记法表是 parse 期全局副作用）。"}
{"id": "G-06", "title": "sokonanoda course 只按单文件编译，不认 import", "kind": "tooling", "severity": "blocker", "status": "fixed", "found": "2026-09-18", "found_by": "course-agent", "where": {"area": "crates/cli/src/course.rs:118 count_unit → compile_cached(&file, src, &CompileOptions::default())", "note": "CompileOptions（crates/front/src/compile/prelude.rs:31）只有 prelude 字段，没有路径/模块根 ⇒ 项目闭包不被加载"}, "repro": "docs/gaps/repro/G06-course-import.sh", "today": "course 聚合报 failed: 1；同一文件走项目模式 query check 是 1 checked / 0 failed", "expected_lean": "课程清单的单元若声明了 import，聚合器应走项目闭包（模块根 = 最近 sokonanoda.toml / 清单目录）", "workaround": "单元保持自给自足（入库时把库内容复制进画布）——与「库化课程」目标冲突", "blocks": ["卷 I 的 10 单元共享 lib/ 的整个方案", "课程进度聚合"], "fixed_in": "0.59.0", "notes": "同一根因还会影响 query 的摘要键与 build 的项目路径，修复时一起看 crates/cli/src/project_cache.rs｜夹具目录：docs/gaps/repro/G06-course-import/（proj/ + course.json）；自断言脚本见 repro 字段｜**已修（0.59.0）**：course 走项目闭包；真实课程现场 `soko course courses/set-theory/course.json --json` 由 failed:63 → **failed:0**（12 单元全部 compiled）。", "wo": "docs/gaps/WO-007-course-import.md"}
{"id": "G-07", "title": "课程清单格式扁平（无卷/章/先修/标签/练习配额）", "kind": "infra", "severity": "painful", "status": "fixed", "found": "2026-09-18", "found_by": "course-agent", "where": {"area": "course.json 契约（crates/cli/src/course.rs:29-41 只读 file/title/unit；title_en/en_file 由 scripts/gen-site-data.py 约定）", "note": ""}, "repro": "docs/gaps/repro/G07-course-manifest-v2.sh", "today": "卷 I 的「卷/章/先修/练习类型配额」只能写进文档，机器读不到", "expected_lean": "（非 Lean 语义）清单需要可扩展字段，且向后兼容既有 11 单元", "workaround": "先沿用扁平清单，把卷/章信息放在文档与文件名里", "blocks": ["P6 度量与看板", "多课程/多卷聚合"], "wo": null, "fixed_in": "0.60.0", "notes": "as-built（0.60.0）：清单升成 soko.course/2（卷/章/先修/标签/quota.exercises，设计 docs/design/course-manifest-v2.md）；v1 扁平数组继续被接受（入门课未改，它就是兼容性活体回归）。五个消费者同轮跟上：CLI course 两种都读、v2 的 course.unit 新增 volume/chapter/tags、summary 新增 volumes/chapters（v1 事件一个键都不多）；课程门禁展平后 G1–G5 语义不变 + 新判据 G6=清单自洽（id 唯一 / unit 恰好一章 / prereqs 不悬空；quota 差额只报告不判红），--selftest 覆盖 G6 并有纯清单单测 tools/test_manifest_v2.py；VS Code 课程树 v2 按卷→章→单元分组（v1 平铺保留）；站点卷 I 页面按卷/章分组（计数仍实测自门禁）。原 notes：改动面小但影响 site 生成器与 course 测试；建议随 P2 一起做。收尾轮（0.60.x）：§7 的「不做多卷聚合」已销账——CLI `course <path>… [--all]` 一次聚合多份清单（>1 份才加 `course.unit.manifest`、summary 加 `manifests`；坏清单一票否决且零事件；`--all` 递归发现、字典序、去重），成本台账 `docs/courses/ledger.jsonl`（`check.py --ledger`，默认关闭、人工跑）落地，实测见 `docs/design/course-manifest-v2.md` §4.5/§4.6/§8.1。"}
{"id": "G-08", "title": "没有 abbrev（可展开的类型别名）", "kind": "language", "severity": "nice", "status": "fixed", "found": "2026-09-18", "found_by": "course-agent", "where": {"area": "parser 命令集", "note": ""}, "repro": "docs/gaps/repro/G08-abbrev.sokonanoda", "today": "0.60.0 实测（**修前**）：parse 阶段 `unexpected-token`（`abbrev` 不是命令）。**修后**：`abbrev` 走 `parse_def`，产出与 `def` 逐字段相同的 `Command::Def`（真 Lean 的 `abbrev` 行可原样编）；复现件已翻成已修后形状，`scripts/gap.py check` 判 clean", "expected_lean": "abbrev Set (α : Type) : Type := α → Prop（定义上可展开，类型位置透明）", "workaround": "def 够用：OK-set-spike.sokonanoda 实测 def Set 之后 A x / Set.subset 都能判卷（走内核 def-eq）", "blocks": [], "wo": null, "fixed_in": "0.60.0", "notes": "等真撞上「类型位置不展开」的具体案例再修，不提前做｜**as-built（0.60.0，G-08 关账）**：先测量再决定——设计 `docs/design/abbrev.md` §1 的实测结论是**本语言里 `def` 与 Lean 的 `abbrev` 没有可观察差异**：① 类型位/项位透明（`def A1 (α) (f : α -> Prop) : Set α := f` 与反向都 `decl.checked`）；② `#reduce Set => fun (α : Type 0) => α -> Prop`；③ hover/`#check` 保留别名名不展开（真 LSP 实测 `A : Set α`、`Set.mem : forall (α : Type 0), α -> Set α -> Prop`）；④ 项层 `rfl` 判等成立（`Eq.{1} (Set α) (Set.empty α) (fun x => False) := by rfl` checked）；⑤ 递归两边都不支持（`def loop (n : Nat) : Nat := loop n` ⇒ `elab-unknown-identifier`）；⑥ 唯一的真实差异是 reducibility hint，而本语言只有一个透明度层级——内核 `ReducibilityHint::Abbrev` 只被 `conv::is_lt` 用来决定先展开哪一边（效率启发式），且没有 `simp`/`rw`/类型类能观察它。所以按任务书分支实现成**与 `def` 同语义的关键字**：parser 一条臂 + `is_reserved_command` 加 `abbrev`，AST/elab/内核零改动。测试三层：front 7 条（`parser::tests::abbrev_*` 3 + `compile::tests::abbrev_*` 4）、CLI e2e 3 条（`crates/cli/tests/cli.rs::cli_abbrev_*`）、复现件转绿。白名单：CLI help（`abbrev <name> : <type> := <value>`）+ `docs/architecture.md` §4.1 命令清单。**未做（交给主线）**：`front::semantic::KEYWORDS` 与 `editor/vscode` TM 语法词表本轮禁改，所以 `abbrev` 暂不高亮成关键字——两份词表必须同一轮加（`tm_grammar_keywords_follow_the_single_source` 是守护）"}
{"id": "G-09", "title": "内核断言以裸文本外泄（left: 1 / right: 0），hint 是通用「类型不匹配」", "kind": "tooling", "severity": "painful", "status": "fixed", "found": "2026-09-18", "found_by": "course-agent", "where": {"area": "kernel 错误包装 + front::error::refine_kernel_kind", "note": "与 G-03 同源：断言失败没有稳定错误码"}, "repro": null, "today": "message = 'rejected: assertion `left == right` failed\\n  left: 1\\n right: 0'，hint 说「类型不匹配或证明项不完整」", "expected_lean": "内部断言不应成为用户可见错误；至少给出稳定 code + 「这是编译器缺陷」的提示", "workaround": "无（已无需绕过）", "blocks": [], "wo": null, "fixed_in": "0.59.0", "notes": "**已修（0.59.0）**，两半各自落实：①用户可见面有稳定码——`refine_kernel_kind` 把 `assertion failed:` / `called \\`Option::unwrap()\\`` / `internal error:` 一律归 `ErrorKind::KernelInternal` ⇒ code `kernel-internal` + hint「内核内部错误（这不是你的代码问题）。请把这段代码反馈给工具作者。」（crates/front/src/compile/error.rs，测试 crates/front/src/compile/tests.rs::refine_kernel_kind_internal_shapes_stay_kernel_internal 与 kernel_fine_grained_kinds_stage_as_kernel_with_codes）；设计见 docs/design/kernel-taxonomy.md。②唯一已知可达触发路径（Prop+Type 参数 inductive 的计数断言）随 G-03/WO-006 关闭，所以原复现件（与 G-03 共用 docs/gaps/repro/G03-prop-type-param-inductive.sokonanoda）已转绿 ⇒ **撤下 repro 字段**（留一个「应当判绿」的文件只会假报缺口）。边界：内核冻结，「断言永不外泄」是不变量而非可达缺口；若日后写出新的可达反例，按 `kernel-internal` 码新开一条台账（而不是把这条改回 open）。"}
{"id": "G-16", "title": "启动器在「版本未知」时会 exec 缓存里的陈旧二进制（绕过「过期即拒绝」守卫）", "kind": "tooling", "severity": "blocker", "status": "fixed", "found": "2026-09-18", "found_by": "WO-001 作者（写 G-11 工作单时实测）", "where": {"area": "scripts/soko 的 resolve() 与守卫分支", "note": "!version 分支把 cache(STALE…) 改写成 cache(unknown repo version)（≈:339-342），而守卫只认 cache(STALE 前缀（≈:564）⇒ 本机缓存 0.55.0 时真的 exec 了旧二进制"}, "repro": "docs/gaps/repro/G11-launcher-version-source.sh", "today": "版本未知时：setup → exit 3 且 stderr 指向 v? 的下载地址（死循环）；grade → exit 3「Run scripts/soko setup first」；doctor --json 没有 version 键；有旧缓存时**真的 exec 0.55.0**，报出的是旧 CLI 的 error: No such file or directory", "expected_lean": "（非 Lean 语义，是工具链行为）AGENTS.md 承诺「缓存过期就拒绝运行并提示」——版本未知时绝不允许 exec 任何缓存二进制，必须提示如何钉版本或显式 SOKONANODA_BIN", "workaround": "显式 SOKONANODA_BIN=<绝对路径>；或让仓库有 Cargo.toml（语言仓内本身没这个问题）", "blocks": ["非 Rust 仓库（独立课程仓）的工具链自举", "CI 里 setup 步骤的正确性"], "wo": "docs/gaps/WO-001-launcher-version-source.md", "fixed_in": "0.59.0", "notes": "与 G-11 同文件不同缺陷：G-11 是「没有版本源」，G-16 是「有旧缓存时守卫失效」。WO-001 已把两者一起写进修法。｜**已修（0.59.0）**：版本未知时绝不 exec（守卫收紧，不再把 STALE 改写成 unknown 绕过）；复现件已翻成\"已修后形状\"（.sh 复现 exit 1）。"}
{"id": "G-17", "title": "query goals / holes 对解析失败也假绿（空数组 + ok:true）", "kind": "tooling", "severity": "painful", "status": "fixed", "found": "2026-09-18", "found_by": "WO-003 作者（写 G-10 工作单时顺带实测）", "where": {"area": "crates/front/src/query/ 的 goals()/holes()（与 G-10 的 check() 同族：没人读 self.parse_error）", "note": ""}, "repro": "docs/gaps/repro/G17-query-goals-holes-parse-error.sh", "today": "解析失败的文件：goals → data: []、holes → holes: []，都 ok:true、exit 0；state 则给 outside-declarations", "expected_lean": "（契约）解析失败应向所有 op 一致地暴露（至少 ok:false 或带 parse 诊断），不能各自为空", "workaround": "先跑 grade 的退出码确认文件能编译，再问 goals/holes", "blocks": ["agent 用 goals/holes 判断「还有哪些洞」时的可信度"], "wo": null, "wo_planned": "WO-003", "fixed_in": "0.59.0", "notes": "建议与 G-10 同轮修（同一个 parse_error 字段的消费点）；WO-003 已把它列为排除项并建议另开条目——本条即该条目。｜与 G-10 同轮修（WO-003 把它列为排除项并建议另开条目——本条即该条目）：goals/holes/next_hole 返回 Result，解析失败=NotParsable ⇒ ok:false + error.code not-parsable + exit 1；空数组/None 仍只表示「正常的没有」｜**已修（0.59.0）**：goals/holes 对解析失败返回 ok:false + not-parsable（不再假绿）；复现件已翻成\"已修后形状\"（.sh 复现 exit 1）。"}
{"id": "G-18", "title": "def f.{u} 被静默解析成名字 f.（声明消失且不报错）", "kind": "language", "severity": "painful", "status": "fixed", "found": "2026-09-18", "found_by": "WO-009 作者（G-14 边界普查第 20 号样本）", "where": {"area": "crates/front/src/parser.rs 的名字/宇宙参数切分（f.{u} 本应报错）", "note": ""}, "repro": "docs/gaps/repro/G14-single-universe-binder.sokonanoda", "today": "def f.{u} … 被解析为名字 f.，声明数不变且不报任何错（静默变形）", "expected_lean": "官方 Lean 4 里 def f.{u} 合法（随后跟 binder 与类型）；本子集若不支持应报 parse 错，而不是把名字吃掉一个字符", "workaround": "不要写 f.{u}；用 def f {u} …", "blocks": ["从 Lean 4 复制粘贴代码的正确性"], "wo": "docs/gaps/WO-008-axiom-binder-params.md", "wo_planned": null, "fixed_in": "0.59.0", "notes": "与 G-14 同一次普查发现；建议随 G-14 的 WO 一起修（都在 parse_universe_params 的名字切分附近）。｜2026-09-19 落地：`expect_decl_name`（parser.rs）拒绝以 `.` 结尾的声明名，报 parse 错并给 `def f {u} …` 的改写提示；由 `def_cannot_end_with_a_dot`（front）与 `cli_universe_param_name_cannot_end_with_a_dot`（CLI e2e）钉死，复现件 G14-…sokonanoda 第 ④ 段注释登记。｜**已修（0.59.0）**：def f.{u} 现在报 parse 错并给出正确写法提示（不再静默吃掉名字）；复现件已翻成\"已修后形状\"（.sh 复现 exit 1）。"}
{"id": "L-06", "title": "语言没有累积性（def T : Type := <Prop 值> 被拒）且 Exists.elim 的 Q 只能是 Prop ⇒ 取数据的引理写不出来", "kind": "library", "severity": "painful", "status": "workaround", "found": "2026-09-18", "found_by": "单元⑨/⑦ 作者（写 lib/Equiv 与 lib/Fun 时实测）", "where": {"area": "内核/前端：无累积性；lib/Exists 的公理签名把消去限制在 Prop", "note": "0.60.0：① 的诊断已细化（专用码 `kernel-prop-not-cumulative`）；两条边界本身仍是内核性质（设计 docs/design/prop-cumulativity-boundary.md）"}, "repro": null, "today": "def T : Type := P（P : Prop）→ 内核拒绝「类型不匹配：期望 Sort(1)，实际是 Sort(0)」；因此等势只能用 Prop 值定义（Set.Equiv … : Prop），不能用 Lean 的 data 版 Equiv；Surjective f ⇒ Exists (β→α) (fun g => RightInverse g f)（满射可裂）本语言**接受签名但证不出来**——Exists.elim 的 Q 必须是 Prop，取不出函数值（= 需要选择公理）。", "lean_names": ["Classical.choice", "Exists.choose", "Equiv"], "owner": "prelude", "expected_lean": "Lean 4 有累积性（Prop ⊆ Type）且有 Classical.choice / Exists.choose 取数据；Mathlib 的 Equiv 是 data（toFun/invFun/left_inv/right_inv）", "workaround": "课程改成**数据版陈述**：把逆函数与互逆证据作为参数交给引理（unit07 的 bijective_iff_inverse、unit09 的 Set.Equiv 都是这么做的）；取数据只在单元⑩ 用成对公理 Exists.choose/Exists.choose_spec。诊断侧（0.60.0）：`def T : Type := <Prop>` 报专用码 `kernel-prop-not-cumulative` + 人话 hint（设计 docs/design/prop-cumulativity-boundary.md）", "blocks": ["卷 II+ 的商类型/等价结构", "任何「从满射抽逆函数」的证明"], "wo": null, "wo_planned": null, "fixed_in": null, "notes": "不是 bug 而是设计边界；记账的理由是它**系统性地改变了课程陈述的形状**（数据版 vs 存在版），课程文档必须写清。｜**0.60.0 诊断质量改进（这一部分算 fixed）**：`def T : Type := True`（及同形状 `def T : Type := And True True`）从裸 `kernel-rejected` 变成专用码 **`kernel-prop-not-cumulative`** + 人话 hint（内核消息一字不改：`类型不匹配：期望 Sort(1)，实际是 Sort(0)`）。分类器**只认** def-eq 双侧「期望 `Sort(n>0)`、实际 `Sort(0)`」**且两侧都是裸排序**这一个形状（`def T : Type := True`；`Pi` 形状的同一现象——`def bad : Prop -> Type := fun (x : Prop) => x`——保持通用码：它是 CLI/LSP/扩展契约测试里代表通用内核拒绝的夹具，加宽要迁移 8 处断言，不在本单文件面内，见设计 §3.1）；反方向（期望 `Sort(0)`、实际 `Sort(m>0)`）**故意不发码**——它与普通的「该写 Prop 却写了 Type」（`def f : Prop := Nat`）逐字同形，发码会误标（判别性理由见设计 §4）。`docs/protocol.md` 的错误码清单同轮补行；单测 4 条（`pipeline_classifies_prop_where_type_was_required`、`prop_not_cumulative_code_does_not_catch_other_kernel_gaps`、`generic_def_eq_mismatch_keeps_kernel_rejected`、`exists_eliminator_motive_stays_in_prop`）。｜**workaround（边界本身，冻结内核下不改）**：课程三条绕法见设计 `docs/design/prop-cumulativity-boundary.md` §5——① 等势写成命题版数据 `Set.Equiv α β A B : Prop`（`courses/set-theory/lib/Equiv.sokonanoda`，代价：取不出 `f`，要用就两次 `Exists.elim` 用完即弃）；② 满射可裂写成**数据版**（`courses/set-theory/units/unit07-functions.sokonanoda:45-55`：存在版与选择公理等价）；③ 真要取数据只有单元⑩ 的成对公理 `Exists.choose`/`Exists.choose_spec`（`courses/set-theory/units/unit10-cantor.sokonanoda:170-173`，练习 7 是全卷唯一允许用它的一题）。｜**② 的实测**：`def Exists.witness … := Exists.rec A p (fun _ => A) …` → `kernel-rejected`：期望 `Pi (x : ((Exists.[] $2) $1)), Sort(0)`、实际 `… Sort(1)`；`#check Exists.rec.{1}` → `elab-universe-arity`（消去子 **0** 个宇宙参数）；`Exists.elim`（`Q : Prop`）照常 checked。｜**不做**：累积性（`Prop ⊆ Type`）与 Prop 归纳的大消去都是**内核性质**（硬规则 1：冻结快照不改；官方 Lean 4 的 `Exists.rec` 同样只到 Prop）；不把 `Classical.choice`/`Exists.choose` 装进 prelude（L2 课程库的活）。"}
{"id": "G-19", "title": "零元记法（`∅`）落在「被应用」的位置 + `by` 块 ⇒ 补不出前导类型参数", "kind": "language", "severity": "painful", "status": "fixed", "found": "2026-09-19", "found_by": "course-agent（全课程 Lean 4 化 R1 第四片实测）", "where": {"area": "crates/front/src/spine.rs 的 unfold_head_once（源级 delta 展开）→ by.rs 的 render→re-read", "note": "`∅` 是零元记法（Notation{target:\"Set.empty\", operands:[]}）。作 `∈` 的操作数时能从 Set.mem 的第三参数位拿到期望类型 `Set α` ⇒ 解得出；作 `⊆` 的操作数时 `intro` 先要把 Set.subset（**def**）delta 展开成 `forall (x : α), A x -> B x`，`∅` 就落进**被应用**的位置（`∅ x`）——期望类型没了，回读时 Set.empty 零实参、`α` 无解。"}, "repro": "docs/gaps/repro/G19-zero-ary-notation-applied.sokonanoda", "today": "0.61.0 **修前**实测：`grade` exit 1 + `elab-tactic-failed`「`exact` 判定失败：记法 `∅` 展开成 `Set.empty` 时补不出前面的类型参数」。同文件里两个对照组都 checked：① `x ∈ ∅ -> False := by …`；② 点名 `Set.subset α (Set.empty α) A := by …`。term 模式同句也通过。**修后**：exit 0、7 条声明全 `decl.checked`。", "expected_lean": "`∅ ⊆ A := by …` 应当与 term 模式、与点名形式**同判**（记法是糖：`∅` 只是 `Set.empty` 的写法，不该因为出现在被应用的位置就判红）", "workaround": "（已消除）0.61.0 起 `∅` 在 `by` 块里与 term 模式、点名形式同判", "blocks": [], "fixed_in": "0.61.0", "notes": "**已修（0.61.0）**：修法落在 `crates/front/src/by.rs` 的 `Tactic::Intro` 分支——目标头剥不动（说明是 def）时，**先用内核 pp 的规范形态**（`canonical_goal_with_spec`，点名 + 参数写全）再剥，源级 delta 展开只作为退路。pp 形态下 `∅ ⊆ A` 是 `Set.subset α (Set.empty α) A`，展开成 `(Set.empty α) x` ⇒ 回读无碍。`keep_if_lossless` 继续挡住「pp 丢隐式实参」那一档。回归测试：`crates/cli/tests/notation.rs::a_zero_ary_notation_survives_delta_unfolding_in_a_by_block`（改前 FAILED、改后 ok，含两条改前就过的对照组）。复现件已翻成 clean；课程门禁 36/329/99/0 逐项不变、`cargo test --workspace --locked` 1180 passed。｜修法方向（原记录）：render→re-read 对零元记法补出前导参数，或对「补不出参数的零元记法」直接渲染成点名形式。", "wo": "docs/gaps/WO-012-zero-ary-notation-applied.md"}
{"id": "G-20", "title": "LSP 单文件 parse 失败会丢掉已经装好的项目报告（hover/documentSymbol 全 null + 假诊断）", "kind": "tooling", "severity": "blocker", "status": "fixed", "found": "2026-09-19", "found_by": "course-agent（全课程 Lean 4 化 R1 第四片，读 crates/lsp/src/lib.rs 时发现，随后真 LSP over stdio 复现）", "where": {"area": "crates/lsp/src/lib.rs 的 Doc::set_text（原 :160-164）与 Doc::diagnostics（原 :78-88）", "note": "front 层**专门**为这种情况留了退路（project/mod.rs 的 is_project_source：入口单独 parse 失败但写了 import 时走闭包），query/mod.rs 的 set_text_with_overlay 也确实把闭包报告装好了（self.report = Some(report)）；LSP 紧接着因 parse_error.is_some() 把 report 置 None，diagnostics 又让 parse 错误优先 ⇒ 两半互相矛盾。"}, "repro": "docs/gaps/repro/G20-lsp-drops-rescued-report.sh", "today": "0.61.0 实测（真 LSP over stdio）：打开一个 `import SetLib` + 用库记法 `∈` 的入口 ⇒ publishDiagnostics 发**一条假诊断** `notation-unknown-symbol`，documentSymbol 回答 null、hover 回答 null；**同一份文本**走 CLI `grade` 是 exit 0。", "expected_lean": "记法随 import 传播（G-04 第二刀）之后，「入口单独 parse 失败」是**常态**而不是错误：闭包编译成功时诊断必须以闭包报告为准（0 条），hover/documentSymbol 必须可用；闭包**也**失败（入口真有语法错误）时才按老契约发 parse 错误", "workaround": "编辑器里判卷改用 CLI `scripts/soko grade`（DSH 侧本来就只能这么办）；但 hover/文档符号在课程单元里一直是死的", "blocks": ["课程单元在编辑器里的 hover / documentSymbol / codeAction / inlayHint", "记法输入法 D5 的 hover 提示（正好死在最需要它的那批文件里）"], "fixed_in": "0.61.0", "notes": "修法：QueryDoc 新增 project_entry_compiled()（入口模块状态 == Compiled 才算闭包救回来了），LSP 只在它为 false 时维持老契约。回归测试两条（crates/lsp/src/tests/project.rs）：imported_notation_keeps_the_report_and_the_diagnostics_honest（0 诊断 + hover 非 null + documentSymbol 非空 + hover 文案含 `\\in`）与 a_genuinely_broken_entry_still_reports_the_parse_error（闭包也失败时仍报错）。**顺带修掉一个测试基建 bug**：crates/lsp/src/testutil.rs 的 lsp_pos 按**字节差**算 character，含多字节符号的行会偏（想 hover `⊗` 却 hover 到 `b`）——ASCII 夹具上两种算法恒等，所以一直没显形。", "wo": "docs/gaps/WO-013-lsp-drops-rescued-report.md"}
{"id": "G-21", "title": "点名调用漏了**前导类型参数**（`Set.mem a A` 少了 `α`）时，`by` 路径的报错指不出根因", "kind": "language", "severity": "painful", "status": "open", "found": "2026-09-21", "found_by": "course-agent（卷 I 收尾复查记法对照页的「护城河」话术时实测）", "where": {"area": "crates/front/src/compile/error.rs（诊断分类）+ crates/front/src/compile/check/walk.rs（签名检查的时序）", "note": "`mymem α a A` 是正解；`mymem a A` 少写最前面的 `α`，`a` 被顶到**类型位**。两条路径质量不同：① **声明位已修**（同日）：`elab` 期不查实参类型（设计如此，判定交给内核），内核在 def_eq 阶段报 `expected: Sort(1) | actual: $2`，原先落进泛化的 `kernel-rejected`；新增 `classify_term_in_type_position`（`$k` vs `Sort(n)` 形状）把它归到 `KernelExpectedSort`，提示里点名「漏了前导类型参数」并给出记法替代。② **`by` 路径仍欠**：`by` 块跑的时候声明签名**还没被内核检查过**——`open_signature`（用 axiom 探针验签名）只在值位是洞时才走，于是错误落到 tactic 引擎的比较上，报成「期望 `A a`，实际是 `mymem a A`」这种**同形**对照。"}, "repro": "docs/gaps/repro/G21-omitted-type-argument.sh", "today": "0.62.0 实测（半修）：夹具两条声明——声明位 `def bad_sig … := mymem a A` 报 `kernel-expected-sort` 且 hint 含「前导类型参数」；`by` 路径 `theorem bad_by … : mymem a A -> A a := by intro h; exact h` 仍报 `elab-tactic-failed`「`exact` 类型不匹配：期望 `A a`，实际是 `mymem a A`」（对照脚本 exit 0）。", "expected_lean": "官方 Lean 4：`Set.mem a A` 直接报 `function expected at … ` / `application type mismatch`，**指向那个应用**并说明第一个显式参数要的是 `Type`；不会拖到后面的 tactic 才炸，也不会把出错项与目标渲染成两个几乎同名的类型。", "workaround": "课程里一律写全前导类型参数（`Set.mem α a A`），或用记法（`a ∈ A`，记法路径会自动补 `α`）；记法对照页的「护城河」一节已把这条边界与**当前报错形态**都写清楚。", "blocks": ["学习者写点名调用漏参数时的自纠（错误信息看不出缺什么，只能靠人讲）", "课程文档里「点名形式要把 α 写全」这条纪律的可发现性（现在只能靠读懂同形对照）"], "notes": "修法方向（未做，需评估）：把**签名检查前移到 `by` 块之前**——`walk.rs` 在 `lower_value` 之前跑一次签名验证，让签名错在**声明处**就报出来（与 G-01「签名受检」同一条纪律）。难点：现成的 `open_signature` 会装一个 **axiom 探针**（`SIG_PROBE_NAME`）进 builder，前移要确认它不进声明表/事件流，且不为每条 `by` 声明付一次额外 elaborate 的代价。另一半（②）也可以退而求其次：让 tactic 引擎在「actual 是某个上下文假设的**书写类型**且该类型本身没通过内核」时，把内核的原始原因一起报出来。", "wo": null}
{"id": "G-22", "title": "项目入口（含 import + 库记法）的 `soko/goals` 恒为空 ⇒ Infoview「声明」栏失效", "kind": "tooling", "severity": "blocker", "status": "fixed", "found": "2026-09-21", "found_by": "user", "where": {"area": "`QueryDoc::goals` 的可用性判据（crates/front/src/query/mod.rs:481 → :403-408）+ LSP 的 `.unwrap_or_default()`（crates/lsp/src/lib.rs:495）", "note": "同一条 G-20 补丁 `check()`（query/mod.rs:353-357）与 `Doc::set_text`（lsp/src/lib.rs:170-182）都打了，**只有 `goals` 漏了**"}, "repro": "docs/gaps/repro/G22-lsp-goals-empty-for-import-entry.sh", "today": "0.63.0 实测：项目入口（`import` + 库记法，单独 parse 必然失败）的 `soko/goals` 返回 `decls: []`，而同一份文档 `documentSymbol` 有 2/8/27 项、`soko/stateAt` 正常 ⇒ **目标栏好、声明栏空**。13 个 units + 13 个 solutions + 5 个 lib 全部命中。同族：`holes`/`nextHole`（alt+n 跳洞）也经由 `goals` ⇒ 在项目文件里全不可用", "expected_lean": "项目入口的 `soko/goals` 非空，且条数与 `documentSymbol` 一致；`holes`/`nextHole` 同样可用。「真的解析不了」（闭包也失败）仍答 `not-parsable`（G-17 契约不破）", "workaround": "CLI 侧 `query goals` 在缓存热时偶然正常（`query` 走 `set_cached_entry`，会清 `parse_error`），编辑器走不到那条路", "blocks": ["Infoview 声明栏", "alt+n 跳洞"], "notes": "计划 T-B03 的修法：把判据收敛成单一 `QueryDoc::usable()`（一处判据、三处引用）", "fixed_in": "0.63.1"}
{"id": "G-23", "title": "记法符号不能跳转，hover 也不给原始类型", "kind": "tooling", "severity": "painful", "status": "open", "found": "2026-09-21", "found_by": "user", "where": {"area": "记法使用处的 hover 行硬编码 `resolution: None`（crates/front/src/compile/elab.rs:2914）；`definition_at` 只读它（crates/lsp/src/render.rs:306-314）", "note": "hover 的记法分支在 crates/lsp/src/lib.rs:809-858，`target` 只认本文件声明的记法（notation_input.rs:261-267）"}, "repro": "docs/gaps/repro/G23-notation-navigation.sh", "today": "0.63.0 实测（光标在 import 来的 `∈` 上）：hover = 「`∈` —— 记法符号 / 输入：`\\in`（别名 `\\mem`） / `a ∈ A : Prop`」——有输入法提示与外层表达式类型，**没有**「展开成 `Set.mem`」、**没有** `Set.mem` 的原始类型；`textDocument/definition` = **null**；`documentHighlight` = **null**", "expected_lean": "① hover 给出目标常量的**原始类型**（`Set.mem : …`）；② definition 跳到声明该记法的地方（跨文件时是库里的 `infix` 行）", "workaround": "点名形式（`Set.mem a A`）可以跳转与 hover", "blocks": ["编辑器导航"], "notes": "最便宜的一刀：`judge::judge_type_of_constant` 已经是 `pub`（crates/front/src/judge.rs:621-642），只差一次调用。另见计划 T-D30（rename/highlight 在记法符号上会误解析到外层 binder，调研已定位但尚未复现，等复现后再立账）"}
{"id": "G-24", "title": "清单 `requires` 不随仓库版本自动提升 ⇒ 漂移 ⇒ 项目编译缓存被静默关掉", "kind": "infra", "severity": "blocker", "status": "fixed", "found": "2026-09-21", "found_by": "user", "where": {"area": "courses/set-theory/sokonanoda.toml 的 `requires = \"0.61\"`（二进制 0.63.0）；判定在 crates/front/src/project/manifest.rs:135-144（只比 major.minor）", "note": "`requires_warning` ⇒ `ProjectReport::is_clean()`（crates/front/src/project/report.rs:158-166）为假 ⇒ `store_if_clean`（crates/cli/src/project_cache.rs:33-41）永不写缓存"}, "repro": "docs/gaps/repro/G24-project-cache-never-warms.sh", "today": "0.63.0 实测（同一份 3 行项目、隔离缓存、`build` 连跑两次）：requires=0.61 → 第二次仍 `0 hit`；requires=0.1 → 仍 `0 hit`；requires=0.63 → `1 hit`；无清单 → `1 hit`。课程实测：`query check` 冷 4.873s，`build` 之后仍 4.836s；把清单改成 `0.63` 后立刻 `1 hit`。**两条写缓存路径规则还不一致**：`build`/`check` 走 `store_if_clean(is_clean())`，`query` 走 `set_cached_entry`（不看 `is_clean`）⇒ 命不命中取决于先跑了哪条命令", "expected_lean": "① `courses/` 与 `course/` 的清单版本与仓库版本**同一来源**（同仓共同开发，本应一致），或由门禁挡住漂移；② 漂移即使发生也不该静默关掉缓存，且必须在 JSON / LSP 视图里可见；③ 两条写缓存路径规则一致", "workaround": "手动把清单的 `requires` 改成当前版本（下次 bump 又会漂移）", "blocks": ["整个卷 I 的编译缓存"], "notes": "用户判定：**开发过程没有自动提升项目清单的 `requires`** 就是根因。`sorry` **不是**原因（它只进 `exercise_open` 计数，不在 `module.report.warnings` 里）", "fixed_in": "0.63.3"}
{"id": "G-25", "title": "含 `import` 的文档在 LSP 上不读不写任何缓存 ⇒ 每次打开都从零重编整个闭包", "kind": "tooling", "severity": "blocker", "status": "fixed", "found": "2026-09-21", "found_by": "user", "where": {"area": "crates/lsp/src/lib.rs:148-156（`let cached = if cfg!(test) || has_imports { None } else { cache::load(...) }`）与 :183-194（`cache::store` 同样被 `!has_imports` 门住）", "note": "项目缓存只活在 CLI crate（crates/cli/src/project_cache.rs），LSP 依赖不到；`grep digest crates/lsp` = 0。设计 docs/design/compile-cache.md:16-17 声称「LSP 与 CLI 共用同一份」——对项目文件不成立"}, "repro": "docs/gaps/repro/G25-lsp-recompiles-on-every-open.sh", "today": "0.63.0 实测（release LSP，`didOpen → 诊断`）：unit01（2 import）1781ms / unit08（4）4599ms / unit12（7）8465ms / unit12 解答（8 模块、25 个 by）36.13s；无 `import` 的 course/unit1 ~100ms。同一会话里同文本再改只要 121–476ms ⇒ 贵的是「第一次打开一份新文档」。两个**独立进程**各开一次同一文件：338ms vs 343ms（0.99×，无提速）", "expected_lean": "含 `import` 的文档也读/写项目缓存（键 = `ProjectPlan::digest`）；第二个独立进程打开同一文件明显快于第一个", "workaround": "`sokonanoda build <file>` 预热——但 LSP 不读，所以对项目文件无效", "blocks": ["编辑器打开/按键延迟"], "notes": "T-009/G-27 是它的第二层：即使接上缓存，键里的二进制 mtime 也可能让它 miss", "fixed_in": "0.63.3"}
{"id": "G-26", "title": "Infoview 目标栏不显示记法（goal 文本的四个生产者里，内核 pp 那两个必然点名）", "kind": "tooling", "severity": "painful", "status": "open", "found": "2026-09-21", "found_by": "user", "where": {"area": "根状态取 `DeclState.ty_text`（crates/front/src/compile/check/kernel_phase.rs:170-178/207-213 → 内核 `pp_expr`）；无 `by` 的开练习取 `render_expr`（crates/front/src/compile/goals.rs:1088 等）", "note": "`by` 步进里 `canonical_goal_type`/`canonical_goal_with_spec`/`apply`/`cases` 会把源 AST 换成内核 pp 文本再 parse 回来（by.rs:497/553/1277/1450+）"}, "repro": "docs/gaps/repro/G26-goal-text-loses-notation.sh", "today": "0.63.0 实测（课程写法 `:= by` + `sorry`）：目标 = `forall (α : Type 0) (A B : Set α), Set.subset α A B -> (forall (a : α), Set.mem α a A -> Set.mem α a B)`，源文件写的是 `A ⊆ B → (∀ (a : α), a ∈ A → a ∈ B)`。**对照**：不带 `by` 的 `:= sorry` 走另一个生产者，目标 = `A ⊆ B`（记法保留）⇒ 两个生产者行为不同，修法必须按生产者分｜**真 VS Code e2e 逐列量过（夹具 `units/u01` 的 `sorry` 行）**：光标在 `sorry` **内**（半开区间 `start<=cursor<end`）⇒ 根状态 ⇒ **点名**（`forall (α : Type 0) (A B : Set α), Set.subset α A B -> …`）；光标在 `sorry` **之后** ⇒ 走另一支（无 by 的声明级目标）⇒ **记法保留**（`(A ⊆ B) -> (x : α) -> (x ∈ A) -> x ∈ B`）；`by` 行内 ⇒ 根状态 ⇒ 点名。⇒ **用户看到的是根状态那一路**，而 `revealRange` 会把光标停在 range 末尾（= 之后）⇒ e2e 一度假绿，必须显式设 selection（`extension.test.js` 用例 #6）。", "expected_lean": "两个生产者的目标/类型文本都用上源文件的记法；`Eq` 之类的点名形式仍可回退", "workaround": "无（学习者只能看到点名形式）", "blocks": ["Infoview 可读性"], "notes": "**不能改内核 pp**：`pp_expr` 同时是 `#check`/`#reduce`/`#print` 的出口 ⇒ 会动 `--json` 字节（硬规则 1 的「golden 与 --json 逐字节不变」）。走 front 侧显示边界重写，设计见 docs/notes/course-lean-style/printback-feasibility.md §4"}
{"id": "G-27", "title": "编译缓存的键折进了可执行文件的 mtime ⇒ 同一份二进制换个 mtime 就 miss", "kind": "infra", "severity": "painful", "status": "fixed", "found": "2026-09-21", "found_by": "agent", "where": {"area": "crates/front/src/compile/cache.rs 的 `build_stamp()` = `current_exe()` 的 mtime（秒级）", "note": "CLI（sokonanoda）与 LSP（sokonanoda-lsp）是两个不同的可执行文件；release 走 upload/download-artifact + 两份独立 tarball，mtime 不保证同秒"}, "repro": "docs/gaps/repro/G27-cache-key-folds-binary-mtime.sh", "today": "0.63.0 实测：同一份二进制内容的两个副本，只有 mtime 不同（2001 vs 2023），同一个缓存目录跑 `build` —— 第二次 `0 hit, 1 compiled`（miss）。仓库里 staged 的那对恰好同秒，是「同一次 cargo build + 同一次 stage」的巧合，不是设计保证", "expected_lean": "缓存键只含（编译器版本、构建形态、prelude 模式、闭包摘要），与可执行文件的 mtime 无关 ⇒ `sokonanoda build` 预热的条目 LSP 一定能命中", "workaround": "无（只能靠两个二进制恰好同秒）", "blocks": ["CLI 预热对编辑器的价值"], "notes": "是 G-25 的第二层：G-25 修好（LSP 读缓存）之后，这一层才会暴露出来。计划 T-A02 修它，`CACHE_FORMAT` 要 bump", "fixed_in": "0.63.3"}
{"id": "G-28", "title": "`→` 右边直接跟 `∀` / `forall` 不解析（Lean 4 里合法）", "kind": "language", "severity": "painful", "status": "fixed", "found": "2026-09-21", "found_by": "agent", "where": {"area": "parser 的箭头右侧表达式入口（`parse_arrow` 的 RHS 不接受 `forall`/`∀` 作为表达式起点）", "note": "`A -> (forall (x : Prop), x -> x)` 正常、`A ↔ ∀ x, P` 也正常 ⇒ 只有 `→` 右侧的 `∀` 不认"}, "repro": "docs/gaps/repro/G28-arrow-forall.sokonanoda", "today": "0.63.0 实测：`theorem t (A : Prop) : A -> forall (x : Prop), x -> x := sorry` → parse 诊断 `unexpected-token`「expected an expression, found Forall」；加一对括号即通过。课程正文里 `A ⊆ B → ∀ a, a ∈ A → a ∈ B` 这种形状很常见（今天得手写括号绕开）", "expected_lean": "`A → ∀ x, P` 直接可写（与 Lean 4 一致）；括号形式继续可用", "workaround": "手写括号 `A → (∀ x, P)`", "blocks": ["课程写作自然度"], "notes": "硬规则 3（教学语法是真实 Lean 4 的子集）的违例。是写 G-26 复现夹具时顺带发现的", "fixed_in": "0.63.1"}
{"id": "G-29", "title": "**编辑**项目文件 ⇒ 从零重编整条 import 闭包（缓存只对「打开」有效）", "kind": "perf", "severity": "blocks", "status": "open", "found": "2026-09-21", "found_by": "user", "where": {"area": "`QueryDoc::project_compile` → `compile_project_with_overlay` → `compile_all_units`：没有任何跨调用的复用", "note": "依赖的源文本没变，但它们的**内核环境**在编译结束后就没了；要接着编入口就必须重新 elaborate 它们"}, "repro": "docs/gaps/repro/G29-edit-recompiles-whole-closure.sh", "today": "0.64.0 实测（unit08，4 个 import，真 LSP 进程，按 `version` 过滤取真 post-edit 诊断）：冷开 4970ms · 热开（缓存命中）8ms · **改一行 3039ms**。拆开看：4 个依赖模块 2.16s + 入口自身 2.7s（用一个「只 import 这 4 个依赖 + 一条 `theorem t : Prop := Prop`」的极简入口量的）。⇒ **缓存只让「打开」变快，一敲键盘就回到秒级**", "expected_lean": "改一行应当只重编**受影响的模块**（依赖没变就不该重编依赖），并尽量只重查改动的后缀", "workaround": "无。用户只能忍着；`build` 对此毫无帮助", "blocks": ["「编译很慢」这条反馈的**主要**部分（打开已经修好，编辑没有）", "教学回路：改一行等 3–36 秒，等于不能迭代"], "notes": "用户 2026-09-21 指出：「我每次修改一下文件，就要编译 6807ms 吗？那我要你 build 有什么用？……速度和性能是生命线！」。同时指出文档写错了框定——原 `docs/design/compile-cache.md` §4 把这条写成「命中缓存后第一次按键慢 1.56×，差的是进程预热」，避重就轻；已更正为 4.1–4.4 节。修法是线 K 的 K1（`EnvBuilder::with_env` / `snapshot`，计划 §5.6.1 的 T-K12 / T-K13），依赖 T-K01/K02 的护栏与 T-K03 的分阶段 profile"}
{"id": "G-30", "title": "期望类型不传播到嵌套应用的实参（`Iff.intro`/`And.intro` 的分支里写不出集合字面量）", "kind": "language", "severity": "painful", "status": "open", "found": "2026-09-21", "found_by": "agent", "where": {"area": "elaborator：外层已知期望类型（如 `({a} = {b}) ↔ a = b`）时，`Iff.intro` 的分支作为实参 elaborate，拿不到那个期望类型", "note": "于是分支里的 `Set.ext α {a} {b} (…)` 中集合字面量 `{a}`/`{b}` 无类型可推 ⇒ 内核报 `期望 Sort(0)，实际是 (Set.[] $2)`"}, "repro": "docs/gaps/repro/G30-expected-type-not-propagated-into-nested-args.sokonanoda", "today": "0.64.0 实测：`Iff.intro (fun …) (fun …)` 在已知期望类型的位置报 `类型不匹配：期望 Sort(0)，实际是 (Set.[] $2)`；把前导 Prop 实参显式写出来（`Iff.intro ({a} = {b}) (a = b) (fun …) (fun …)`）立刻通过。`And.intro` 同理", "expected_lean": "Lean 4 里期望类型会传播给构造子的（隐式）参数，`Iff.intro h1 h2` 在已知期望类型处照常工作", "workaround": "显式写前导 Prop 实参：`Iff.intro P Q h1 h2` / `And.intro P Q h1 h2`", "blocks": ["项风格（lambda）证明的书写——它在 `solutions/` 上是 8–25× 的性能差（docs/PERF.md）"], "notes": "把 `solutions/` 从 tactic 改成项风格（2026-09-21 用户拍板：性能差不该由解答承担）时撞到的第一堵墙。同一批改写还发现：`Iff.intro` 的两个分支必须是**函数**（`Iff.intro h k` 这种直接给证明项会被拒：`期望 Pi ( : $3), $3，实际是 $2`），与 Lean 4 的 `Iff.intro h k`（`h : a → b`）一致，不算缺口 子代理转 unit12 时复现并细化：前导 Prop 实参是**按位置**填的（`Iff.intro (A) (B) h1 h2`，同 `Iff.refl (C (g (f x)))`），报错文本是「期望 Pi ( : Nat.[]), Sort(0)，实际是 (Set.[] (Set.[] Nat.[]))」。"}
{"id": "G-31", "title": "判定每批重跑整份前缀（`by` 贵的真正原因；Lean 不是这样）", "kind": "perf", "severity": "blocks", "status": "open", "found": "2026-09-21", "found_by": "user", "where": {"area": "`crates/front/src/judge.rs::judge_pairs_uncached` 的最后一步", "note": "它把整份前缀的命令 + 合成的判定声明拼成一份新 FolFile，交给 check_document_with；后者走 run(&[SourceUnit::single(...)]) 从头跑完整流水线，零复用"}, "repro": "docs/gaps/repro/G29-edit-recompiles-whole-closure.sh", "today": "0.64.0 实测（SOKO_JUDGE_STATS=2，unit12-solution）：25 个 by 块 ⇒ 25 次 check_document_with；前缀从 13286 涨到 83187 字节，合计处理 1,064,669 字节 ≈ 文件本身的 13 倍；单次最贵 28.6 秒。判定占整个文件墙钟的 68%（81.9s / 120.4s）", "expected_lean": "Lean 4 里 by 只是一个普通命令，按顺序 elaborate 一次，tactic 逐步改 goal state，前面的声明不会被重新 elaborate ⇒ 25 个 by 块的文件与 25 个项风格证明同量级", "workaround": "把解答写成项风格（没有 by 就没有判定批处理），实测快 8 到 25 倍。这是绕开不是修复", "blocks": ["用 tactic 教学（课程核心卖点）在大文件上就是分钟级", "solutions 不得不放弃 tactic 写法"], "notes": "用户 2026-09-21 质疑：lean 的 by 风格有这么耗时吗？是不是我们的 by 的实现方案有问题呢？——质疑成立，代码证据在 judge_pairs_uncached 的最后一步。根因是前端没有「往已有环境里再 elaborate 一条声明」的入口，唯一入口 check_document_with(FolFile) 只能整份重跑。by-tactics.md 第 13 节把「复用环境」记成死路（EnvBuilder 字段私有、finish(self) 消费自身、NamePtr::decl_idx 绑定 builder），但那是在内核冻结时写的。用户指出：这违背产品的红线（热编译设计初衷）"}
{"id": "G-32", "title": "后缀记法不能当应用头（`Aᶜ x` 不解析；Lean 4 读作 `(Aᶜ) x`）", "kind": "language", "severity": "painful", "status": "open", "found": "2026-09-21", "found_by": "agent", "where": {"area": "parser：后缀记法节点后面直接跟一个项时不构成应用", "note": "中缀头没事（`(A ∪ B) x`、`(C' \\ f) y` 都好），只有**后缀**头不行"}, "repro": "docs/gaps/repro/G32-postfix-notation-cannot-head-application.sokonanoda", "today": "0.64.0 实测：`Aᶜ x` 与 `((A ∪ B)ᶜ x)` 报 parse 错（`expected ')' , found Ident(\"x\")`）；写 `¬ (A x)` 或 `Set.compl α A x` 才行", "expected_lean": "Lean 4 里 `Aᶜ x` 读作 `(Aᶜ) x`（postfix 记法有自己的优先级，后面可以继续应用）", "workaround": "写 `¬ (A x)`（与 `Set.compl` 定义等价）或点名叫法 `Set.compl α A x`", "blocks": ["项风格证明的书写（把 `by` 改成项时大量撞到）"], "notes": "把 unit12-solution 从 tactic 改写成项风格时发现的（子代理实测，P1）"}
{"id": "G-33", "title": "无类型 binder 只在期望类型**语法上是 Pi** 时可用（def-headed 位置报 elab-untyped-binder）", "kind": "language", "severity": "painful", "status": "open", "found": "2026-09-21", "found_by": "agent", "where": {"area": "elaborator：`fun h => …` 的 binder 类型要靠期望类型推，而期望类型是 def-headed（`And.intro` 的分支位、`Exists.elim` 的 `f` 位）时就推不出来", "note": "报 `elab-untyped-binder`：cannot infer the type of this binder: the declared type does not provide a matching position"}, "repro": "docs/gaps/repro/G33-untyped-binder-needs-syntactic-pi.sokonanoda", "today": "0.64.0 实测：`And.intro (fun _ => h) (fun _ => k)` 报 elab-untyped-binder；每个 binder 都写全类型（`fun (_ : P) => h`）才过", "expected_lean": "Lean 4 里 `And.intro (fun h => …) (fun k => …)` 照常工作（期望类型做 whnf 之后是 Pi 就够）", "workaround": "项风格里每个 `intro` 派生的 binder 都写全类型标注", "blocks": ["项风格证明的书写（每个 `intro` 都得改写带类型）"], "notes": "把 unit12-solution 改写成项风格时发现的（子代理实测，P3）。同一批还发现 P4（把 `have` 内联进实参位要整体加括号，否则平脊会把 `And.intro` 当裸常量）与 P5（`Exists.elim` 的显式谓词要按**源类型展开后**的形状给，不是目标的形状——错误信息里印的是没展开的源类型，容易误判成 delta 能力问题）"}
