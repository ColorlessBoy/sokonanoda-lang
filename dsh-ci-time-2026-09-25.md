# dsh 近两天"时间都去哪了"分析（2026-09-23 12:00 → 09-25 21:35）

数据来源：GitHub Actions run 列表（120 条，窗口内 79 条）+ dsh 两个主会话的原始日志（`~/.dsh/sessions/...`，按工具调用实际耗时归因）。

## 一句话结论

**你的感觉方向对，但"80%"要修正**：CI 等待占**墙钟** 18%（今天）/ 47%（昨天），但占**真正执行命令的时间**是 **45% / 60%**；更扎眼的是**新提交里 40% 是 CI 相关返工**。而且 **70% 的 CI 失败是两个假红在刷**——真凶只有一个检查步骤。

## 一、GitHub 侧（近两天）

| 指标 | 数值 |
|---|---|
| run 总数 | **79**（ci 71 / release 7 / pages 1） |
| 失败 / 成功 | **55 失败（70%）** / 21 成功 / 2 进行中 / 1 取消 |
| 单次时长 | 中位 **31.2min** · p90 36.7min · 最长 81.4min |
| 累计 CI 墙钟 | **35.1 小时**（两天！） |
| 同一 commit 跑多次 | 8 个（最多 3 次） |

**job 级拆解**（典型一次 run）：

| job | 耗时 | 备注 |
|---|---|---|
| `lint` | **20 秒** | 很快 |
| `e2e`（3 条） | **~2.5 分钟** | 很快，且今天已修好不 flake ✓ |
| `test` | **19–37 分钟** | 长杆 |
| `gates` | **18–22 分钟** | 长杆 |
| `e2e ledger` | 十几秒 | 但会红 |

⇒ **"慢"不是 GitHub 慢，是两个长 job 本身就要 20–37 分钟**；一条 commit 从推上去到知道结果 ≈ **30 分钟**。

## 二、失败的东西是什么（近 12 次失败抽样）

| 失败步骤 | 次数 |
|---|---|
| **`Gap ledger is consistent (docs/gaps)`** | **9 / 12**（在 `test` 和 `gates` 里都会跑 ⇒ 一红两个 job 一起红） |
| e2e ubuntu（`Real VS Code integration tests`） | 4（今天已修 ✓） |
| `Workspace tests` / `e2e ledger commit` | 各 1 |

**关键发现：本地跑同一个检查是通过的**

```
$ python3 scripts/gap.py check --shard 1/3
全部与台账一致。        （exit 0）
```

而 CI 上同代码必红。`docs/CI-FAILURES.md:1063-1075` 里 dsh 自己已经写过结论：
**复现件在慢 runner 上超时 ⇒ `gap.py` 把"任何非零退出"都判成"行为已变" ⇒ 假红**；并写了修法（给 gap 步加超时预算，或把 >120s 的复现件降级为"跳过并标注"）——**但没实施**。

⇒ 这是**环境差异导致的假红**，不是产品回归。它一个人就贡献了失败率的大头，每次还要赔 20–37 分钟等结论。

## 三、dsh 自己的时间账（按工具调用实际耗时归因）

| | 09-24 23:00 → 09-25 21:34（22.6h） | 09-23 20:01 → 09-24 22:59（27.0h） |
|---|---|---|
| 墙钟 | 22.6h | 27.0h |
| 工具执行累计 | 9.0h（40%） | 21.0h（78%） |
| **CI 等待** | **4.0h = 墙钟 18% / 工具时间 45%** | **12.7h = 墙钟 47% / 工具时间 60%** |
| └ 其中 sleep 轮询 | 3.87h（64 次） | 11.99h（153 次） |
| 脚本/改造代码 | 1.99h（22%） | 4.71h |
| 本地编译测试（cargo） | 0.89h | 1.60h |
| 本地 e2e/gate | 0.17h | 0.81h |

- 等待方式非常"贵"：`sleep 600` / `sleep 660` 和 `for i in $(seq 1 40); do …` 轮询，**单次就是 10 分钟**，且是**纯阻塞**。
- 近两天共 **334 个提交**，其中 **135 个（40%）** 的提交信息与 CI/flake/e2e/台账相关。

## 四、建议（按性价比排序）

1. **先修那个假红**（一条改动，砍掉大部分失败）：`gap.py` 区分"行为变了"与"环境慢"——给 gap 步加超时预算，或把 >120s 的复现件降为"跳过 + 标注"。`docs/CI-FAILURES.md` 已有方案，落它即可。
2. **把长杆 job 本地化 / 分片**：`test`(19–37min) 与 `gates`(18–22min) 是全部时长来源；本地已能跑 `scripts/soko gate`，应做到**推之前本地全绿**，CI 只兜底。
3. **改掉"等 CI"的姿势**：推完不要 `sleep 600` 干等——要么异步继续干下一件事、回头收结论，要么用更短的轮询间隔（拿到结论就退出）。
4. **减少推送次数**：批量提交 + 本地 gate 先行；注意 `auto-tag` 只在 **push 事件**跑（rerun 永远出不了 tag），重跑等于白等。
5. e2e 那条线已经修好（判据从"数事件"改成"内容 + 幂等"）——这是这两天最成功的一笔，保持。
