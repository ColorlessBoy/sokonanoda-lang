-- ==========================================================================
-- unit12-synthesis.sokonanoda —— 卷 I 单元⑫ 综合与读证明（期末单元）
--
-- 靶子：Tao《Analysis I》§3 全书（§3.1 集合 · §3.3 关系与函数 · §3.4 像与原像 ·
--       §3.6 等势与 Cantor——基数那一环由 12.3 的小项目补上）。
-- 先修：单元①–⑪（本单元是它们的**综合**，不引入任何新概念）。
--
-- 概念顺序（本文件的编排）：
--   12.0 演示（已写好，3 条）：关系复合结合律 · 单射假设的展开读法 · 复合算在点上
--   12.1 形式↔散文互译（X 类 4 题）：散文写在题面注释里，形式版判卷通过
--   12.2 给错证明找错 · 法证式评阅（R 类 3 题，其中第 7 题承担 D 类必破）
--   12.3 期末小项目（P 类 2 题）：串起「集合 → 关系 → 函数 → 基数」的一条链
--        （7 环：环 1–5 见 12.0–12.2，环 6 是等势的复合，环 7 是基数收口题）
--
-- 练习配额：**X 4 · R 3 · P 2 = 9 题**（X = 题 1/2/3/4；R = 题 5/6/7；P = 题 8/9）。
--
-- **本单元只允许用**：
--   · 证明：一律 `by` tactic 块，白名单**只有**
--       intro / exact / apply / assumption / rfl / match / constructor /
--       left / right / use / exfalso / cases / have / sorry
--     ——**没有** rw / obtain / change / simp / induction / calc / rintro / `·`；
--     便利：匿名构造子 `⟨a, b⟩`（用哪个构造子由**期望类型**定：`∧` / `↔` / `∃` 都行）；
--   · 语句一律写**记法**：`x ∈ A` · `A ⊆ B` · `A ∪ B` · `A ∩ B` · `A \ B` ·
--     `∅` · `𝒫 A` · `Aᶜ` · `f '' A` · `f ⁻¹' B`；逻辑连接符 `∧ ∨ ↔ ¬` 与
--     `→ = ≠` 是**语言内建**，任何文件零声明可用；`∃ (x : α), …` 随
--     `import lib.Exists` 生效（本单元 import 了它）；
--   · 记法**由 lib 随 import 带来**（`infix` / `notation` / `binder_notation`
--     写在 lib/Set.sokonanoda 与 lib/Exists.sokonanoda 里）：本文件一条记法
--     声明都不写——同一个符号全课程只能声明一次；
--   · 记法只是**连接符的糖**，不是引理名的糖：引理照旧点名写——
--       Or.inl / Or.inr / Or.elim / And.intro / And.left / And.right /
--       Iff.intro / Iff.mp / Iff.mpr / Set.ext / Rel.ext / Exists.intro / Exists.elim；
--   · 库里已备好、直接用（lib.Logic + lib.Set + lib.Exists + lib.Equiv + lib.Rel +
--     lib.Fun + lib.Image）：
--       True / True.intro / False / False.elim / Not / absurd /
--       And / And.intro / And.left / And.right / And.elim /
--       Or（构造子是**点号名** Or.inl / Or.inr）/ Or.elim /
--       Iff / Iff.intro / Iff.mp / Iff.mpr / Iff.refl / Iff.symm / Iff.trans /
--       Eq.symm / Eq.trans / congrArg / Eq.subst（**类型位**的等式写记法 `a = b`；证明项 `Eq.subst.{1}` 仍显式宇宙）/
--       `Set` 一族（`Set.mem` / `Set.subset` / `Set.empty` / `Set.univ` / `Set.singleton` /
--       `Set.pair` / `Set.union` / `Set.inter` / `Set.sdiff` / `Set.compl` / `Set.powerset` /
--       `Set.image` / `Set.preimage`）+ 公理 `Set.ext` + 展开引理 Set.subset_def ·
--       Set.mem_empty_iff_false · Set.notMem_empty · Set.mem_univ · Set.mem_singleton_iff ·
--       Set.mem_singleton_self · Set.mem_union · Set.mem_inter_iff · Set.mem_sdiff ·
--       Set.mem_powerset_iff · Set.mem_image · Set.mem_preimage · Set.image_subset_iff ·
--       Set.image_mono；
--       Exists / Exists.intro / Exists.elim / Exists.imp；
--       Set.MapsTo / Set.LeftInvOn / Set.RightInvOn · 等势 Set.Equiv · 构造子 Set.Equiv.mk
--       （读法见 lib/Equiv.sokonanoda 文件头：**拿**数据靠 Exists.elim 两次，**造**数据靠 mk）；
--       Rel / Rel.inv / Rel.comp / **公理 Rel.ext** / Rel.inv_apply · Rel.comp_apply ·
--       Rel.inv_inv_apply；
--       Function.comp / Injective / Surjective / Bijective / LeftInverse /
--       RightInverse / Inverse 及它们的定义展开引理；
--   · 本单元**不 import 别的单元**（units/ 之间互相独立），也**不新建概念**（def /
--     inductive / 公理一律不加；12.3 的环 6 是等势复合这一条**已知事实**，不是新概念）：
--     反例一律落在 `Set Nat` 上——∅ 与 univ 现成可分辨（Set.mem_univ 把 0 送进 univ，
--     Set.notMem_empty 挡住 ∅），既不用新增归纳类型，也不需要 Decidable。
--   · 语言里**不存在**的（别去找）：funext、propext、Classical.choice、Decidable、
--     obtain；`let` 存在（term 位置 `let x : T := v; body`，能过内核），
--     但本单元的子集不含它——卷 I 一律不用。四条后果：
--       (a) 两个**函数**相等只能靠"两边展开后是同一个 λ"（定义相等），不许逐点证；
--       (b) 两个**关系**相等用公理 Rel.ext 逐点证 Iff，两个**集合**相等用 Set.ext 逐点证 Iff；
--       (c) 证明项里不能像 Lean 的 `obtain` 那样"拆开再命名"——`cases` 降低成 `match`，
--           需要一个**已知的结果类型**，所以它写在 `have h : T := by cases …` 的 `by` 块里
--           （`have` 的类型标注定住它），**不能**直接写在 `constructor` / `apply` 拆出来的
--           子目标上（12.2 的长证明项靠这一条读）；
--       (d) 记法 `''` / `⁻¹'` 的前导类型参数**只按操作数的类型**补：操作数写成 λ 时补不出来
--           （题 7 的常函数因此只出现在点名 `Set.image` 的实参位上），语言也没有类型标注
--           `(e : T)` 可以救它。
--
-- ── 两条"看起来对"的错证明（12.2 要评的对象；名字都是本课程真有的）──────────────
--
-- 【错证明 A】命题：对任意 f : α → β 与 C : Set β，有
--                  f '' (f ⁻¹' C) = C 。
--   证明（**错**）：
--     "→"：设 y ∈ f '' (f ⁻¹' C)。按 Set.mem_image 展开，存在 x 使 x ∈ f ⁻¹' C
--          且 f x = y；按 Set.mem_preimage，x ∈ f ⁻¹' C 就是 f x ∈ C。
--          沿 f x = y 用 Eq.subst 搬过去，得 y ∈ C。✓
--     "←"：设 y ∈ C。**取 x 使 f x = y**（"y 的原像"），则 f x ∈ C，即 x ∈ f ⁻¹' C，
--          于是 y = f x ∈ f '' (f ⁻¹' C)。✓
--     两边互相包含，故相等。∎
--
-- 【错证明 B】命题：对任意 f : α → β 与 A B : Set α，有
--                  f '' (A ∩ B) = f '' A ∩ f '' B 。
--   证明（**错**）：
--     "→"：设 y ∈ f '' (A ∩ B)。存在 x ∈ A ∩ B 使 f x = y；由 Set.mem_inter_iff，
--          x ∈ A 且 x ∈ B，于是 y 同时落在 f '' A 与 f '' B 里。✓
--     "←"：设 y ∈ f '' A ∩ f '' B。存在 x1 ∈ A 与 x2 ∈ B 使 f x1 = y、f x2 = y。
--          由 f x1 = y = f x2 **得 x1 = x2**，这个点既在 A 里又在 B 里，
--          所以 y ∈ f '' (A ∩ B)。✓
--     两边互相包含，故相等。∎
--
-- 评阅任务（12.2）：两条都**假**。错在"哪一条推理"上、反例长什么样，由下面三道题
-- （练习 5、6、7）分别负责：改正版 + 证伪。
-- ==========================================================================
--
-- ── 已知边界（本轮实测，**环 6 因此从画布移除**）──────────────────────────────
-- 「等势的复合仍是等势」这条本身成立，但它的证明要**把两把复合映射当 lambda
-- 证人交出去**，而判卷器今天在这一形状上撞两条边界：① 内核不做 beta 转换
-- （`(fun (c : γ) => g (g2 c)) ((fun (a : α) => f2 (f a)) a)` 与
-- `g (g2 (f2 (f a)))` 在它眼里不是同一个类型）；② `cases` 拆 `Set.Equiv`
-- 之后臂里假设的变量编号会与目标对不上（内核原文 `expected="$28"
-- actual="$27"`）。前端已经把代入后的 redex 消掉（`spine::beta_normalize`），
-- ① 那一半已修；② 仍是开口，记录在 `docs/design/course-lean-style.md` §9。
-- 结论：环 6 的**事实**留在散文里，证明不进画布；练习 9 的两半不受影响。
import lib.Logic
import lib.Set
import lib.Exists
import lib.Equiv
import lib.Rel
import lib.Fun
import lib.Image

-- ─────────────────────────────────────────────────────────────
-- 12.0 演示（已写好）：三条读法，后面 9 道题反复用
-- ─────────────────────────────────────────────────────────────

-- 演示 1（已写好）：**关系复合结合律**——期末小项目那条链的第一环。
-- 它是"两个方向的 ∃ 拆装"的样板：关系等式先用公理 Rel.ext 换成"逐点的 Iff"，
-- 每个方向再用 Exists.elim 拆证人（Q 取目标那个 ∃）、Exists.intro 装回去。
-- 注意两边**不是**定义相等：把 ∃ 从"先 c 后 b"换成"先 b 后 c"，是要证的数学内容。
-- 写法：`cases` 需要一个已知的结果类型，所以它写在 `have h1 : … := by cases h with …`
-- 里（`have` 的类型标注定住它）；拆出来的两条证据用 And.left / And.right 取用。
theorem demo_rel_comp_assoc (A B C D : Type) (r : Rel A B) (s : Rel B C) (t : Rel C D) :
    Rel.comp A C D (Rel.comp A B C r s) t = Rel.comp A B D r (Rel.comp B C D s t) := by
  apply Rel.ext A D
  intro a
  intro d
  constructor
  intro h
  have h1 : ∃ (b : B), r a b ∧ (∃ (c : C), s b c ∧ t c d) := by
    apply Exists.elim C (fun (c : C) => (Rel.comp A B C r s a c) ∧ (t c d))
      (∃ (b : B), r a b ∧ (∃ (c2 : C), s b c2 ∧ t c2 d)) h
    intro c hc
    have hb : ∃ (b : B), r a b ∧ s b c :=
      And.left hc
    apply Exists.elim B (fun (b : B) => r a b ∧ s b c)
      (∃ (b : B), r a b ∧ (∃ (c2 : C), s b c2 ∧ t c2 d)) hb
    intro b hb2
    exact Exists.intro b (And.intro (And.left hb2) (Exists.intro c (And.intro (And.right hb2) (And.right hc))))
  exact h1
  intro h
  have h2 : ∃ (c : C), (∃ (b : B), r a b ∧ s b c) ∧ t c d := by
    apply Exists.elim B (fun (b : B) => r a b ∧ (∃ (c : C), s b c ∧ t c d))
      (∃ (c : C), (∃ (b2 : B), r a b2 ∧ s b2 c) ∧ t c d) h
    intro b hb
    have hc : ∃ (c : C), s b c ∧ t c d :=
      And.right hb
    apply Exists.elim C (fun (c : C) => s b c ∧ t c d)
      (∃ (c : C), (∃ (b2 : B), r a b2 ∧ s b2 c) ∧ t c d) hc
    intro c hc2
    exact Exists.intro c
      (And.intro (∃ (b2 : B), r a b2 ∧ s b2 c) (t c d)
        (Exists.intro b
          (And.intro (r a b) (s b c)
            (And.left hb)
            (And.left hc2)))
        (And.right hc2))
  exact h2

-- 演示 2（已写好）：**单射假设怎么"读"**——`Function.Injective α β f` 展开后就是
-- `∀ x y, f x = f y → x = y`，所以它本身就是"吃两个点加一条等式"的函数。
-- 后面凡是手里有单射、又要用它降一层的地方（练习 3、练习 6）都是这个读法。
theorem demo_injective_unpack (α β : Type) (f : α → β)
    (hf : Function.Injective α β f) :
    ∀ (x : α), ∀ (y : α), f x = f y → x = y := by
  intro x
  intro y
  intro h
  exact hf x y h

-- 演示 3（已写好）：**复合算在点上**——`Function.comp α β γ g f x` 就是 `g (f x)`
-- （库里的 Function.comp_apply 是 rfl 级）。所以"复合相等"与"g (f x) = g (f y)"
-- 定义上就是同一句话，用的时候不必先改写（练习 3 就靠这一条省掉一次搬运）。
theorem demo_fun_comp_apply (α β γ : Type) (f : α → β) (g : β → γ) (x : α) :
    Function.comp α β γ g f x = g (f x) := by
  exact Function.comp_apply α β γ g f x

-- ─────────────────────────────────────────────────────────────
-- 12.1 形式↔散文互译（X 类 4 题）
--
-- 怎么答：题面给一句中文散文；签名里的形式版已经替你写好（不许改），
-- 你要做的是"读懂它"，然后把值位置上的 sorry 换成真的证明项。
-- 读法提示：散文里的"并且/或者/不是"对应 ∧/∨/¬，"存在"对应 ∃，
-- "相等"要看成两个方向（Iff / Set.ext / Rel.ext）。
-- ─────────────────────────────────────────────────────────────

-- 练习 1（X 类：形式↔散文互译）。散文：
--
--   「x 既不在 A 里、也不在 B 里」与「x 不在 A ∪ B 里」是同一句话。
--
-- （这就是 De Morgan 律在集合上的那一半；形式版已经在签名里。）
-- soko:hint 思路：集合等式先走外延，再在同一个 x 上做命题逻辑——两个方向各是一次"否定与或"的拆装。
-- soko:hint 目标形态：(A ∪ B)ᶜ = Aᶜ ∩ Bᶜ；apply Set.ext 之后逐元素是 x ∈ (A ∪ B)ᶜ ↔ x ∈ Aᶜ ∩ Bᶜ（展开即 ¬ (x ∈ A ∨ x ∈ B) ↔ (¬ x ∈ A ∧ ¬ x ∈ B)）。
-- soko:hint 关键件：触发条件是"目标已经是逐元素 Iff"（Set.ext 只吃这个）；→ 方向把 Or.inl / Or.inr 分别喂给那个否定，← 方向对 x ∈ A ∨ x ∈ B 用 Or.elim 拆两支（目标取 False），两支分别交给 And.left / And.right。
theorem xlat_compl_union (α : Type) (A B : Set α) :
    (A ∪ B)ᶜ = Aᶜ ∩ Bᶜ := by
  sorry

-- 练习 2（X 类：形式↔散文互译）。散文：
--
--   「关系先复合、再取逆，等于把两个关系各自取逆、再按**相反的顺序**复合。」
--
-- （读法：Rel.comp A B C r s 里 r : Rel A B 先走、s : Rel B C 后走；Rel.inv 把两个
--   参数对调。所以形式版右边先出现 s 的逆、后出现 r 的逆——顺序正是"相反"。）
-- soko:hint 思路：两个关系相等只能用公理 Rel.ext 逐点证 Iff；把"逆"与"复合"都展开成 ∃ 之后，剩下的只是 ∧ 的两支换序。
-- soko:hint 目标形态：Rel.inv A C (Rel.comp A B C r s) = Rel.comp C B A (Rel.inv B C s) (Rel.inv A B r)；逐点是 (∃ (b : B), r a b ∧ s b c) ↔ (∃ (b : B), s b c ∧ r a b)——参数顺序是 c a，与 Rel.inv 对调有关。
-- soko:hint 关键件：触发条件是"要证的是两个 Rel 相等"⇒ Rel.ext（它吃"每个 c a 的 Iff"）；每个方向一次 Exists.elim 拆证人（Q 取目标那个 ∃），再用 And.left / And.right 换序、Exists.intro 装回；展开可用 Rel.comp_apply / Rel.inv_apply，也可以直接让定义展开。
theorem xlat_rel_inv_comp (A B C : Type) (r : Rel A B) (s : Rel B C) :
    Rel.inv A C (Rel.comp A B C r s) = Rel.comp C B A (Rel.inv B C s) (Rel.inv A B r) := by
  sorry

-- 练习 3（X 类：形式↔散文互译）。散文：
--
--   「单射复合单射，还是单射。」
--
-- （先作用的 f : α → β，后作用的 g : β → γ；复合写作 Function.comp α β γ g f。）
-- soko:hint 思路：单射是全称命题：先 intro 两个点，再 intro"它们在复合下相等"这条假设，然后从外到内用掉两个单射。
-- soko:hint 目标形态：Function.Injective α γ (Function.comp α β γ g f)；intro 之后手里是 g (f x) = g (f y)（Function.comp 展开后的样子），目标是 x = y。
-- soko:hint 关键件：触发条件是假设形如"复合相等"；`hg (f x) (f y) …` 把它降到 f x = f y，再交给 `hf x y …`——复合相等与 g (f x) = g (f y) 定义上相等，不必先搬运（见演示 2、演示 3）。
theorem xlat_injective_comp (α β γ : Type) (f : α → β) (g : β → γ)
    (hf : Function.Injective α β f) (hg : Function.Injective β γ g) :
    Function.Injective α γ (Function.comp α β γ g f) := by
  sorry

-- 练习 4（X 类：形式↔散文互译）。散文：
--
--   「复合函数的原像，就是先把外层拉回来、再把内层拉回来。」
--
-- （f ⁻¹' B 是回拉：x ∈ f ⁻¹' B 当且仅当 f x ∈ B。）
-- soko:hint 思路：两边展开后逐点是同一个命题，不需要真的"推理"——外延加 Iff.refl 就够。
-- soko:hint 目标形态：f ⁻¹' (g ⁻¹' C) = (Function.comp α β γ g f) ⁻¹' C；逐元素时两边都展开成 C (g (f x))。
-- soko:hint 关键件：触发条件是"两边定义上相等"（`Set.preimage` 与 `Function.comp` 都是 λ）；apply Set.ext 之后逐元素交 Iff.refl 就行了（这是本单元唯一不用拆 ∃ 的题）。
theorem xlat_preimage_comp (α β γ : Type) (f : α → β) (g : β → γ) (C : Set γ) :
    f ⁻¹' (g ⁻¹' C) = (Function.comp α β γ g f) ⁻¹' C := by
  sorry

-- ─────────────────────────────────────────────────────────────
-- 12.2 给错证明找错 · 法证式评阅（R 类 3 题）
--
-- 评阅的规矩（本单元的训练目标）：
--   1. 先判**真假**（假命题再漂亮的推理也是白搭）；
--   2. 再定位**哪一条推理**坏了——要能指出"这一步用了一个没有的前提"；
--   3. 最后给**改正版**（补上前提/换掉结论）或**反例**（把它形式化，判卷通过）。
-- 上头【错证明 A】【错证明 B】就是评阅对象；练习 5、6 写改正版，练习 7 写证伪。
-- ─────────────────────────────────────────────────────────────

-- 练习 5（R 类：读·评译 + 改正）。【错证明 A】的"←"方向靠的是
--   "y ∈ C ⇒ 取 x 使 f x = y"——这一步需要 `C` 的每个点都在 f 的像里。
-- 改正的做法就是**把这个前提摆到台面上**（签名里的 hC），再走两个方向。
-- soko:hint 思路：先判 A 的错在哪：假的是"←"（反向）——它偷用了"C 里每个点都有原像"；把它写成参数 hC 之后，→ 方向照旧、← 方向由 hC 供料。
-- soko:hint 目标形态：f '' (f ⁻¹' C) = C，前提 hC : C ⊆ f '' Set.univ α；逐元素是 y ∈ f '' (f ⁻¹' C) ↔ y ∈ C。
-- soko:hint 关键件：→ 方向用 Exists.elim 拆出 x 与 (x ∈ f ⁻¹' C) ∧ (f x = y)，再用 Eq.subst 沿 f x = y 把 x ∈ f ⁻¹' C 搬到 y 上（注意搬的方向）；← 方向 hC y hy 本身就是目标那个 ∃（univ 那一支永远成立），装回像里时 **f x = y 与 y ∈ C 不能直接互换**，要用 Eq.subst（这一步正是错证明里被跳过的地方）。
theorem fix_image_preimage (α β : Type) (f : α → β) (C : Set β)
    (hC : C ⊆ f '' Set.univ α) :
    f '' (f ⁻¹' C) = C := by
  sorry

-- 练习 6（R 类：读·评译 + 改正）。【错证明 B】的"←"方向靠的是
--   "f x1 = y 且 f x2 = y ⇒ x1 = x2"——这一步正是**单射**。
-- 改正的做法：把缺的前提（签名里的 hf）补上，等号才是真的。
-- soko:hint 思路：先指出坏掉的那一步是"f x1 = f x2 ⟹ x1 = x2"（那需要单射）；补上 hf 后，→ 方向照旧，← 方向用它把两个不同的原像捏成同一个。
-- soko:hint 目标形态：f '' (A ∩ B) = f '' A ∩ f '' B，前提 hf : Function.Injective α β f；逐元素是 y ∈ f '' (A ∩ B) ↔ (y ∈ f '' A ∧ y ∈ f '' B)。
-- soko:hint 关键件：→ 方向把 x ∈ A ∩ B 用 And.left / And.right 拆给两个 Exists.intro；← 方向两次 Exists.elim（x1 ∈ A 与 x2 ∈ B 各一次，Q 都取目标那个 ∃），先由 f x1 = y、f x2 = y 经 Eq.trans / Eq.symm 凑出 f x1 = f x2 交给 hf，再用 Eq.subst 沿 x2 = x1 把 B x2 搬到 B x1，最后 And.intro 现装 x1 ∈ A ∩ B。
theorem fix_image_inter (α β : Type) (f : α → β) (A B : Set α)
    (hf : Function.Injective α β f) :
    f '' (A ∩ B) = f '' A ∩ f '' B := by
  sorry

-- 练习 7（D 类：必破——判断真假并给反例）。请对【错证明 A】【错证明 B】的结论各判一次，
-- 并把两个反例**形式化**成下面这条合取（签名不许改）：
--   · 第一条反例：f 取常函数 `fun (_ : Set Nat) => ∅`（把一切都送到 ∅），
--     C 取 `Set.univ (Set Nat)`；此时 `Set.univ Nat` 是 C 的点，却**没有原像**；
--   · 第二条反例：同一个 f，A 取 `{∅}`、
--     B 取 `{(Set.univ Nat)}`；此时 A ∩ B = ∅，两个像却都含 ∅。
-- 判词：两条等式**都假**（详情与"错在哪一步"写在上面 12.2 的评阅规矩里）。
-- 写法提醒：常函数写成 λ，而记法 `''` / `⁻¹'` 补不出 λ 操作数的前导类型参数
--   （语言也没有类型标注 `(e : T)`），所以这一条的像/原像/交写**点名形式**。
-- soko:hint 思路：每条都是 intro 一个等式假设、再从"某个元素被搬过去"造出 False：第一条用 Set.univ Nat（它不在像里），第二条用 ∅（两个像的共同点）。
-- soko:hint 目标形态：两个 ≠ 的合取；每个 ≠ 展开后目标都是 False，手里是一条把两个集合划等号的假设。
-- soko:hint 关键件：触发条件是"等式两边只差一个具体元素"⇒ 先备好成员证据（第一条 Set.mem_univ (Set Nat) (Set.univ Nat)，第二条两个 Exists.intro + Eq.refl 打进 ∩），再沿 Eq.symm 过的等式用 Eq.subst 搬成员关系，最后 Exists.elim 拆出"∅ 与 univ 撞上"：第一条用 Set.notMem_empty Nat 0 与 Set.mem_univ Nat 0，第二条由 x = ∅、x = univ 经 Eq.trans / Eq.symm 合成 ∅ = univ 再照第一条收尾。
theorem flawed_equalities_refuted :
    Set.image (Set Nat) (Set Nat) (fun (_ : Set Nat) => Set.empty Nat)  -- soko:notation-ok: R5：λ 操作数补不出前导类型参数（无类型标注）
        (Set.preimage (Set Nat) (Set Nat) (fun (_ : Set Nat) => Set.empty Nat)
          (Set.univ (Set Nat)))
      ≠ Set.univ (Set Nat)
    ∧ Set.image (Set Nat) (Set Nat) (fun (_ : Set Nat) => Set.empty Nat)  -- soko:notation-ok: R5：同上
        (Set.inter (Set Nat) (Set.singleton (Set Nat) ∅)
          (Set.singleton (Set Nat) (Set.univ Nat)))  -- soko:notation-ok: R5：λ 操作数补不出前导类型参数（无类型标注）
      ≠ Set.inter (Set Nat)
          (Set.image (Set Nat) (Set Nat) (fun (_ : Set Nat) => Set.empty Nat)  -- soko:notation-ok: R5：同上
            (Set.singleton (Set Nat) ∅))
          (Set.image (Set Nat) (Set Nat) (fun (_ : Set Nat) => Set.empty Nat)  -- soko:notation-ok: R5：同上
            (Set.singleton (Set Nat) (Set.univ Nat))) := by
  sorry
theorem project_chain (α β γ : Type) (f : α → β) (g : β → γ) (A : Set α) :
    Rel.comp α β γ (fun (a : α) => fun (b : β) => f a = b)
        (fun (b : β) => fun (c : γ) => g b = c)
      = (fun (a : α) => fun (c : γ) => g (f a) = c)
    ∧ (Function.comp α β γ g f) '' A = g '' (f '' A) := by
  sorry

-- 练习 9（P 类：期末小项目 · 基数环）。把单元⑨ 的等势与单元⑩ 的 Cantor 钉在一起，两半：
--   · ① **搬运**：`h : A ≈ C` 与 `hC : C = C'` 给出 `A ≈ C'`。
--     **这不是单元⑨ 练习 4 的重复**：那里两边同论域、来回都取恒等映射，要搬的只是成员关系；
--     这里 h 给的那两把映射**照用**（不必重新挑映射），只有"落在哪一侧"的域条件沿 hC 搬家，
--     两条互逆律原样交回——是那条练习的**跨论域 ＋ 真映射**版。
--   · ② **天花板**：`C'` 与它的幂集 𝒫C' 不等势（⑩ 的对角线论证）。
--     **这也不是单元⑩ 练习 4 的重复**：那里集合固定为 univ（对角线集直接写成
--     `fun (y : γ) => ¬ (f y y)`）；这里集合是**任意** C'，于是对角线集必须先把自己约束成
--     C' 的子集，才够格当 𝒫C' 的元素——差集 `C' \ (fun (y : γ) => f y y)` 正好是这个
--     形状；代价是反向映射的域条件**第一次真的用上**（它保证 `g D` 落在 C' 里，缺了它矛盾合不上）。
-- 两半合起来说的一句话：等势对复合与搬运封闭，但对幂集永远差一档。
-- soko:hint 思路：① 走"拆 h 拿两把映射、再 mk 重新打包"，只在成员关系上用 hC 搬三处；② 反证——假设有等势，取对角线集 D，用反向映射把它拉回一点，撞出"某个命题等于自己的否定"。
-- soko:hint 目标形态：Set.Equiv α γ A C' ∧ ¬ Set.Equiv γ (Set γ) C' (𝒫 C')；② 拆开后目标是 False，手里有 f : γ → Set γ、g : Set γ → γ 与四条条件，对角线集取 D := C' \ (fun (y : γ) => f y y)。
-- soko:hint 关键件：① 触发条件是"两个集合只差一条等式"⇒ 成员关系用 Eq.subst 搬（motive 取 fun (X : Set γ) => z ∈ X），反侧那一处先把 hC 用 Eq.symm 掉头；打包仍用 Set.Equiv.mk（两条互逆律不必动）。② D 属于 𝒫C' 的证据是一行 And.left（D y 展开就是 C' y ∧ ¬ (f y y)）；④号互逆律在 D 上给出 f (g D) = D，用 congrArg 把它作用到 g D 上，得到"(f (g D)) (g D) 与 D (g D) 相等"这条**命题等式**，再用 Eq.subst 两个方向互喂（D (g D) 展开后用 And.right 取否定那一半；反向映射的域条件给出 C' (g D)，正是 And.intro 缺的那一半）。
theorem project_chain_cardinal (α γ : Type) (A : Set α) (C C' : Set γ)
    (h : Set.Equiv α γ A C) (hC : C = C') :
    Set.Equiv α γ A C' ∧ ¬ Set.Equiv γ (Set γ) C' (𝒫 C') := by
  sorry
