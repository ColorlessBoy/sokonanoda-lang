-- ==========================================================================
-- unit07-functions.sokonanoda —— 卷 I 单元⑦ 函数
--
-- 靶子：Tao《Analysis I》§3.3（函数：定义、复合、单射/满射/双射、逆）。
--
-- ── 大纲条目（docs/design/set-theory-syllabus.md §3 第 7 行）────────────────
--   单元号：卷 I ⑦（依赖树 1 → 2 → … → 6 → 7）；先修：单元②（子集/空集——第 9 题
--   的反例要用 ∈ 的两条引理）与单元⑥（关系与复合——本单元只讲**函数层**的复合，
--   关系层的复合只在演示 1 里作为对照出现，不重复单元⑥ 的练习）。
--   练习配额：**T-5 · L-2 · D-2 = 9 题**（本文件：T = 题 1/2/3/4/6；L = 题 5/7；
--   D = 题 8/9）。类型记号见 units/README（T 项填空 · L 自证引理 · D 证明或证伪）。
--
-- ── 实证导入语（这一单元为什么存在）────────────────────────────────────────
-- Breidenbach, Dubinsky, Hawks & Nichols 1992 的实测（24 个情境、约 60 名本科生）：
-- **本该 ~100% 判为函数的场合**只有约 40% 的学生答"是函数"——分表征看，图那道
-- （第 19 题）40.7%、方程那道（第 25 题）31%、表格 39.9–48.6%。
-- 换句话说，"什么是一个函数"不是自明的：它与你手里的表示形式有关，而课程要做的
-- 正是把这两种表示摆在一起对照。
--
-- ── 本单元只允许用什么 ────────────────────────────────────────────────────
--   · 逻辑：lib.Logic —— True/False/False.elim、Eq.symm/Eq.trans/congrArg、
--           And/Or/Not/Iff/absurd；prelude 另带 Nat、Bool 与 Eq.refl/Eq.subst。
--   · 集合：lib.Set —— **只在第 9 题的反例里用**（∅ 与 univ、∈ 的两个引理）。
--   · 存在：lib.Exists —— Exists / Exists.intro / Exists.elim（满射与换序题要用）。
--   · 函数：lib.Fun —— Function.comp / Injective / Surjective / Bijective /
--           LeftInverse / RightInverse / Inverse，以及它们的定义展开引理。
--   · tactic：只有 by 块里的 intro/exact/apply/assumption/rfl/match/sorry；
--     但本单元 9 题**全部可以用 term 风格**（`fun (x : α) => …`）写完，推荐这样写。
--   · **不许用**（本语言里根本没有）：选择公理（Classical.choice）、funext、propext。
--     这三样一起决定了第 1 题与第 6 题的形状——请看下面两处说明。
--
-- ── 概念顺序（本文件的编排）──────────────────────────────────────────────
--   演示 1：函数 ⇒ 单值关系（"函数即关系"那条路的一小步，用于对照）
--   演示 2：语言原语视角——`α -> β` 是**类型**，复合就是"先 f 后 g"的应用
--   演示 3：双射 = 单射 ∧ 满射（定义展开）
--   题 1–3：复合（结合律、单射复合、满射复合）
--   题 4–7：单射/满射/双射 ↔ 逆（数据版）——左逆⇒单射、右逆⇒满射、双边逆⇔双射
--   题 8–9：D 类——量词换序的四个变体（哪个恒真、哪个有反例）
--
-- ── 两条"写死在注释里"的约定 ──────────────────────────────────────────────
-- (1) **复合的顺序**：`Function.comp α β γ g f` = g ∘ f，即**第一个函数参数最后作用**：
--     先 f : α → β，再 g : β → γ，结果 α → γ（Mathlib 同款）。所以题 2、3 的前提
--     写作 `Injective f`、`Injective g` 时，"f 在前"指的是**先作用**的那个。
-- (2) **没有 funext**：语言里证不了 `(∀ x, f x = g x) → f = g`。所以题 1 的等式
--     只能靠"两边展开后是同一个 λ"（定义上相等）来证，**不能**逐点证。
--
-- ── 第 6 题与选择公理（务必读）────────────────────────────────────────────
-- 若把"双射 ↔ 有逆"写成**存在版**：
--     Bijective f ↔ Exists (β -> α) (fun g => Inverse f g)
-- 那么 "→" 方向就是"每个满射都可以裂开"——**与选择公理等价**，本语言证不出来
-- （没有 Classical.choice，也没有 funext/propext）。所以第 6 题证的是**数据版**：
-- 逆函数 g 连同"它是右逆"的证据**由调用者交出来**，此时 Iff 两边都成立。
-- 这是一条 "能证多少证多少" 的边界，报告里记在 unprovable。
-- ==========================================================================
import lib.Logic
import lib.Set
import lib.Exists
import lib.Fun

-- ─────────────────────────────────────────────────────────────
-- 演示（已写好）：三条，分别对应"关系视角 / 原语视角 / 双射"
-- ─────────────────────────────────────────────────────────────

-- 演示 1（已写好）：函数 ⇒ 单值关系。
-- 把 f 读成关系 `fun (x : α) => fun (y : β) => f x = y`：同一个 x 不会对上两个 y
-- ——这正是"函数即单值关系"那条路的第一条性质（对照：语言原语那条路里，
-- 单值性是**类型的构造规则**，不需要证）。
theorem demo_graph_single_valued (α β : Type) (f : α -> β) :
    forall (x : α), forall (y : β), forall (z : β),
      Eq.{1} β (f x) y -> Eq.{1} β (f x) z -> Eq.{1} β y z :=
  fun (x : α) => fun (y : β) => fun (z : β) => fun (hxy : Eq.{1} β (f x) y) =>
    fun (hxz : Eq.{1} β (f x) z) =>
      Eq.trans.{1} β y (f x) z (Eq.symm.{1} β (f x) y hxy) hxz

-- 演示 2（已写好）：语言原语视角——复合算在参数上就是"先 f 后 g"（rfl 级）。
theorem demo_comp_apply (α β γ : Type) (g : β -> γ) (f : α -> β) (x : α) :
    Eq.{1} γ (Function.comp α β γ g f x) (g (f x)) :=
  Function.comp_apply α β γ g f x

-- 演示 3（已写好）：双射 = 单射 ∧ 满射（定义展开，两个方向都是恒等函数）。
theorem demo_bijective_def (α β : Type) (f : α -> β) :
    Iff (Function.Bijective α β f)
      (And (Function.Injective α β f) (Function.Surjective α β f)) :=
  Function.bijective_def α β f

-- ─────────────────────────────────────────────────────────────
-- 题 1–3：复合（T 类）
-- ─────────────────────────────────────────────────────────────

-- 练习 1（T 类）：复合**结合律**——先说清参数顺序：下面写的是
--     h ∘ (g ∘ f) = (h ∘ g) ∘ f，其中 f : α → β、g : β → γ、h : γ → δ，
--     而 `Function.comp α β γ g f` 读作 g ∘ f（第一个函数参数最后作用，见文件头 (1)）。
-- soko:hint 思路：两边都是 `α -> δ` 的**函数**；没有 funext，别去逐点证——回到 Function.comp 的定义，看两边是不是同一个 λ。
-- soko:hint 目标形态：Eq.{1} (α -> δ) (Function.comp α γ δ h (Function.comp α β γ g f)) (Function.comp α β δ (Function.comp β γ δ h g) f)。
-- soko:hint 关键件：触发条件是"两边定义上相等"（delta 展开后 λ 体都是 h (g (f x))），此时用 Eq.refl.{1} (α -> δ) 即可——这是本单元唯一不需要 intro 的题。
theorem comp_assoc (α β γ δ : Type) (f : α -> β) (g : β -> γ) (h : γ -> δ) :
    Eq.{1} (α -> δ) (Function.comp α γ δ h (Function.comp α β γ g f))
      (Function.comp α β δ (Function.comp β γ δ h g) f) :=
  sorry

-- 练习 2（T 类）：单射在复合下封闭。
-- soko:hint 思路：单射的定义是全称命题：intro 两个点 x y，再 intro"它们在复合下相等"这条假设。
-- soko:hint 目标形态：intro 之后手里是 `g (f x) = g (f y)`（Function.comp 展开后的样子），目标是 `x = y`。
-- soko:hint 关键件：先用 g 的单射把假设降到 `f x = f y`（hg (f x) (f y) …），再交给 f 的单射收尾。
theorem injective_comp (α β γ : Type) (f : α -> β) (g : β -> γ)
    (hf : Function.Injective α β f) (hg : Function.Injective β γ g) :
    Function.Injective α γ (Function.comp α β γ g f) :=
  sorry

-- 练习 3（T 类）：满射在复合下封闭。原像要**穿过两层**，这是本题唯一的新东西。
-- soko:hint 思路：满射是"给每个 z 找一个原像"：先用 g 的满射把 z 拉回 y，再用 f 的满射把 y 拉回 x。
-- soko:hint 目标形态：intro z 之后目标是 Exists α (fun (x : α) => Eq.{1} γ (Function.comp α β γ g f x) z)。
-- soko:hint 关键件：`hg z` 与 `hf y` 都是 Exists，各用一次 Exists.elim 拆证人（Q 都取目标那个 Exists）；最后由 Exists.intro 把内层证人交出去，等式用 congrArg 把 hx 抬过 g，再与 hy 经 Eq.trans 接起来。
theorem surjective_comp (α β γ : Type) (f : α -> β) (g : β -> γ)
    (hf : Function.Surjective α β f) (hg : Function.Surjective β γ g) :
    Function.Surjective α γ (Function.comp α β γ g f) :=
  sorry

-- ─────────────────────────────────────────────────────────────
-- 题 4–7：逆（数据版）——逆函数由调用者交出来，我们只验证它
-- ─────────────────────────────────────────────────────────────

-- 练习 4（T 类）：左逆 ⇒ 单射（"单射 ↔ 左可逆"能证的那一半；另一半要选择公理）。
-- soko:hint 思路：要从 `f x = f y` 得到 `x = y`：把等式沿 g 推过去，两头再各用一次"g (f ·) = ·"。
-- soko:hint 目标形态：intro x y 与 hxy : Eq.{1} β (f x) (f y) 之后，目标是 Eq.{1} α x y。
-- soko:hint 关键件：链条是 x ← g (f x) → g (f y) → y：左端对 h 用 Eq.symm，中间用 congrArg 把 hxy 抬过 g，右端直接用 h，两段经 Eq.trans 接。
theorem leftInverse_injective (α β : Type) (f : α -> β) (g : β -> α)
    (h : Function.LeftInverse α β g f) : Function.Injective α β f :=
  sorry

-- 练习 5（L 类）：右逆 ⇒ 满射（另一半同样要选择公理）。这条是第 6 题要用的引理。
-- soko:hint 思路：右逆自带"f (g y) = y"，所以对每个 y，原像**现成**——就是 g y。
-- soko:hint 目标形态：intro y 之后目标是 Exists α (fun (x : α) => Eq.{1} β (f x) y)。
-- soko:hint 关键件：Exists.intro 要四样（α、性质、证人、证据）：证人取 g y，证据就是 h y : f (g y) = y。
theorem surjective_of_rightInverse (α β : Type) (f : α -> β) (g : β -> α)
    (h : Function.RightInverse α β g f) : Function.Surjective α β f :=
  sorry

-- 练习 6（T 类）：**双射 ↔ 有逆**（数据版）。
-- 注意前提 `hg`：逆函数 g 与"它是右逆"这条证据由调用者交出——没有它，Iff 的 "→"
-- 方向就是选择公理（见文件头"第 6 题与选择公理"）。有了 hg，两边都能证：
--   "←"：双边逆 ⇒ 单射（题 4）∧ 满射（题 5）；
--   "→"：只需补出左逆，而左逆由**单射**给出（先用 hg 把 f (g (f x)) 拉回 f x）。
-- soko:hint 思路：Iff 是一对箭头：← 方向用第 4 题与第 5 题各取一半；→ 方向手里已有单射与 hg，只差左逆。
-- soko:hint 目标形态：Iff (Function.Bijective α β f) (Function.Inverse α β f g)，两边展开后是"(单射 ∧ 满射) ↔ (左逆 ∧ 右逆)"。
-- soko:hint 关键件：两半都用 And.intro/And.left/And.right 拆装；→ 方向的左逆：对每个 x 要 `g (f x) = x`，用单射把它化成 `f (g (f x)) = f x`——而 hg (f x) 正好是这个等式。
theorem bijective_iff_inverse (α β : Type) (f : α -> β) (g : β -> α)
    (hg : Function.RightInverse α β g f) :
    Iff (Function.Bijective α β f) (Function.Inverse α β f g) :=
  sorry

-- 练习 7（L 类）：题 2 的**逆命题方向**——复合单射 ⇒ 内层单射（外层 g 不变单射）。
-- soko:hint 思路：把 `f x = f y` 沿 g 推成"复合相等"，再交给复合的单射假设。
-- soko:hint 目标形态：intro x y 与 hxy : Eq.{1} β (f x) (f y) 之后，目标是 Eq.{1} α x y。
-- soko:hint 关键件：用 congrArg 把 hxy 沿 g 抬成 `g (f x) = g (f y)`（定义上就是复合相等），再喂给假设 h 的两个点参数。
theorem injective_of_comp_injective (α β γ : Type) (f : α -> β) (g : β -> γ)
    (h : Function.Injective α γ (Function.comp α β γ g f)) : Function.Injective α β f :=
  sorry

-- ─────────────────────────────────────────────────────────────
-- 题 8–9（D 类）：量词顺序交换的**四个变体**
--
-- 把 P x y 读成"f x = y"最直观，四个变体是：
--   (1) `∀ x, ∃ y, P x y`                          —— 不普遍真：β 空而 α 非空时假
--   (2) `∃ y, ∀ x, P x y`                          —— 不普遍真：见第 9 题的反例
--   (3) `(∃ y, ∀ x, P x y) → (∀ x, ∃ y, P x y)`    —— **恒真**（第 8 题）
--   (4) `(∀ x, ∃ y, P x y) → (∃ y, ∀ x, P x y)`    —— **假**（第 9 题）
-- 注意 (1)/(2) 与 (3)/(4) 的关系：(3) 与 (4) 就是"两个命题之间的两个方向"，
-- 而 (3) 恒真、(4) 有反例——这正是"换序不是等价变换"的全部内容。
--
-- （本单元的设计判断：教材里常见的"四个命题哪个为真"判读题，这里压成"判真"与
--  "判假给反例"各一道；(3)(4) 两条判决在题 8/9 里可核，(1)(2) 的不普遍真由上面
--  各自给出的反例说明。）
--
-- 实证提醒（Dubinsky & Yiparaki 2000，**未发表手稿**，引用须注明状态）：94% 的学生
-- 至少把一个 "∀∃" 陈述读成 "∃∀"（反向只有 5%）——所以 (4) 不是粗心，是系统性读法。
-- ─────────────────────────────────────────────────────────────

-- 练习 8（D 类）：换序中**恒真**的那个方向：`∃ y, ∀ x, P x y` 推出 `∀ x, ∃ y, P x y`。
-- 判词：真（证人不动，只是从"一次选好"变成"逐个取用"）。
-- soko:hint 思路：先判真假——∃∀ 强于 ∀∃，所以方向只能是 ∃∀ ⇒ ∀∃；intro x 之后把假设里的证人拿出来用。
-- soko:hint 目标形态：intro x 之后目标是 Exists β (fun (y : β) => P x y)。
-- soko:hint 关键件：先用 Exists.elim 把假设拆成证人 y 与 hy : ∀ x, P x y（Q 取目标那个 Exists）；把 x 交给 hy，再用 Exists.intro 交同一个证人 y。
theorem swap_exists_forall (α β : Type) (P : α -> β -> Prop)
    (h : Exists β (fun (y : β) => forall (x : α), P x y)) :
    forall (x : α), Exists β (fun (y : β) => P x y) :=
  sorry

-- 练习 9（D 类）：换序中**假**的那个方向：`∀ x, ∃ y, P x y` **推不出** `∃ y, ∀ x, P x y`。
-- 判词：假；反例取 P x y := (x = y)，即 **Set Nat 上的恒等关系**：
--   · 前提"每个 x 都有 y"成立——取 y := x 即可（Eq.refl）；
--   · 结论"存在一个 y 对所有 x 成立"会逼出 ∅ = univ：把 y 分别代到 x := ∅、x := univ，
--     再用 0 ∈ univ（Set.mem_univ）但 0 ∉ ∅（Set.notMem_empty）制造矛盾。
-- 也就是说 "∃ y ∀ x" 是一种**一致性**要求，比 "∀ x ∃ y" 强得多——这正是换序不能反过来的原因。
-- soko:hint 思路：先判假并选好反例（Set Nat 上的恒等关系）；intro 假设 h 后把反例 P 与前提喂给 h，得到 Exists (Set Nat) (fun y => ∀ x, x = y)，接下来要从它造 False。
-- soko:hint 目标形态：目标是 Not (forall P, …)；内层要证的是 False（用 Exists.elim 把那个 Exists 拆空）。
-- soko:hint 关键件：前提用 Exists.intro 与 Eq.refl；结论用 Exists.elim 拆出 y 与 hy——把 hy 分别用在 univ 与 empty 这两个具体集合上，经 Eq.trans/Eq.symm 逼出 ∅ = univ；再用 Eq.subst 把 0 ∈ univ 沿这条等式搬到 ∅ 上，交给 Set.notMem_empty 收尾。
theorem swap_converse_false :
    Not (forall (P : Set Nat -> Set Nat -> Prop),
      (forall (x : Set Nat), Exists (Set Nat) (fun (y : Set Nat) => P x y)) ->
      Exists (Set Nat) (fun (y : Set Nat) => forall (x : Set Nat), P x y)) :=
  sorry
