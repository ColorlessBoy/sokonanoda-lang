-- Set.sokonanoda —— 卷 I 的**集合标准库**（L2 层：**只有词汇 + 定义展开**）
--
-- 分层判据（docs/design/course-stdlib.md）：
--   L2（本文件）= 集合的**词汇 + 定义展开**：Mathlib 里是 `rfl` 或一行、**没有数学内容**；
--   L3（units/）= 一切**有数学内容**的陈述（包含三律、最小元、分配律、像/原像…）——
--                 哪怕 Mathlib 早就有，课程就是要让学习者自己证一遍。
-- 台账 L-04 记录了这一层缺什么；L-05 记录了"把 L3 误放进库"的教训。
--
-- **G-05（0.60.0）**：本文件已改用 `namespace Set` 包起来（设计
-- `docs/design/namespace-open.md`）。**全局名一个都没变**（`Set.mem` 还是
-- `Set.mem`，只是源里写成短名 `mem`），所以库外引用（units/、其他 lib/）
-- 一行都不用改。注意 `def Set`（集合**类型**本身）留在命名空间**外面**：
-- 放进去它会变成 `Set.Set`，`Set α` 全部失效（Mathlib 也是把 `Set` 放根上）。
--
-- **L-05 第二刀（2026-09-19）后的完整清单**——本文件只允许留这三类（下面的
-- 名字是**全局名**，源里除 `Set` 外都写在 `namespace Set` 内、不带前缀）：
--   · 12 个定义：Set / Set.mem / Set.subset / Set.empty / Set.univ / Set.singleton /
--     Set.pair / Set.union / Set.inter / Set.sdiff / Set.compl / Set.powerset；
--   · 1 条公理：Set.ext（Mathlib 由 propext + funext 推出，我们两者都没有）；
--   · 10 条定义展开引理：Set.subset_def / Set.mem_empty_iff_false / Set.notMem_empty /
--     Set.mem_univ / Set.mem_singleton_iff / Set.mem_singleton_self / Set.mem_union /
--     Set.mem_inter_iff / Set.mem_sdiff / Set.mem_powerset_iff。
--   往这里加声明之前先问一句：**它有数学内容吗**？有 → 写进 units/ 当练习，不进库。
--
-- L-05 那批**内容型**引理一条都不在本文件里，全在单元中（同名进解答、自证、不引用 lib 版本）：
--   · 单元①：singleton_subset_iff；
--   · 单元②：subset_refl / subset_trans / subset_antisymm / empty_subset /
--             subset_empty_iff / eq_empty_iff_forall_notMem；
--   · 单元③：subset_union_left / subset_union_right / union_subset_iff /
--             inter_subset_left / inter_subset_right / subset_inter / subset_inter_iff /
--             sdiff_subset / powerset_mono；
--   · 单元④：union_comm / inter_comm / inter_union_distrib_left。
--   例外核查（2026-09-19）：`grep -rn` 扫过整个 lib/，以上名字**零出现**——Image/Equiv/Fun/Rel
--   等模块都不依赖内容型集合引理，所以没有"为别的模块保留"的例外。
--   本刀新增练习：**subset_inter_iff**（原先 12 条里唯一画布与解答都没覆盖的一条）。
--   残留待决定：台账 L-05 原表里的 subset_antisymm_iff 至今没有单独立题——它的两半已被
--   单元② 的 subset_antisymm 与单元④ 的外延套路覆盖（这是设计判断，不是遗漏的证明）；
--   要不要像 eq_empty_iff_forall_notMem 那样再包一条 iff，留待下一刀定。
--
-- 名字一律用 **Loogle 取证版**（docs/design/course-stdlib.md §3.1）：
--   Set.notMem_empty（大写 M）、Set.mem_powerset_iff（不是 power）、Set.mem_sdiff（不是 diff）、
--   Set.Subset.refl/trans/antisymm（点号在 Subset 上）。
--
-- 记法对照（语言还没有 notation，G-04）：
--   Set.mem α a A ↔ a ∈ A   ·   Set.subset α A B ↔ A ⊆ B   ·   Set.empty α ↔ ∅
--   Set.union α A B ↔ A ∪ B ·   Set.powerset α A ↔ 𝒫 A
import lib.Logic

-- ── 词汇（Mathlib: Mathlib/Data/Set/Defs.lean，逐字同款）────────────────────
-- 集合**类型**本身在根命名空间（`Set α` 的 `Set`）。
def Set (α : Type) : Type := α -> Prop

namespace Set

def mem (α : Type) (a : α) (A : Set α) : Prop := A a

def subset (α : Type) (A B : Set α) : Prop := forall (x : α), A x -> B x

def empty (α : Type) : Set α := fun (x : α) => False

def univ (α : Type) : Set α := fun (x : α) => True

def singleton (α : Type) (a : α) : Set α := fun (x : α) => Eq.{1} α x a

def pair (α : Type) (a b : α) : Set α :=
  fun (x : α) => Or (Eq.{1} α x a) (Eq.{1} α x b)

def union (α : Type) (A B : Set α) : Set α := fun (x : α) => Or (A x) (B x)

def inter (α : Type) (A B : Set α) : Set α := fun (x : α) => And (A x) (B x)

def sdiff (α : Type) (A B : Set α) : Set α := fun (x : α) => And (A x) (Not (B x))

def compl (α : Type) (A : Set α) : Set α := fun (x : α) => Not (A x)

def powerset (α : Type) (A : Set α) : Set (Set α) := fun (B : Set α) => subset α B A

-- ── 外延性（**公理**：Mathlib 由 propext + funext 推出，我们两者都没有）──────
axiom ext : (α : Type) -> (A B : Set α) ->
  (forall (x : α), Iff (A x) (B x)) -> Eq.{1} (Set α) A B

-- ── 定义展开引理（L2：证明全是"恒等函数"，没有数学内容）────────────────────
theorem subset_def (α : Type) (A B : Set α) :
    Iff (subset α A B) (forall (x : α), A x -> B x) :=
  Iff.intro (subset α A B) (forall (x : α), A x -> B x)
    (fun (h : subset α A B) => h)
    (fun (h : forall (x : α), A x -> B x) => h)

theorem mem_empty_iff_false (α : Type) (a : α) :
    Iff (mem α a (empty α)) False :=
  Iff.intro (mem α a (empty α)) False
    (fun (h : mem α a (empty α)) => h)
    (fun (h : False) => h)

theorem notMem_empty (α : Type) (a : α) : Not (mem α a (empty α)) :=
  fun (h : mem α a (empty α)) => h

theorem mem_univ (α : Type) (a : α) : mem α a (univ α) := True.intro

theorem mem_singleton_iff (α : Type) (a b : α) :
    Iff (mem α a (singleton α b)) (Eq.{1} α a b) :=
  Iff.intro (mem α a (singleton α b)) (Eq.{1} α a b)
    (fun (h : mem α a (singleton α b)) => h)
    (fun (h : Eq.{1} α a b) => h)

theorem mem_singleton_self (α : Type) (a : α) : mem α a (singleton α a) :=
  Eq.refl.{1} α a

theorem mem_union (α : Type) (a : α) (A B : Set α) :
    Iff (mem α a (union α A B)) (Or (mem α a A) (mem α a B)) :=
  Iff.intro (mem α a (union α A B)) (Or (mem α a A) (mem α a B))
    (fun (h : mem α a (union α A B)) => h)
    (fun (h : Or (mem α a A) (mem α a B)) => h)

theorem mem_inter_iff (α : Type) (a : α) (A B : Set α) :
    Iff (mem α a (inter α A B)) (And (mem α a A) (mem α a B)) :=
  Iff.intro (mem α a (inter α A B)) (And (mem α a A) (mem α a B))
    (fun (h : mem α a (inter α A B)) => h)
    (fun (h : And (mem α a A) (mem α a B)) => h)

theorem mem_sdiff (α : Type) (a : α) (A B : Set α) :
    Iff (mem α a (sdiff α A B)) (And (mem α a A) (Not (mem α a B))) :=
  Iff.intro (mem α a (sdiff α A B))
    (And (mem α a A) (Not (mem α a B)))
    (fun (h : mem α a (sdiff α A B)) => h)
    (fun (h : And (mem α a A) (Not (mem α a B))) => h)

theorem mem_powerset_iff (α : Type) (A B : Set α) :
    Iff (mem (Set α) B (powerset α A)) (subset α B A) :=
  Iff.intro (mem (Set α) B (powerset α A)) (subset α B A)
    (fun (h : mem (Set α) B (powerset α A)) => h)
    (fun (h : subset α B A) => h)

end Set

-- ── 记法（G-04 第二刀，0.60.0）：卷 I 的**数学符号**────────────────────────
--
-- 设计 `docs/design/notation-subset.md` §10。记法**不是声明**（N6）：它不产生
-- 任何事件、不进声明表，所以本文件加这一段**不改变任何计数**（课程门禁的
-- 36 目标 / 329 checked / 99 open 逐项不变）。
--
-- 五条都是**集合论专用**符号，所以放课程库、**不进 prelude**（L1 的 30 个名字
-- 与记法无关）。被导入模块声明的记法在**入口文件里直接可用**（第二刀 §10.3 的
-- 跨 `import` 传播）：单元只要 `import lib.Set` 就能写 `𝒫 A`，不必自己重声明
-- ——这也意味着**同一符号全课程只能声明一次**（重声明是 parse 错误）。
--
-- 目标名在**使用点**解析（§9.4）：`Set.powerset` / `Set.compl` 就在本文件里，
-- `Set.image` / `Set.preimage` 在 `lib/Image.sokonanoda`（单元⑧⑫ 会 import 它），
-- `Set.prod` 是**单元⑤ 给出的词汇**（`def Set.prod` 在画布里）。用到某个符号
-- 时，那个目标必须在作用域里——否则报 `elab-notation-unknown-target`，
-- 报错信息会点名缺的是哪个名字。
--
-- 形状与优先级（§10.1、§10.4）：`𝒫`/`ᶜ` 取 100（比 `∪`/`∩` 的 65 紧）、
-- `''`/`⁻¹'`/`×ˢ` 取 80（`''`/`⁻¹'` 的 80 是 Lean 源码逐字取证版）。
prefix:100 " 𝒫 " => Set.powerset
postfix:100 " ᶜ " => Set.compl
infixr:80 " '' " => Set.image
infixr:80 " ⁻¹' " => Set.preimage
infixr:80 " ×ˢ " => Set.prod
