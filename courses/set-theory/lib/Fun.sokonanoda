-- Fun.sokonanoda —— 卷 I 的**函数标准库**（L2 层）
--
-- 分层判据（docs/design/course-stdlib.md）：
--   L2（本文件）= 函数的**词汇 + 定义展开**：Mathlib 里是 `rfl`、一行或纯管道，
--                 **没有数学内容**；
--   L3（units/）= 一切**有数学内容**的陈述（复合结合律、单射/满射在复合下的封闭、
--                 "双射 ↔ 有逆"、量词换序的真假…）——哪怕 Mathlib 早就有，课程就是
--                 要让学习者自己证一遍（台账 L-05：把 L3 放进库，练习就作废了）。
--                 所以单元⑦ 的 9 道题**一条都不在这里**。
--
-- 名字一律照抄 **Mathlib 取证名**（docs/design/course-stdlib.md §3.1）：
--   Function.comp / Function.Injective / Function.Surjective / Function.Bijective /
--   Function.LeftInverse / Function.RightInverse
-- 唯一的例外是 `Function.Inverse`：Mathlib 没有这个名字（它有 `Function.invFun` /
-- `Equiv` / `Function.LeftInverse`）。这里是**设计判断**（不是取证名）：卷 I 的"逆"
-- 只能以**数据版**出现——见下面 §逆 的长注释。
--
-- 记法对照（`g ∘ f` 是 **Mathlib 的**记法，本课程没有声明它；课程库声明的记法
-- 清单见 lib/Set 文件末尾——函数这一层今天仍然全部点名）：
--   Function.comp α β γ g f        ↔  g ∘ f      （先 f 后 g）
--   Function.Injective α β f       ↔  f 单射
--   Function.Surjective α β f      ↔  f 满射
--   Function.Bijective α β f       ↔  f 双射（单射 ∧ 满射）
--   Function.LeftInverse α β g f   ↔  g 是 f 的左逆（g ∘ f = id）
--   Function.RightInverse α β g f  ↔  g 是 f 的右逆（f ∘ g = id）
--   Function.Inverse α β f g       ↔  g 是 f 的双边逆（左逆 ∧ 右逆）
--
-- ── 参数为什么**全部显式**（本卷约定，不是语言缺陷）──────────────────────────
-- 官方 Lean 里这些参数都是隐式的（`Function.Injective f`）。本卷一律点名成
-- `Function.Injective α β f`，理由有二：
--   · 语言今天只有**宇宙**隐式 binder（`{u}`），没有类型隐式 binder 的通行写法；
--   · 点名之后 `query state` 打印的目标、以及学习者抄下来的类型，两边逐字一致，
--     不会出现"隐式参数被省略后在错误信息里变形"的干扰。
-- 代价是每次调用都要把 α β γ 写全——本卷接受这个代价（写在每道题的注释里）。
--
-- ── 复合的顺序（务必与 Mathlib 对齐）─────────────────────────────────────────
-- Mathlib: `Function.comp f g x = f (g x)`，即**第一个函数参数最后作用**：
--     Function.comp α β γ g f  就是  g ∘ f  ：先 f（α → β），再 g（β → γ），得 α → γ。
-- 所以 `Function.comp α β γ g f` 的类型是 `α -> γ`，单元⑦ 的
-- `injective_comp`（f 在前还是 g 在前）就是按这条顺序写的：
--     Injective f -> Injective g -> Injective (Function.comp g f)。
--
-- ── 逆（数据版）：为什么不是 "∃ g, …" ───────────────────────────────────────
-- 拆开看，官方 Lean 里"逆"有**两个层次**：
--   (a) 数据版：`g` 由调用者**交出来**，我们只验证它是左逆/右逆——本文件的
--       `LeftInverse` / `RightInverse` / `Inverse` 就是这个层次，全都能证；
--   (b) 存在版：`Surjective f -> ∃ g, RightInverse g f`（即"满射可裂"）——
--       这是**选择公理**，官方 Lean 里由 `Classical.choice` 提供。
-- 本文件只立 (a)：prelude/lib 里既没有 `Classical.choice`，也没有 `funext`/`propext`，
-- (b) 在本语言里**证不出来**（单元⑦ 的 `bijective_iff_inverse` 只能证一半，
-- 见画布注释与单元报告）。把 (a) 立成定义，练习才有东西可证。
-- 注意 `Inverse α β f g` 的参数顺序与 Mathlib 的 `LeftInverse` 一致：**逆函数 g 在前、
-- 原函数 f 在后**（`LeftInverse g f` 读作 "g 是 f 的左逆"），`Inverse f g` 则按
-- "谁是谁的逆"的自然语序把 f 放前面。
import lib.Logic
import lib.Exists

-- ── 复合（Mathlib: Mathlib/Logic/Function/Defs.lean，`Function.comp`）──────────
-- `Function.comp α β γ g f` 展开就是 `fun x => g (f x)`；定义体直接是 λ，所以
-- 复合**不引入任何数学内容**，`Function.comp_apply` 是 `rfl` 级展开引理。
def Function.comp (α β γ : Type) (g : β → γ) (f : α → β) : α → γ :=
  fun (x : α) => g (f x)

-- ── 单射 / 满射 / 双射（Mathlib: Mathlib/Logic/Function/Defs.lean）────────────
-- 单射按题面要求的形状写：`forall x y, f x = f y -> x = y`（Mathlib 逐字同款）。
def Function.Injective (α β : Type) (f : α → β) : Prop :=
  ∀ (x : α), ∀ (y : α), Eq.{1} β (f x) (f y) → Eq.{1} α x y

-- 满射用 `Exists`（lib.Exists 的三件套）：每个 y 都有**原像** x，且原像满足 f x = y。
-- 注意 `Exists α p` 的 `p` 是 `α -> Prop`，所以"原像"写成 `fun (x : α) => Eq.{1} β (f x) y`。
def Function.Surjective (α β : Type) (f : α → β) : Prop :=
  ∀ (y : β), ∃ (x : α), Eq.{1} β (f x) y

def Function.Bijective (α β : Type) (f : α → β) : Prop :=
  (Function.Injective α β f) ∧ (Function.Surjective α β f)

-- ── 左逆 / 右逆 / 双边逆（数据版；见文件头 §逆）──────────────────────────────
-- `LeftInverse α β g f`：g : β -> α，f : α -> β，逐点 `g (f x) = x`。
def Function.LeftInverse (α β : Type) (g : β → α) (f : α → β) : Prop :=
  ∀ (x : α), Eq.{1} α (g (f x)) x

-- `RightInverse α β g f`：逐点 `f (g y) = y`（与 Mathlib 同款：`RightInverse g f`
-- 就是 `LeftInverse f g`，只是展开写出来）。
def Function.RightInverse (α β : Type) (g : β → α) (f : α → β) : Prop :=
  ∀ (y : β), Eq.{1} β (f (g y)) y

-- `Inverse α β f g`：双边逆 = 左逆 ∧ 右逆。**它是 Prop，g 是参数**——这就是"数据版"：
-- 逆函数不是被"存在"出来的，而是被调用者交进来的（对照文件头 §逆 的 (a)/(b)）。
def Function.Inverse (α β : Type) (f : α → β) (g : β → α) : Prop :=
  (Function.LeftInverse α β g f) ∧ (Function.RightInverse α β g f)

-- ── 定义展开引理（L2：证明全是恒等函数或 `rfl`，没有数学内容）────────────────
-- 与 lib/Set 的 `Set.subset_def` / `Set.mem_union` 同一性质：两个方向都是 `fun h => h`。
-- 它们的用处是**让学习者不必手写展开**：`Iff.mp (Function.injective_def α β f) h` 就把
-- 单射假设变成 `forall x y, f x = f y -> x = y`。

-- `rfl` 级：复合算在参数上就是"先 f 后 g"（Mathlib: `Function.comp_apply`）。
theorem Function.comp_apply (α β γ : Type) (g : β → γ) (f : α → β) (x : α) :
    Function.comp α β γ g f x = g (f x) :=  Eq.refl.{1} γ (g (f x))

theorem Function.injective_def (α β : Type) (f : α → β) :
    Function.Injective α β f ↔ ∀ (x : α), ∀ (y : α), f x = f y → x = y :=  Iff.intro (Function.Injective α β f)
    (∀ (x : α), ∀ (y : α), Eq.{1} β (f x) (f y) → Eq.{1} α x y)
    (fun (h : Function.Injective α β f) => h)
    (fun (h : ∀ (x : α), ∀ (y : α), Eq.{1} β (f x) (f y) → Eq.{1} α x y) => h)

theorem Function.surjective_def (α β : Type) (f : α → β) :
    Function.Surjective α β f ↔ ∀ (y : β), ∃ (x : α), f x = y :=  Iff.intro (Function.Surjective α β f)
    (∀ (y : β), ∃ (x : α), Eq.{1} β (f x) y)
    (fun (h : Function.Surjective α β f) => h)
    (fun (h : ∀ (y : β), ∃ (x : α), Eq.{1} β (f x) y) => h)

theorem Function.bijective_def (α β : Type) (f : α → β) :
    Function.Bijective α β f
      ↔ Function.Injective α β f ∧ Function.Surjective α β f :=  Iff.intro (Function.Bijective α β f)
    ((Function.Injective α β f) ∧ (Function.Surjective α β f))
    (fun (h : Function.Bijective α β f) => h)
    (fun (h : (Function.Injective α β f) ∧ (Function.Surjective α β f)) => h)

theorem Function.leftInverse_def (α β : Type) (g : β → α) (f : α → β) :
    Function.LeftInverse α β g f ↔ ∀ (x : α), g (f x) = x :=  Iff.intro (Function.LeftInverse α β g f) (∀ (x : α), Eq.{1} α (g (f x)) x)
    (fun (h : Function.LeftInverse α β g f) => h)
    (fun (h : ∀ (x : α), Eq.{1} α (g (f x)) x) => h)

theorem Function.rightInverse_def (α β : Type) (g : β → α) (f : α → β) :
    Function.RightInverse α β g f ↔ ∀ (y : β), f (g y) = y :=  Iff.intro (Function.RightInverse α β g f) (∀ (y : β), Eq.{1} β (f (g y)) y)
    (fun (h : Function.RightInverse α β g f) => h)
    (fun (h : ∀ (y : β), Eq.{1} β (f (g y)) y) => h)

theorem Function.inverse_def (α β : Type) (f : α → β) (g : β → α) :
    Function.Inverse α β f g
      ↔ Function.LeftInverse α β g f ∧ Function.RightInverse α β g f :=  Iff.intro (Function.Inverse α β f g)
    ((Function.LeftInverse α β g f) ∧ (Function.RightInverse α β g f))
    (fun (h : Function.Inverse α β f g) => h)
    (fun (h : (Function.LeftInverse α β g f) ∧ (Function.RightInverse α β g f)) => h)
