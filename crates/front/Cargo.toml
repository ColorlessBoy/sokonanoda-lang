[package]
name = "sokonanoda-front"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Restricted .sokonanoda front-end (lexer, parser, diagnostics) for sokonanoda-lang"

[dependencies]
sokonanoda = { path = "../kernel" }
stumpalo = "0.5.1"
num-bigint = "0.4.8"

# lang：教学 crates 的 clippy 严格门禁（kernel 冻结快照不参与）。
[lints.rust]
warnings = "deny"
