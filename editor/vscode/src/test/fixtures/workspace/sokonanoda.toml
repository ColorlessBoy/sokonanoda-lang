# e2e 夹具的模块根（计划 T-015）。
#
# **故意不写 `requires`**：写了就要跟着仓库版本 bump，一旦漂移会让
# `requires_warning` 把项目编译缓存静默关掉（G-24），从而把这里所有 e2e 用例
# 变成"时快时慢"的。`requires` 漂移本身由 G-24 自己的复现脚本管，
# 不该由 e2e 夹具承担。
name = "workspace"
