{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "cargo build sokonanoda-lsp",
      "type": "shell",
      "command": "cargo",
      "args": ["build", "-p", "sokonanoda-lsp"],
      "options": { "cwd": "${workspaceFolder}/../.." },
      "problemMatcher": ["$rustc"]
    }
  ]
}
