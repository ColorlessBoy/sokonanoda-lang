-- ==========================================================================
-- playground.sokonanoda —— 我们的画布
--
-- 下面的注释是课，带 sorry 的声明是练习题。你把 sorry 换成答案，保存，
-- 我来判。判对判错由内核说了算：填好的每一行放进官方 Lean 也一样合法。
--
-- 写的时候记住两个习惯：
--   * 拿不准就把光标放在 sorry 上悬停，那里写着还差什么。
--   * 一道题可以分几次写完，剩下的照旧留 sorry。
-- ==========================================================================

-- ─────────────────────────────────────────────────────────────
-- 第一课 命题与证明项
--
-- 命题逻辑（propositional logic）讨论由真、假、且、或、非这类连接词
-- 拼成的断言，以及断言之间的推理。本课只讲一条中心思想：命题可以
-- 看成一个类型，而证明就是这个类型的一个项。于是“证明一个命题”这件
-- 看似抽象的事，变成了具体的事——构造一个值。
--
-- 下面先看一条声明的格式，再认识公理（axiom）、命题（proposition）
-- 与证明（proof），随后用一套迷你逻辑骨架把这条思想跑起来。
-- 课末有五道练习题（练习 1–5），用来检验能否独立写出证明项。
--
-- 0.1 先学会读一条声明（declaration）。
--
-- 在 Lean 里，每个名字都由一条声明引入，格式固定为
--
--        def 名字 : 类型 := 值
--
-- 冒号后面是这个名字的类型，等号后面是它的值。往后读任何声明都可以
-- 套这个格式：先看名字，再看类型，最后看值。下面几节会用真实声明把
-- 这三部分对上看清。
--
-- 0.2 axiom（公理）。
--
-- 大多数名字的值要靠计算或构造得来，但有些最基本的东西无法再用别的
-- 东西解释。对这类东西我们不构造它，而是直接承认它存在，这样的声明
-- 叫公理（axiom）。它比 0.1 的 def 少一个等号和值，格式为
--
--        axiom 名字 : 类型
--
-- 公理只给出名字和类型，不给值；名字的存在本身就是被承认的，不从别处
-- 推出。本课要引入的第一个东西，是命题所属的类型 Prop：
--
--        axiom Prop : Sort 1
--
-- 这一行的意思是：类型 Prop 的存在被当作公理接受下来，同时指明它自己
-- 的类型是 Sort 1。它带出两个名字：Prop 是命题的类型，往后凡是命题，
-- 类型一栏都写 Prop；Sort 1 描述 Prop 住在哪一层，这里先认得写法，
-- 等以后读到函数类型的类型时再回来解释。
--
-- 0.3 命题（proposition）。
--
-- 命题就是类型为 Prop 的项。Prop 在形式化证明里地位特殊，它装的是
-- 断言而不是数据；判断一个名字是不是命题，看类型一栏是不是 Prop 即可，
-- 格式与 0.2 相同：
--
--        axiom True : Prop
--        axiom False : Prop
--
-- 这两行各声明了一个命题，True 表示真，False 表示假。它们是最简单的
-- 命题，后面会用连接词把命题拼成更复合的命题。单看这两行，编译器并不
-- 知道 True 是真、False 是假——真假要靠后面引入的性质来规定。
--
-- 0.4 证明（proof）。
--
-- 要证明一个命题，用 theorem 声明，格式为
--
--        theorem 名字 : 目标类型 := 证明项
--
-- 证明一个命题 P，就是构造出一个类型为 P 的项。编译器会检查证明项的
-- 类型与目标类型是否一致：只要“证明项 : 目标类型”成立，命题 P 就被
-- 证明成立。把命题当类型、把证明当项，这个看法是本课的中心，后面会
-- 反复用到。
--
-- ─────────────────────────────────────────────────────────────

-- 先把命题所属的类型立起来。
axiom Prop : Sort 1
-- 需要说明的是，官方 Lean 里 Prop 本来就是内建的，不能再声明一次；
-- 画布让这行通过，只是拿它当占位，交代 Prop 从哪一行被引入。
-- Prop 在形式化证明里有非常特殊的地位，它不是普通的类型，所以内核里附带了很多 Prop 规则。

axiom True : Prop
axiom False : Prop

-- 先看 True。声明一个命题并不等于它成立：True 这个名字只是被立了
-- 出来，说明“有这么个命题”。要让它真正成立，得有人拿得出它的证明。
-- 下一行 True.intro 正是这样一份证明，它的类型恰好是 True，也不需要
-- 任何参数——这份证明现成就有。True 能成立，靠的就是它。
axiom True.intro : True

-- False 恰好相反：它没有任何构造子，无论怎么努力都造不出一份 False
-- 的证明。“假”的意思就在这里——一个拿不出证明的命题。False.rec 不是
-- 用来证明 False 的，而是用来使用 False 的：如果有人真给你一份 False
-- 的证明，说明推理中已经出现矛盾，这时任何命题 P 的证明都能由它换到。
-- 这条规则叫“从矛盾推出一切”。
axiom False.rec : (P : Prop) -> False -> P

-- And 表示“且”。它接收两个命题，返回它们合起来的新命题，类型因此是
-- Prop -> Prop -> Prop，也就是从命题到命题的函数类型（函数类型在 0.5
-- 正式讲，这里先按字面读）。
axiom And : Prop -> Prop -> Prop

-- 下面三条规则说明 And 的用法。规则里的箭头读作“有左边的证明，就能
-- 换到右边的结论”：And.intro 用两份证明装出一个“且”，And.left 和
-- And.right 则从一个“且”里各取出一半。
axiom And.intro : (a : Prop) -> (b : Prop) -> a -> b -> And a b
axiom And.left : (a : Prop) -> (b : Prop) -> And a b -> a
axiom And.right : (a : Prop) -> (b : Prop) -> And a b -> b

-- Or 表示“或”。它同样接收两个命题并返回新命题；Or.inl 由左半边的证明
-- 给出整个“或”，Or.inr 则由右半边的证明给出。
axiom Or : Prop -> Prop -> Prop
axiom Or.inl : (a : Prop) -> (b : Prop) -> a -> Or a b
axiom Or.inr : (a : Prop) -> (b : Prop) -> b -> Or a b

-- Not 与上面几条不同，它不是公理而是定义。Not a 展开后就是 a -> False：
-- 一份“非 a”的证明，就是一台把 a 的证明变成 False 的证明的机器。
-- 初看会觉得奇怪，“非”怎么会是一个函数；等 0.5 讲完函数类型，这一点
-- 就自然了。
def Not : Prop -> Prop := fun (a : Prop) => a -> False

-- 到这里，本课要用的公理都齐了。下面完整读一条写好的证明，看看前面
-- 每一件东西实际怎么组合在一起：
--
--   example : Or True False := Or.inl True False True.intro
--   └名字   └类型(命题)      └值(证明)
--
-- 这条声明的类型是命题 Or True False，也就是“真或假”；它的值是把构造子
-- Or.inl 依次应用到四个参数上：前两个参数是 Or 的两个命题 True 和
-- False，后两个参数是支持左边成立的那份证明，这里就是 True.intro。
-- Or.inl 的规则说，只要有左边的证明，整个 Or 就成立。另外，example
-- 是不取名字的匿名声明，类型和值之间用 := 隔开。
example : Or True False := Or.inl True False True.intro

-- ─────────────────────────────────────────────────────────────
-- 0.5 函数与函数类型
--
-- 练习 2 到练习 4 都要用到函数，所以先把它讲清楚。这里约定一个说法：
-- “输入”“输出”只用来描述函数；类型本身没有输入输出，它只负责把函数
-- 归成一类。
--
-- 函数（function）是有输入输出的值，例如
--
--        def add1 : Nat -> Nat := fun n => n + 1
--
-- 等号右边 fun n => n + 1 就是函数本身：输入 n，输出 n + 1。把它用到
-- 某个值上（比如 add1 41）会算出结果 42，这一步叫应用（application）。
--
-- 函数类型（function type）则是 Nat -> Nat 这样的写法，读作“从 Nat 到
-- Nat 的函数”。它是 add1 的类型：所有“输入 Nat、输出 Nat”的函数都归
-- 到这一类。类型自己不输入也不输出，它只做分类。
--
-- 于是 f : A -> B 就表示 f 的类型是 A -> B，f 是一个从 A 到 B 的函数，
-- 类型永远写在冒号右边。公理行里出现的第二个冒号是另一回事。在
--
--        axiom Or.inl : (a : Prop) -> (b : Prop) -> a -> Or a b
--
-- 里，(a : Prop) -> 是一个带名字的箭头，意思是“先给一个命题，并把它
-- 命名为 a”，后面的每一层都可以提到这个名字。逐层读这条类型：第一层
-- 先收一个命题叫 a，第二层再收一个命题叫 b，第三层要求一份 a 的证明，
-- 最终给出 Or a b 的证明。第三层 a -> Or a b 里没有冒号，因为 a 不是
-- 新名字，就是第一层那个 a。箭头之所以要带名字，是因为结论 Or a b
-- 必须提到前两层给的东西，不起名字就写不出来。这种类型在 Lean 里用
-- forall 书写，是后面量词的骨架。
--
-- 命题里的每一层箭头，在证明项里对应一层 fun。看下面这个写好的例子：
theorem demo_K : (a : Prop) -> a -> a :=
  fun (a : Prop) => fun (h : a) => h
-- 类型有两层带名字的箭头，值就写两层 fun：第一层参数是 a : Prop，第二
-- 层参数是 h : a，也就是 a 的证明，函数体直接返回 h。练习 2 的类型有
-- 四层箭头，值就写四层 fun，最后用 And.intro 把结果装起来，做法完全
-- 一样。
--
-- 最后是箭头的结合方式，读长类型时用得上。全课程只依赖两条规则：
-- -> 向右结合，所以 a -> b -> c 等于 a -> (b -> c)；fun 的函数体则一直
-- 延伸到能吃到的最远处，所以在 fun (a : Prop) => a -> False 里，先结合
-- 的是 a -> False，整个箭头都在 fun 的内部。编辑器里可以验证这一点：
-- 把光标放在 -> 上反复按“扩展选择”（macOS 是 Ctrl+Cmd+Shift+→），
-- 第一级圈出的就是先结合的那一段。
-- ─────────────────────────────────────────────────────────────

-- 练习 1：证明 True。它的证明已经存在，把它叫出来即可。
-- soko:hint 思路：目标就是找一个类型为 True 的现成证明，翻回 axiom 列表找。
-- soko:hint 目标形态：目标 True 没有箭头可拆，直接问自己：环境里谁能造出 True？
-- soko:hint 关键件：True.intro——True 的构造子，不需要任何参数。
theorem true_is_true : True := sorry

-- 练习 2：若 a、b 都成立，则 a 且 b 成立。
-- soko:hint 思路：类型里的箭头对应值里的 fun——写证明就是一串 fun 加最后一步构造。
-- soko:hint 目标形态：最外层是四个箭头：先写 fun (a : Prop) =>，每写一层就看剩余目标。
-- soko:hint 关键件：最后一步用 And.intro，它吃 a、b 和两个证明，装出 And a b。
theorem and_intro_rule : (a : Prop) -> (b : Prop) -> a -> b -> And a b := sorry

-- 练习 3：由 a 且 b 推出 b 且 a。
-- soko:hint 思路：先把拿到的 And a b 拆开，再按新顺序装回去——先消去，再构造。
-- soko:hint 目标形态：三层箭头：fun (a : Prop) => fun (b : Prop) => fun (h : And a b) => …，最后要造出 And b a。
-- soko:hint 关键件：And.left / And.right 从 h 里取出两半；And.intro b a … 换个顺序装回去。
theorem and_swap : (a : Prop) -> (b : Prop) -> And a b -> And b a := sorry

-- 练习 4：由 False 推出任意命题 P。
-- soko:hint 思路：False 证不出来（没有构造子），但可以“被使用”——从矛盾推出任何命题。
-- soko:hint 目标形态：两层箭头：先 fun (P : Prop) =>，再吃掉 h : False，剩余目标是任意的 P。
-- soko:hint 关键件：False.rec——它吃一个目标命题和一份 False 的证明，吐出那个命题的证明。
theorem ex_falso : (P : Prop) -> False -> P := sorry

-- 练习 5（挑战）：a 与“非 a”不能同时成立。
-- soko:hint 思路：And 把 a 和 Not a 打包在一起；Not a 展开后是 a -> False——一个“吃 a”的函数。
-- soko:hint 目标形态：两层 fun 拿到 a 和 h : And a (Not a) 之后，目标就是 False 本身。
-- soko:hint 关键件：And.right 取出 Not a 那一半、And.left 取出 a 那一半；把前者当函数应用于后者。
theorem and_not_absurd : (a : Prop) -> And a (Not a) -> False := sorry

-- ─────────────────────────────────────────────────────────────
-- 下一课预告
--
-- 这五道题都做出来之后，我们再在画布上加下一课：等式与 rfl。如果中途
-- 卡住，可以打开编辑器左侧的“提示”节点逐条查看，或直接告诉我你写到
-- 了哪里。
-- ─────────────────────────────────────────────────────────────
