-- ==========================================================================
-- playground.sokonanoda —— 我和你共同的教学画布
--
-- 逻辑先行：先证明命题，Sort 等你自然问起“函数类型的类型”时再揭晓。
-- 规则：
--   * 带 sorry 的声明是练习。把 sorry 换成你的答案，由完整内核判定对错。
--   * sorry 也可以写成 sorry（和官方 Lean 一样的占位符，两者等价）。
--   * 部分作答是合法状态：先写前几层 fun，最后一层留 sorry，剩余目标会显示出来。
--   * 悬停任何表达式都能看到它的类型（编辑器里）。
--   * 判定永远走内核：填完的每一行放回官方 Lean 也合法。
-- ==========================================================================

-- ─────────────────────────────────────────────────────────────
-- 单元① 命题与证明项（先学语法，再证明）
--
-- 本单元的路线（四小步）。规矩：新概念先给专有名词（中文 + 英文），
-- 比喻只留一个（"门票"，讲证明时用）：
--   0.1 学会读一条声明 → 0.2 认识 axiom（公理）→
--   0.3 命题是什么 → 0.4 证明是什么（票）
--   → 读一张写好的完整证明（拆演示）→ 补两件小工具（0.5 箭头与 fun）
--   → 然后轮到你上手（练习 1–5）。
-- 别跳步；每一步都为下一步垫一块砖。
--
-- 0.1 先学会读一条声明。所有声明长一个样：
--
--        名字 : 类型 := 值
--
--     读作：「名字」的类型是「类型」，「值」是它对应的具体内容。
--     例：def one : Nat := 1 ——「one 的类型是 Nat，值是 1」。
--
-- 0.2 axiom（公理）= 直接承认「存在这么个东西」，不解释来历。
--     下面这些 axiom 搭出一个迷你逻辑世界：True / False / And / Or
--     这些命题，以及各自的构造规则。
--     （真实 Lean 里这些是内建的；画布里自己声明，是为了看清每条规则。）
--
-- 0.3 命题 = 对世界的一个断言（"真"、"假"、"a 且 b"……），
--     它们住在 Prop 这个世界里（就像 1、2 住在 Nat 里）。
--     True 是一个命题；And a b（读作 a 且 b）也是一个命题。
--
-- 0.4 什么是证明？我们把「命题」也当成一种类型：
--     ★ 类型正好是命题 P 的值，就是 P 的证明。
--     所以「证明 P」没有任何神秘——就是按 0.1 的格式，造一个
--     类型为 P 的值出来：
--
--        声明 : 命题 P := 证明（就是值）
--
-- ─────────────────────────────────────────────────────────────

-- True / False：两个最基础的命题……
axiom True : Prop
axiom False : Prop

-- ★ 关键：光有名字，不算数。
--   上一行只是把 True 这个名字立在墙上：有这么个命题。
--   名字立起来，命题还不成立——得有人真能拿出它的门票。
--   看下一行：True.intro 的类型恰好就是 True，也就是说这张票现成就有。
--   有票，True 才算立住了。
--   换个角度看：True.intro 名义上挂在 True 名下，
--   其实 True 能不能站住，全靠它。
axiom True.intro : True

-- False 反过来：一张票都不发。
--   没有任何办法造出 False 的票——"假"就是这个意思：
--   什么证明都拿不出来的命题。不写构造子，本身就是一种定义。
-- False.rec 是使用 False 的办法（注意：不是证明！）：
--   假如有一天有人真的递给你一张 False 的票——说明世界已经矛盾了——
--   那你想要什么命题的票都能换到。这叫"从矛盾推出一切"。
axiom False.rec : (P : Prop) -> False -> P

-- And：And a b 读作「a 且 b」。And 是个函数：输入两个命题，
-- 输出"且"起来的新命题。Prop -> Prop -> Prop 就是从命题到命题的函数类型。
-- （"函数类型"0.5 正式讲，这里先按字面读。）
axiom And : Prop -> Prop -> Prop

-- And 的三条规则（类型里的箭头 a -> b 读作「有 a 的票，就能换 b 的票」）：
axiom And.intro : (a : Prop) -> (b : Prop) -> a -> b -> And a b
axiom And.left : (a : Prop) -> (b : Prop) -> And a b -> a
axiom And.right : (a : Prop) -> (b : Prop) -> And a b -> b

-- Or：Or a b 读作「a 或 b」；inl / inr 是它的两个构造子。
axiom Or : Prop -> Prop -> Prop
axiom Or.inl : (a : Prop) -> (b : Prop) -> a -> Or a b
axiom Or.inr : (a : Prop) -> (b : Prop) -> b -> Or a b

-- Not 不是公理，是定义：Not a 展开后就是 a -> False。
-- def 的值不再"直接承认"，而是算出来：fun (a : Prop) => a -> False
-- 读作「给一个命题 a，还你 a -> False（有 a 的票就换成 False 的票）」。
-- ★ 第一次见都会愣住："非"怎么是个函数？！——正常，几乎所有人都卡这。
--   往下看 0.5，两分钟后它就不奇怪了。
def Not : Prop -> Prop := fun (a : Prop) => a -> False

-- 到这里，牌发完了（True/False/And/Or 的票和规则），Not 也见过了。
-- 下面读一张**已经写好的完整证明**——本课第一张。别急着自己写，
-- 先把它逐行拆开，看前几节里的每件东西怎么各就各位：
--
--   example : Or True False := Or.inl True False True.intro
--   └名字   └类型(命题)      └值(证明)
--
--   读作：这张证明的类型是命题 Or True False（真，或，假）；
--   它的值是——把构造子 Or.inl 按顺序应用到 4 个参数上：
--   ①②先给 Or 两个命题（True、False），③④再各给一个证明：
--   True 的证明（True.intro）。Or.inl 的规则说：只要有左边的证明，
--   Or True False 就成立了。
--   （顺带：`example` = 匿名声明，不取名字；类型/值两半用 := 隔开。）


-- ─────────────────────────────────────────────────────────────
-- 0.5 函数类型与函数（练习 2–4 的全部工具都在这节）
--
-- 措辞约定（别跳过这两行）：「输入」「输出」只说函数；
-- 类型本身不输入不输出，它只负责把函数分类。
--
-- ① 函数（function）：有输入输出的值。
--    def add1 : Nat -> Nat := fun n => n + 1
--    函数本尊是 fun n => n + 1：输入 n，输出 n + 1。
--    add1 41 把函数用到 41 上，算出 42（这叫应用）。
--
-- ② 函数类型（function type）：Nat -> Nat 这样的写法，
--    读作"从 Nat 到 Nat 的函数"。它是 add1 的类型：
--    把"输入 Nat、输出 Nat"的函数归为一类，add1 就属于这一类。
--    类型自己没有输入输出。
--
-- ③ f : A -> B 的读法：f 的类型是 A -> B，也就是说 f 是一个
--    从 A 到 B 的函数。类型永远写在冒号右边。
--
--    那 axiom 行里的**两个冒号**呢？比如
--      axiom Or.inl : (a : Prop) -> (b : Prop) -> a -> Or a b
--    这里的冒号不是声明行的冒号，而是**带名字的箭头**：
--    (a : Prop) -> 的意思是"先给一个命题，给它起名 a"，
--    后面每一层都能提到这个名字。逐层读：
--      第 1 层 (a : Prop)  ：你给一个命题，它从此叫 a
--      第 2 层 (b : Prop)  ：你再给一个命题，它叫 b
--      第 3 层 a -> Or a b ：没有冒号——a 不是新名字，就是第 1 层那个 a。
--                            a -> Or a b 仍是函数类型：它的函数
--                            吃 a 的证明，产出 Or a b 的证明
--    为什么箭头要带名字？因为结果 Or a b 要提到前两层给的东西——
--    不起名字就写不出来。这种类型在 Lean 里用 forall 写，
--    是逻辑量词的骨架（后面单元重逢）。
--
-- ④ 命题里的箭头 ↔ 值里的 fun：类型每有一层箭头，值就写一层 fun。
--    微演示（已写好，真代码）：
theorem demo_K : (a : Prop) -> a -> a :=
  fun (a : Prop) => fun (h : a) => h
--    两层带名字的箭头 ↔ 两层 fun。第 1 层参数 a : Prop；
--    第 2 层参数 h : a（h 是 a 的证明）；函数体 h 就是返回的证明。
--    练习 2 的 and_intro_rule 类型有四层箭头，值就写四层 fun，
--    最后用 And.intro 拼装——同一个游戏。
--
-- ⑤ 优先级与作用范围（读长类型不迷路）：
--    感觉复杂？拆开只有两条规则，而且全课程复用这两条，不再新增：
--    · -> 右结合：a -> b -> c 就是 a -> (b -> c)。
--    · fun 的身体延伸到"能吃到的最远处"——所以在
--      fun (a : Prop) => a -> False 里，-> 属于 fun 的身体，
--      先结合的是 a -> False（不是先 fun）。
--    · 编辑器实证：光标放到 -> 上，按"扩展选择"
--      （macOS: Ctrl+Cmd+Shift+→；Win/Linux: Ctrl+Shift+→）
--      逐级放大——第一级圈出的就是先结合的那一段。
-- ─────────────────────────────────────────────────────────────

-- 练习 1（第一个“证明”）：True 的证明项已经存在，把它叫出来即可。
-- soko:hint 思路：目标就是找一个类型为 True 的现成证明，翻回 axiom 列表找。
-- soko:hint 目标 True 没有箭头可拆，直接问自己：环境里谁能造出 True？
-- soko:hint 关键件：True.intro——True 的构造子，不需要任何参数。
theorem true_is_true : True := True.intro

-- 练习 2：假设 a、b 都成立，就能得到 And a b。
-- 提示：四层 fun，最后 And.intro a b ha hb。
-- soko:hint 思路：类型里的箭头对应值里的 fun——写证明就是一串 fun 加最后一步构造。
-- soko:hint 目标形态：最外层是四个箭头：先 fun (a : Prop) => …，每写一层就看剩余目标。
-- soko:hint 关键件：最后一步用 And.intro，它吃 a、b 和两个证明，装出 And a b。
theorem and_intro_rule : (a : Prop) -> (b : Prop) -> a -> b -> And a b := And.intro

-- 练习 3：先用 And.left/And.right 把 h 拆开，再用 And.intro 装回去（换序）。
-- soko:hint 思路：先把拿到的 And a b 拆开，再按新顺序装回去——先消去，再构造。
-- soko:hint 目标形态：三层箭头：fun (a : Prop) => fun (b : Prop) => fun (h : And a b) => …，最后要造出 And b a。
-- soko:hint 关键件：And.left / And.right 从 h 里取出两半；And.intro b a … 换个顺序装回去。
theorem and_swap : (a : Prop) -> (b : Prop) -> And a b -> And b a := 
  fun (a : Prop) (b : Prop) (h : And a b) => And.intro b a (And.right a b h) (And.left a b h)

-- 练习 4：False 没有证明（没有构造子），但可以“用”它：从矛盾推出任何 P。
-- soko:hint 思路：False 证不出来（没有构造子），但可以“被使用”——从矛盾推出任何命题。
-- soko:hint 目标形态：两层箭头：先 fun (P : Prop) =>，再吃掉 h : False，剩余目标是任意的 P。
-- soko:hint 关键件：False.rec——它吃一个目标命题和一份 False 的证明，吐出那个命题的证明。
theorem ex_falso : (P : Prop) -> False -> P := False.rec

-- 练习 5（挑战 ★）：And a (Not a) 是自相矛盾的组合。拆开它，把 Not a 那一半
-- 当作函数应用到另一半。提示：Not a 会展开成 a -> False。
-- soko:hint 思路：And 把 a 和 Not a 打包在一起；Not a 展开后是 a -> False——一个“吃 a”的函数。
-- soko:hint 目标形态：两层 fun 拿到 a 和 h : And a (Not a) 之后，目标就是 False 本身
-- soko:hint 关键件：And.right 取出 Not a 那一半、And.left 取出 a 那一半；把前者当函数应用于后者。
theorem and_not_absurd : (a : Prop) -> And a (Not a) -> False := 
  fun (a : Prop) (h : And a (Not a)) => (And.right a (Not a) h) (And.left a (Not a) h)

-- ─────────────────────────────────────────────────────────────
-- 单元② 等式与 rfl（先认识一个具体类型：数字）
-- Nat 是自然数类型；0、1、2 是它的字面量；+ 是真正的加法，
-- 内核会把 1 + 1 算成 2。“相等”也是命题：Eq x y 读作 x = y。
-- prelude 提供受信任的等式基元（签名与官方 Lean 一致）：
--   Eq {u}       : {α : Sort u} -> α -> α -> Prop
--   Eq.refl {u}  : {α : Sort u} -> (a : α) -> Eq.{u} α a a
--   Eq.subst {u} : {α : Sort u} -> {p : α -> Prop} -> {a : α} -> {b : α}
--                  -> Eq.{u} α a b -> p a -> p b
-- 机械规则：数字的等式必须显式写 Eq.{1} —— 为什么是 1？单元④揭晓。
-- 隐式参数不做补全：按位置显式给全。
-- ─────────────────────────────────────────────────────────────

-- 演示（已写好，无需改动）：证明 2 + 2 = 4。内核把左边算成 4，refl 立即成立。
def demo_refl : Eq.{1} Nat (2 + 2) 4 := Eq.refl.{1} Nat (2 + 2)

-- 练习 6（热身）：定义 two : Nat。提示：写字面量 2，或写 1 + 1 都可以。
-- （本练习只检查类型；下面注释着的 two_def 会回头“证明”它的值确实是 1 + 1。）
-- soko:hint 思路：这个洞里放的不是证明，是一个类型为 Nat 的普通表达式。
-- soko:hint 目标形态：目标就是 Nat 本身，没有箭头也没有假设——直接给值。
-- soko:hint 关键件：数字字面量就是合法项；填好后记得放开 two_def，让内核回判它的值。
def two : Nat := sorry

-- 练习 7（rfl 会计算）：证明 1 + 1 = 2。提示：Eq.refl.{1} 的第一个参数给
-- Nat，第二个参数给等号左边；内核会把左边算出来，发现就是 2。
-- soko:hint 思路：rfl 不是咒语而是函数：Eq.refl 能证任何“两边本来就是同一个值”的等式。
-- soko:hint 目标形态：内核会把 1 + 1 算成 2，所以 Eq.{1} Nat (1 + 1) 2 两边本来就相等。
-- soko:hint 关键件：Eq.refl.{1}——第一个参数给类型 Nat，第二个参数给等号一边的表达式。
theorem one_plus_one_eq_two : Eq.{1} Nat (1 + 1) 2 := sorry

-- 练习 8（自己造“对称”，本场最深的一步）：从 a = b 推出 b = a。
-- 关键一步：subst 需要你**自己设计**谓词 p。取
--   p := fun (x : Nat) => Eq.{1} Nat x a
-- 于是 p a = “a = a”（用 Eq.refl 证），而 p b 恰好是目标“b = a”。
-- soko:hint 思路：没有现成的“对称”引理——要自己设计一个谓词 p，交给 Eq.subst 沿 h 替换。
-- soko:hint 目标形态：三层 fun 拿到 a、b、h 之后，目标是 b = a；选一个 p，让 p b 恰好是它、p a 又容易证。
-- soko:hint 关键件：Eq.subst.{1} 按签名给全参数；p 的配方就是上方讲解里“x = a”的形状。
theorem eq_symm_nat : (a : Nat) -> (b : Nat) -> Eq.{1} Nat a b -> Eq.{1} Nat b a := sorry

-- 收尾（练习 6 的闭环）：解出练习 6 后，把下面这行的注释去掉，它必须变绿
-- —— 这是内核在“证明”你的 two 确实等于 1 + 1。
-- theorem two_def : Eq.{1} Nat two (1 + 1) := Eq.refl.{1} Nat two

-- ─────────────────────────────────────────────────────────────
-- 单元③ 函数与箭头
-- 一切皆有类型：1 : Nat，add1 : Nat -> Nat。
-- Nat -> Nat 读作“从 Nat 到 Nat 的函数”；
-- 值用 fun 定义：fun (n : Nat) => n + 1 表示参数 n、返回 n + 1。
-- （add1 里故意不写 (n : Nat) —— 编译器能从声明的类型推断出 n : Nat。）
-- ─────────────────────────────────────────────────────────────

-- 演示（已写好，无需改动）：add1 把参数加一。
def add1 : Nat -> Nat := fun n => n + 1

-- 练习 9：补全 double。洞在 lambda 里：剩余目标是 Nat（外层已确定 n : Nat）。
-- soko:hint 思路：洞在 lambda 体内——外层已经确定了 n : Nat，程序可以分步写完。
-- soko:hint 目标形态：剩余目标是 Nat：用 n 凑出一个翻倍的表达式。
-- soko:hint 关键件：+ 是真正的加法——把 n 和 n 组合起来。
def double : Nat -> Nat := fun (n : Nat) => sorry

-- 练习 10（高阶函数）：twice 接收一个函数 f，返回“把 f 用两次”的函数。
-- 注意类型 (Nat -> Nat) -> Nat -> Nat：括号是分组不是应用。
-- 提示：答案需要两层 fun；先把 f 用在 x 上，再对结果再用一次 f。
-- soko:hint 思路：函数是一等公民——f 像普通值一样，可以被使用任意多次。
-- soko:hint 目标形态：两层箭头：先 fun (f : Nat -> Nat) =>，再 fun (x : Nat) =>，最后返回一个 Nat。
-- soko:hint 关键件：把 f 用在 x 上得到一个 Nat，再对结果再用一次 f；括号是分组不是应用。
def twice : (Nat -> Nat) -> Nat -> Nat := sorry

-- ─────────────────────────────────────────────────────────────
-- 单元④ 宇宙：函数类型的类型
-- 你已经在写 `Nat -> Nat` 这样的类型了。可是——“一切皆有类型”，
-- 那么函数类型本身的类型是什么？
--   * Nat : Sort 1   （数据的类型住在 Sort 1，写作 Type）
--   * Nat -> Nat : Sort 1 （函数类型还是“数据的类型”）
--   * Sort 1 : Sort 2     （阶梯向上无限延伸）
--   * Prop = Sort 0       （命题的世界就是阶梯的起点！单元①一直在用它）
-- 单元②的机械规则揭晓：Eq 的签名是 {α : Sort u} -> …，
-- “数字的等式要写 Eq.{1}”是因为 Nat : Sort 1。悬停 #check 问内核：
-- ─────────────────────────────────────────────────────────────

#check Nat
#check (Nat -> Nat)
#check Prop

-- 练习 11：补一个“住在 Sort 1 里的东西”。
-- 提示：想想你在单元②、③里反复见过的类型名字。
-- soko:hint 思路：给“住在 Sort 1 里的东西”报出一个名字——想想你反复在用的那个数据类型。
-- soko:hint 目标形态：目标是 Sort 1 本身（也叫 Type），所以答案是一个类型名，不是一个值。
-- soko:hint 关键件：单元②一直在用的 Nat 就住在 Sort 1。
example : Sort 1 := sorry

-- 练习 12（毕业 ★）：把练习 8 的“对称”推广到任意宇宙层级。
-- 照抄那份配方：把 Nat 换成 α、把 .{1} 换成 .{u}。注意第一个 binder 是
-- 隐式的 {α : Sort u}，值一侧要写成 fun {α : Sort u} => ...。
-- soko:hint 思路：把练习 8 的配方推广到任意宇宙——完全同构，只是 Nat 换成 α、.{1} 换成 .{u}。
-- soko:hint 目标形态：第一个 binder 是隐式的 {α : Sort u}：值一侧先写 fun {α : Sort u} =>，后面照旧三层 fun。
-- soko:hint 关键件：还是 Eq.subst.{u} 加 Eq.refl.{u} 那套；谓词 p 换成“x = a”在 α 上的版本。
theorem Eq.symm {u} : {α : Sort u} -> (a : α) -> (b : α) -> Eq.{u} α a b -> Eq.{u} α b a := sorry

-- ─────────────────────────────────────────────────────────────
-- 单元⑤后日谈：by 写法（tactic 证明）
-- 除了直接写证明项，还可以用 by 块把证明当一串命令写：
--   theorem t : T := by <tactic>; <tactic>; ...
-- 判定永远走内核：搭出来的证明项和手写项完全等价。
-- 五个基础 tactic：intro / exact / apply / assumption / rfl。
-- `by sorry` 表示「留空未完成」（跟值位写 sorry 一样），剩余目标照常显示。
-- ─────────────────────────────────────────────────────────────

-- 练习 13：用 by 块证明「a 且 b 推出 a」。
-- 提示：先 intro 拿到 h : And a b，再用 exact And.left a b h（或 apply And.left 后 assumption）。
-- soko:hint 思路：by 块里先 intro 把箭头拆开，拿到假设后再 exact。
-- soko:hint 目标形态：`by intro a; intro b; intro h; exact And.left a b h`。
-- soko:hint 关键件：And.left 吃 a、b 和 And a b 的证明，吐出 a。
theorem and_left_by : (a : Prop) -> (b : Prop) -> And a b -> a := by sorry

-- 练习 14（挑战 ★）：用 by 块 + apply 证明「a 推出 a 或 b」。
-- 提示：Or.inl 说「只要左边的证明」——apply Or.inl 把目标拆成只剩 a。
-- soko:hint 思路：apply 是「把目标套上构造子」——Or.inl 需要左边，目标就换成左边。
-- soko:hint 目标形态：`by intro a; intro b; intro ha; apply Or.inl; exact ha`。
-- soko:hint 关键件：Or.inl 拆出子目标 a；exact ha 结束。
theorem or_inl_by : (a : Prop) -> (b : Prop) -> a -> Or a b := by sorry
