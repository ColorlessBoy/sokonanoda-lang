# robots.txt —— sokonanoda-lang 站点
#
# 全站可抓。这个项目的读者里有 code agent，所以我们**主动**提供
# /llms.txt（给模型读的入口清单）与 /sitemap.xml（给爬虫的完整清单），
# 而不是靠爬虫自己猜。
#
# 没有 Disallow：站点全部是公开文档，没有任何需要挡的路径。
# 注意 GitHub Pages 会给这个文件加 `Content-Type: text/plain`，符合规范。

User-agent: *
Allow: /

Sitemap: https://colorlessboy.github.io/sokonanoda-lang/sitemap.xml
