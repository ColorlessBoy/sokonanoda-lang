-- ==========================================================================
-- unit3-functions-arrows.sokonanoda —— 单元③ 函数与箭头
--
-- 规则：
--   * 带 ??? 的声明是练习。把 ??? 换成你的答案，由完整内核判定对错。
--   * 部分作答是合法状态：先写前几层 fun，最后一层留 ???。
--   * 判定永远走内核：填完的每一行放回官方 Lean 也合法。
-- ==========================================================================

-- ─────────────────────────────────────────────────────────────
-- 一切皆有类型：1 : Nat，add1 : Nat -> Nat。
-- Nat -> Nat 读作“从 Nat 到 Nat 的函数”。
-- 值用 fun 定义：fun (n : Nat) => n + 1 表示参数 n、返回 n + 1。
-- 注意：add1 里故意不写 (n : Nat) —— 编译器能从声明的类型推断出 n : Nat。
-- ─────────────────────────────────────────────────────────────

-- 演示（已写好，无需改动）：add1 把参数加一。
def add1 : Nat -> Nat := fun n => n + 1

-- 自测：#reduce 把函数应用算出来。add1 41 应该是 42。
#reduce add1 41

-- ─────────────────────────────────────────────────────────────
-- 程序不必一次写完：洞 ??? 就是“剩余目标”的占位符，只能放答案末尾。
-- 函数是一等公民：(Nat -> Nat) -> Nat -> Nat 里，函数也可以是别人的参数。
-- 箭头是右结合的：Nat -> Nat -> Nat 就是 Nat -> (Nat -> Nat)。
-- ─────────────────────────────────────────────────────────────

-- 练习 1：补全 double。洞在 lambda 里：剩余目标是 Nat（外层已确定 n : Nat）。
def double : Nat -> Nat := fun (n : Nat) => ???

-- 练习 2（高阶函数）：twice 接收一个函数 f，返回“把 f 用两次”的函数。
-- 注意类型 (Nat -> Nat) -> Nat -> Nat：括号是分组不是应用。
-- 提示：答案需要两层 fun；先把 f 用在 x 上，再对结果再用一次 f。
def twice : (Nat -> Nat) -> Nat -> Nat := ???

-- 练习 3：三个参数的函数。Nat -> Nat -> Nat -> Nat 一次吃三个 Nat。
-- 提示：三层 fun，一层一层写；洞永远放在最后一层。
def three_args : Nat -> Nat -> Nat -> Nat := ???

-- 练习 4：apply_twice 的输入本身是“一个函数”，输出也是“一个函数”。
-- 因为箭头右结合，整体函数类型必须用括号括住：(Nat -> Nat) -> (Nat -> Nat)。
-- 提示：两层 fun，第一层拿函数 f，第二层拿 Nat n，返回“f 用两次”。
def apply_twice : (Nat -> Nat) -> (Nat -> Nat) := ???

-- ─────────────────────────────────────────────────────────────
-- 悬念（单元④揭晓）：你已经在写 `Nat -> Nat` 这样的类型了。
-- 可是——“一切皆有类型”，那么**函数类型本身**的类型是什么？
-- 想一分钟，再打开单元④。
-- ─────────────────────────────────────────────────────────────
