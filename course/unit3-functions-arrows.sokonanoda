-- ==========================================================================
-- unit3-functions-arrows.sokonanoda —— 单元③ 函数与箭头
--
-- 规则：
--   * 带 sorry 的声明是练习。把 sorry 换成你的答案，由完整内核判定对错。
--   * 部分作答是合法状态：先写前几层 fun，最后一层留 sorry。
--   * 判定永远走内核：填完的每一行放回官方 Lean 也合法。
-- ==========================================================================

-- ─────────────────────────────────────────────────────────────
-- 一切皆有类型：1 : Nat，add1 : Nat -> Nat。
-- Nat -> Nat 读作“从 Nat 到 Nat 的函数”。
-- 值用 fun 定义：fun (n : Nat) => n + 1 表示参数 n、返回 n + 1。
-- 注意：add1 里故意不写 (n : Nat) —— 编译器能从声明的类型推断出 n : Nat。
-- ─────────────────────────────────────────────────────────────

-- 演示（已写好，无需改动）：add1 把参数加一。
def add1 : Nat -> Nat := fun n => n + 1

-- 自测：#reduce 把函数应用算出来。add1 41 应该是 42。
#reduce add1 41

-- ─────────────────────────────────────────────────────────────
-- 程序不必一次写完：洞 sorry 就是“剩余目标”的占位符，只能放答案末尾。
-- 函数是一等公民：(Nat -> Nat) -> Nat -> Nat 里，函数也可以是别人的参数。
-- 箭头是右结合的：Nat -> Nat -> Nat 就是 Nat -> (Nat -> Nat)。
-- ─────────────────────────────────────────────────────────────

-- 练习 1：补全 double。洞在 lambda 里：剩余目标是 Nat（外层已确定 n : Nat）。
-- soko:hint 思路：洞在 lambda 体内——外层已经确定了 n : Nat，程序可以分步写完。
-- soko:hint 目标形态：剩余目标是 Nat：用 n 凑出一个翻倍的表达式。
-- soko:hint 关键件：+ 是真正的加法——把 n 和 n 组合起来。
def double : Nat -> Nat := fun (n : Nat) => sorry

-- 练习 2（高阶函数）：twice 接收一个函数 f，返回“把 f 用两次”的函数。
-- 注意类型 (Nat -> Nat) -> Nat -> Nat：括号是分组不是应用。
-- 提示：答案需要两层 fun；先把 f 用在 x 上，再对结果再用一次 f。
-- soko:hint 思路：函数是一等公民——f 像普通值一样，可以被使用任意多次。
-- soko:hint 目标形态：两层箭头：先 fun (f : Nat -> Nat) =>，再 fun (x : Nat) =>，最后返回一个 Nat。
-- soko:hint 关键件：把 f 用在 x 上得到一个 Nat，再对结果再用一次 f；括号是分组不是应用。
def twice : (Nat -> Nat) -> Nat -> Nat := sorry

-- 练习 3：三个参数的函数。Nat -> Nat -> Nat -> Nat 一次吃三个 Nat。
-- 提示：三层 fun，一层一层写；洞永远放在最后一层。
-- soko:hint 思路：三个箭头就是三层 fun——一次吃三个 Nat，再交出一个 Nat。
-- soko:hint 目标形态：一层一层写：fun (a : Nat) => fun (b : Nat) => fun (c : Nat) => …，洞放在最后一层。
-- soko:hint 关键件：最后一层返回任意一个 Nat 就行——三个参数都是现成的 Nat，内核只判类型。
def three_args : Nat -> Nat -> Nat -> Nat := sorry

-- 练习 4：apply_twice 的输入本身是“一个函数”，输出也是“一个函数”。
-- 因为箭头右结合，整体函数类型必须用括号括住：(Nat -> Nat) -> (Nat -> Nat)。
-- 提示：两层 fun，第一层拿函数 f，第二层拿 Nat n，返回“f 用两次”。
-- soko:hint 思路：这次输入是函数、输出也是函数——注意返回类型 Nat -> Nat 本身还是个箭头。
-- soko:hint 目标形态：两层 fun：第一层拿函数 f，写完发现剩余目标还是 Nat -> Nat——所以再写一层 fun 拿 n。
-- soko:hint 关键件：把 f 连用两次：先用在 n 上，再对结果用一次；最后一层返回的就是“f 用两次”的表达式。
def apply_twice : (Nat -> Nat) -> (Nat -> Nat) := sorry

-- ─────────────────────────────────────────────────────────────
-- 局部绑定 let：给中间结果起个名字，再往下写。
--   let x : T := v; body
-- 读作：先把 v 命名为 x（类型 T），再计算 body，body 里可以引用 x。
-- 注意：v1 要求显式写类型（let x : T := v），先想清楚中间值的类型。
-- 等价性：let x : T := v; body 与 (fun (x : T) => body) v 完全相同——
-- 判定完全交给内核，你写哪种都行。
-- ─────────────────────────────────────────────────────────────

-- 演示（已写好）：用 let 给中间结果命名，再返回它。
def add_two_let : Nat -> Nat := fun (n : Nat) => let m : Nat := n + 2; m

-- 自测：#reduce 会先把 let 算出来；下面应该是 42。
#reduce (let m : Nat := 20 + 20; m + 2)

-- 练习 5：用 let 给中间结果起名。洞在值位：把 n + n 绑定成 m。
-- soko:hint 思路：let 先算右边、再进 body——先给 m 想好值和类型。
-- soko:hint 目标形态：洞的期望类型就是绑定注解 Nat：用 n 凑出翻倍的值。
-- soko:hint 关键件：+ 是真正的加法——把 n 和 n 加起来。
def double_let : Nat -> Nat := fun (n : Nat) => let m : Nat := sorry; m

-- 练习 6：let 绑定之后，body 里就能直接用这个名字。
-- 洞在 let 的 body：m 已经是现成的 Nat，直接把它交出去。
-- soko:hint 思路：let 把 m 放进了作用域——body 里 m 就是一个普通的 Nat。
-- soko:hint 目标形态：剩余目标还是 Nat；m 已经在局部上下文里。
-- soko:hint 关键件：body 直接写 m 即可（或者任何用 m 凑出的 Nat）。
def body_uses_let : Nat -> Nat := fun (n : Nat) => let m : Nat := n + 5; sorry

-- ─────────────────────────────────────────────────────────────
-- 悬念（单元④揭晓）：你已经在写 `Nat -> Nat` 这样的类型了。
-- 可是——“一切皆有类型”，那么**函数类型本身**的类型是什么？
-- 想一分钟，再打开单元④。
-- ─────────────────────────────────────────────────────────────
