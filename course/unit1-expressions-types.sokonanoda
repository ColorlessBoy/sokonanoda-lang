-- ==========================================================================
-- unit1-expressions-types.sokonanoda —— 单元① 表达式与类型
--
-- 规则：
--   * 带 ??? 的声明是练习。把 ??? 换成你的答案，由完整内核判定对错。
--   * 部分作答是合法状态：先写前几层，最后一层留 ???。
--   * 判定永远走内核：填完的每一行放回官方 Lean 也合法。
-- ==========================================================================

-- ─────────────────────────────────────────────────────────────
-- 这个语言里，一切皆“有类型的表达式”。
-- Nat 是自然数类型；0、1、2 是它的字面量；+ 是真正的加法，
-- 内核会把 1 + 1 算成 2（见下面的自测）。
-- ─────────────────────────────────────────────────────────────

-- 演示（已写好，无需改动）：字面量就是 Nat 的合法值。
def one : Nat := 1

-- 自测：#reduce 让内核把表达式算出来。1 + 1 就是 2。
#reduce 1 + 1

-- ─────────────────────────────────────────────────────────────
-- 类型也有类型（宇宙阶梯）：
--   Prop（命题的宇宙）= Sort 0
--   Type（数据的宇宙）= Sort 1
-- 比如 Nat 的类型就是 Sort 1；类型名本身也是表达式。
-- ─────────────────────────────────────────────────────────────

-- 练习 1（热身）：定义 two : Nat。提示：写字面量 2，或写 1 + 1 都可以。
def two : Nat := ???

-- 练习 2：补一个“住在 Sort 1 里的东西”。提示：想想上面出现过的类型名字。
example : Sort 1 := ???

-- 练习 3：定义 four : Nat。提示：字面量 4，或者用 + 把已见过的数字拼起来。
def four : Nat := ???

-- 练习 4（阶梯的另一端）：Prop = Sort 0。补一个“住在 Sort 0 里的东西”。
-- 提示：等式是命题 —— Eq.{1} Nat (1 + 1) 2 就住在 Sort 0 里（单元④详解）。
example : Sort 0 := ???
