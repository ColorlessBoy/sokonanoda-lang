-- ==========================================================================
-- unit5-universes-sort.sokonanoda —— 单元⑤ 宇宙：函数类型的类型
--
-- 规则：
--   * 带 sorry 的声明是练习。把 sorry 换成你的答案，由完整内核判定对错。
--   * 部分作答是合法状态：先写前几层 fun，最后一层留 sorry。
--   * 判定永远走内核：填完的每一行放回官方 Lean 也合法。
-- ==========================================================================

-- ─────────────────────────────────────────────────────────────
-- 单元③结尾的问题：函数类型 Nat → Nat 的**类型**是什么？
-- “一切皆有类型”没有例外，类型自己也有类型：
--   * Nat : Sort 1   （数据的类型住在 Sort 1）
--   * Nat → Nat : Sort 1 （函数类型还是“数据的类型”）
--   * Sort 1 : Sort 2     （阶梯向上无限延伸）
--   * Prop = Sort 0       （命题的世界就是阶梯的起点！单元①一直在用它）
--
-- Lean 给宇宙另备了一套写法：`Type n` 就是 `Sort (n + 1)`——`Type 0`
-- 等于 `Sort 1`，`Type 1` 等于 `Sort 2`，单独一个 `Type` 也是 `Sort 1`。
-- 两种写法指的是同一个东西。用 #check 问内核任何东西的类型：
--
-- 宇宙层级还能做加法：`Sort (u+1)`（也写 `Sort u+1`）就是 `Sort u` 的上一级。
-- 两个**类型**之间的等式也住在上一级：对 `α β : Sort u`，命题 `α = β` 落在
-- 层级 u+1（类型 `Sort u` 自己住在 `Sort (u+1)`）。prelude 的
-- `Eq.mp`/`Eq.mpr`/`cast` 就是这么定型的：它们在两个类型之间搬运，
-- 所以吃的等式证明正是那条 `α = β`。
-- ─────────────────────────────────────────────────────────────

#check Nat
#check (Nat → Nat)
#check Prop
#check (Type 0)
#check (Type 1)

-- ─────────────────────────────────────────────────────────────
-- 现在单元②的机械规则可以揭晓了：Eq 的签名是 {α : Sort u} → …，
-- “数字的等式要落在层级 .{1}”是因为 Nat : Sort 1 —— 那个 α 的层级必须是 1。
-- 裸写 Eq 默认 u = 0（Prop 层）；记法 `a = b` 会替你把层级解出来，
-- 而证明项（Eq.refl / Eq.subst…）仍要自己写全。
-- ─────────────────────────────────────────────────────────────

-- ─────────────────────────────────────────────────────────────
-- 读 #check 的输出：格式是 `<项> : <类型>`，`:` 右边就是这个项的“住所”。
-- 上面已经打印出结果：Nat、Nat → Nat 都是 Type 0（= Sort 1）；
-- Type 0 是 Type 1（= Sort 2）；Type 1 是 Type 2（= Sort 3）。
-- 做题时先自己预测，再看 #check 的答案对照。
-- ─────────────────────────────────────────────────────────────

-- 练习 1（读输出）：从 `#check (Nat → Nat)` 的输出读出函数类型住在哪一层。
-- soko:hint 思路：读上面 `#check (Nat → Nat)` 那一行——`:` 右边写着 Type 0，而 Type 0 就是 Sort 1。
-- soko:hint 目标形态：目标是 Sort 1：要交出一个类型名，它的类型正好是 Sort 1。
-- soko:hint 关键件：每个数据的类型都住在 Sort 1——挑一个刚被 #check 过、你能直接写出的函数类型。
def function_type_universe : Sort 1 := by sorry

-- 练习 2（读输出）：从 `#check (Type 0)` 的输出读出 Type 0 自己住在哪一层。
-- soko:hint 思路：读 `#check (Type 0)` 的输出——`:` 右边是 Type 1，而 Type 1 就是 Sort 2。
-- soko:hint 目标形态：目标是 Sort 2：这次要交出的不是数据，也不是数据类，而是“宇宙本身”。
-- soko:hint 关键件：把 #check 输出里 `:` 右边那层再当成一个项——上一行检查过的 Type 0 就住在 Type 1。
def type0_universe : Sort 2 := by sorry

-- 练习 3（热身）：补一个“住在 Sort 1 里的东西”。
-- 提示：想想你在单元②、③里反复见过的类型名字。
-- soko:hint 思路：给“住在 Sort 1 里的东西”报出一个名字——想想你反复在用的那个数据类型。
-- soko:hint 目标形态：目标是 Sort 1 本身（也叫 Type），所以答案是一个类型名，不是一个值。
-- soko:hint 关键件：读 #check 输出里类型为 Type 0 的项——刚才那两行里就有现成的例子。
example : Sort 1 := by sorry

-- 练习 4（阶梯的起点）：Prop = Sort 0。补一个“住在 Sort 0 里的东西”。
-- soko:hint 思路：Sort 0 就是 Prop——单元①里你一直在证明的那些东西的世界；这次要交出一个“命题”。
-- soko:hint 目标形态：答案是一个 Prop 层的项：不是值，也不是类型名，而是一句可以用证明填的断言。
-- soko:hint 关键件：目标 Sort 0、需要一句命题：用等式（`a = b`，数字上落在层级 .{1}）造一个命题——两边给内核算出来相等的表达式。
example : Sort 0 := by sorry

-- 练习 5（毕业 ★）：把单元②练习 3 的“对称”推广到任意宇宙层级。
-- 照抄那份配方：把 Nat 换成 α、把 .{1} 换成 .{u}。
-- 注意第一个 binder 是隐式的 {α : Sort u}，值一侧要写成 fun {α : Sort u} => ...。
-- soko:hint 思路：把单元②的“对称”配方推广到任意宇宙——完全同构，只是 Nat 换成 α、.{1} 换成 .{u}。
-- soko:hint 目标形态：第一个 binder 是隐式的 {α : Sort u}：值一侧先写 fun {α : Sort u} =>，后面照旧四层 fun 拿 a、b、h。
-- soko:hint 关键件：目标形如 b = a、上下文有 h : a = b：用 Eq.subst.{u} 沿 h 替换，谓词按“x = a”的形状在 α 上设计（配方见单元②练习 3）。
theorem Eq.symm {u} : {α : Sort u} → (a : α) → (b : α) → Eq.{u} α a b → Eq.{u} α b a := by sorry -- soko:notation-ok: α : Sort u 是多态类型，`=` 记法补不出宇宙层级 u（引擎边界）

-- ─────────────────────────────────────────────────────────────
-- 先算后证：读 #reduce 的输出前，先自己预测答案，再让内核验证。
-- ─────────────────────────────────────────────────────────────

-- 练习 6（先预测再证）：先预测 #reduce 会打印什么，再填出这个等式。
-- soko:hint 思路：先在脑子里把 (fun (x : Nat) => x + 1) 应用到 41，得到一个数；这是一个计算式等式。
-- soko:hint 目标形态：目标是 `… = 42`；rfl 在两边算出同一个值时成立。
-- soko:hint 关键件：用 Eq.refl.{1}——类型给 Nat，等号一边给你预测出的那个数；先自己预测，再对照下面的 #reduce。
def predict_then_prove : ((fun (x : Nat) => x + 1) 41) = 42 := by sorry

-- 内核答案（先自己预测，再对照）：这一行会打印 42。
#reduce (fun (x : Nat) => x + 1) 41
