-- ==========================================================================
-- unit11-modules-projects.sokonanoda —— 单元⑪ 模块与项目
--
-- 规则：
--   * 带 sorry 的声明是练习。把 sorry 换成你的答案，由完整内核判定对错。
--   * 部分作答是合法状态：先写前几层 fun，最后一层留 sorry。
--   * 判定永远走内核：填完的每一行放回官方 Lean 也合法。
--
-- 前十个单元都在**一个文件**里写证明。真实项目是「一个入口文件 + 它的 import
-- 闭包」：公共名字住进库模块，入口第一行 `import` 它。本单元教这条边界：置顶
-- 语法、模块名↔路径、项目清单、闭包级规则，最后在 course/unit11-project/ 跑一个
-- 真的两文件项目（跨文件练习在它的 Exercises.sokonanoda）。本画布仍是单文件。
-- ==========================================================================

-- 本文件扮演**被 import 的库模块**：它导出的就是自己声明的这几条——Iff 定义
-- 与 and_comm 定理。And / Or 这些连接符 **prelude 自带**，任何文件（被不被
-- import 都一样）都能直接用，不需要在这里声明。

def Iff (A : Prop) (B : Prop) : Prop := (A → B) ∧ (B → A)

-- 演示（已写好）：库的第一条导出——交换 And 的两半。练习 5 要复用它。
theorem and_comm (A : Prop) (B : Prop) (h : A ∧ B) : B ∧ A :=
  And.intro (And.right h) (And.left h)

-- ─────────────────────────────────────────────────────────────
-- 11.1 为什么需要 import：给名字划一条边界
--
-- 单文件世界靠一个隐式约定运转：每个文件各自声明自己需要的名字，于是**同一个
-- 名字在两处可以指不同东西**——A.sokonanoda 里 `def double (n : Nat) : Nat`
-- 是自然数翻倍，B.sokonanoda 里 `def double (A : Prop) : Prop` 是恒等；两份
-- 各自判卷都合法，拼进同一个文件才撞成 `duplicate declaration double`。import
-- 让「哪个 double？」可回答：名字属于**模块**。三条规则先记住：
--   (1) 被 import 的模块里的顶层名字在整个闭包里**全局可见**（没有 export 列表）；
--   (2) 因此跨模块**重名 = 错误**（import-name-collision）：正解是只声明一次；
--   (3) 「入口文件 + 闭包」是一个**编译单元**，内核仍是唯一判定者。
-- ─────────────────────────────────────────────────────────────

-- ─────────────────────────────────────────────────────────────
-- 11.2 `import Foo.Bar`：写在文件最上方
--
-- 形态与官方 Lean 一致：`import` + 点分模块名，一行一个，**必须在任何声明之前**
-- （否则报 import-must-precede-declarations：把这行移到文件开头）。
--
--   import Logic            -- 同目录的 Logic.sokonanoda
--   import Lesson.Logic     -- 子目录 Lesson/ 下的 Logic.sokonanoda
--
-- 例：course/unit11-project/Canvas.sokonanoda 第一行就是 `import Logic`，它下面
-- 能直接调用 and_comm，因为那条定理声明在 Logic.sokonanoda 里。`import` 是命令
-- 关键字：名字位宽松（`def import := 1` 合法），表达式位会被拒绝（报 cannot appear inside an expression）。
-- ─────────────────────────────────────────────────────────────

-- ─────────────────────────────────────────────────────────────
-- 11.3 模块名来自路径：文件名就是模块名
--
-- `import Foo.Bar` 解析到 `<模块根>/Foo/Bar.sokonanoda`：每段都必须是合法
-- 标识符（字母/下划线开头），分隔符只用 `.`，路径里的 `/` 由编译器补；大小写敏感。
-- 这条规则决定什么文件**不可被 import**：`-` 不是标识符字符，所以
-- course/unit1-propositions-proofs.sokonanoda 不能被 import——写
-- `import unit1-propositions-proofs` 得到 import-not-a-valid-module-name
-- （提示：模块名里不能有 `-`）；写 `import unit1` 得到 import-not-found，消息
-- 里还会主动问「你是不是想 import unit1-propositions-proofs.sokonanoda？」。
-- 想让文件被 import，就给它一个合法标识符名，比如 `Logic.sokonanoda`。
-- ─────────────────────────────────────────────────────────────

-- ─────────────────────────────────────────────────────────────
-- 11.4 项目根与模块根：sokonanoda.toml
--
-- 从入口文件所在目录**向上**找最近的 `sokonanoda.toml`：它就是项目根，模块根
-- 默认 = 清单所在目录。清单**可以是空文件**（存在即标记）；三个可选键：
--
--   name = "unit11-project"   -- 项目名，元数据，不参与解析
--   requires = "0.56"         -- 钉工具链版本：不匹配只给 warning，不阻断
--   src = "src"               -- 模块根，默认 "."（相对清单目录）
--
-- 找不到清单就走**零配置退路**：模块根 = 入口文件自己的目录，所以「同目录两个
-- 文件互相 import」不需要任何清单——单文件是默认，项目是 opt-in。
-- 两个 CLI 开关：`--root <dir>` 显式指定模块根（忽略发现）；`--no-project` 不读清单（模块根 = 入口文件目录）。
-- ─────────────────────────────────────────────────────────────

-- ─────────────────────────────────────────────────────────────
-- 11.5 整个闭包一起判定
--
-- 编译器从入口出发解析出**闭包**，按拓扑序（依赖在前）在同一个环境里逐个判定，
-- 所以下面四种都是**项目级**诊断：
--   * import-cycle             成环 A → B → A：把公共部分抽到更底层的模块；
--   * import-name-collision    两个模块声明同一个顶层名：消息给出两个来源；
--   * import-dependency-failed 依赖没编译成功：只在 import 它的那一行报**一条**，先修依赖的第一条错误；
--   * import-prelude-conflict  prelude 冲突（见下）。
-- prelude 是**整个编译单元**的属性，由入口决定（入口 Bare ⇒ 整个闭包 Bare）；
-- 依赖**没写**指令 = 继承入口，显式写了且与入口不一致才是冲突。
-- 另一条让位规则是**共享 prelude 模块**：闭包里任一模块自带 `inductive Nat`（像
-- 单元⑥/⑦ 手写的 Nat 块），整个闭包就跳过内置 Nat——库可以把 Nat 定义成一份共享。
-- 最后一条最常踩：被 import 的模块里还有 `sorry` 时，导入方只收到一条 **warning**
-- （import-has-open-exercises）——洞不进环境，那些名字用不了，但其余声明照常可用。
-- ─────────────────────────────────────────────────────────────

-- ─────────────────────────────────────────────────────────────
-- 11.6 动手：跑一个真的两文件项目
--
--   course/unit11-project/
--   ├── sokonanoda.toml          -- 清单：name = "unit11-project"
--   ├── Logic.sokonanoda         -- 库模块：导出定理 and_comm（And 来自 prelude）
--   ├── Canvas.sokonanoda        -- 入口：`import Logic` + 用了它的定理（已写好）
--   └── Exercises.sokonanoda     -- 入口：`import Logic` + 两道带 sorry 的练习
--
-- 在仓库根依次跑（判卷永远走内核，不需要任何 Lean 工具链）：
--   scripts/soko course/unit11-project/Canvas.sokonanoda         -- 判整个闭包
--   scripts/soko course/unit11-project/Exercises.sokonanoda      -- 跨文件练习
--   scripts/soko build course/unit11-project                     -- 暖缓存
--
-- `build` 的缓存键是**整条闭包的哈希**：改了 Logic.sokonanoda，import 它的 Canvas 一定重编（依赖变则入口必 miss）；闭包没动就整条命中（hit）。
-- ─────────────────────────────────────────────────────────────

-- ─────────────────────────────────────────────────────────────
-- 11.7 练习 A：给库补导出
--
-- 本文件扮演共享库，练习都是「库该导出的那一条」。每条都必须只用 **prelude
-- 自带的连接符**与**本文件已经声明的名字**（Iff、and_comm）：练习是开放声明，
-- 洞不进环境，别引用另一道还没做的练习。
-- ─────────────────────────────────────────────────────────────

-- 练习 1（导出 X）：给出演示的同型版本，但把假设写成箭头：
--
--   「A ∧ B 推出 B ∧ A。」
--
-- soko:hint 思路：结论是 `A ∧ B → B ∧ A`，最外层是一个箭头——先写 fun (h : A ∧ B) 把假设拿进来。
-- soko:hint 目标形态：fun 之后目标变成 B ∧ A；用 And.intro B A 把两个证明位补齐。
-- soko:hint 关键件：两份证据都来自 h：And.right A B h 是 B 的证明，And.left A B h 是 A 的证明。
theorem and_comm_imp_x (A : Prop) (B : Prop) : A ∧ B → B ∧ A := by
  sorry

-- 练习 2（导出 X）：库不只有 theorem，也有 `def`：把 And 的左消去固定成一条
-- 库导出（它的类型是 A，值是一份证明）。
-- soko:hint 思路：声明形式是 def … (h : A ∧ B) : A；h 已经是参数，不需要再写 fun。
-- soko:hint 目标形态：目标是 A，手里是 h : A ∧ B。
-- soko:hint 关键件：And.left 吃三个显式参数：A、B 与 h。
def and_left_x (A : Prop) (B : Prop) (h : A ∧ B) : A := by
  sorry

-- 练习 3（导出 X）：库也要导出用到连接符的定理：交换 Or 的两半。
-- soko:hint 思路：h : A ∨ B 包着两条分支的信息，用 match 分情况。
-- soko:hint 目标形态：结论是 B ∨ A；inl 支拿到 a : A，inr 支拿到 b : B。
-- soko:hint 关键件：结论的参数顺序是先 B 后 A：a 要走「右边」的构造子（Or.inr B A a），b 要走「左边」的构造子（Or.inl B A b）。
theorem or_comm_x (A : Prop) (B : Prop) : A ∨ B → B ∨ A := by
  sorry

-- 练习 4（导出 X）：这一条故意写成匿名 `example`（单元⑤ 用过这种形式）：库里
-- 也可以不命名地检查一条命题。
--
--   「A 推出 B、B 又推出 A，则 A 与 B 等价。」
--
-- soko:hint 思路：两个方向是两条假设（两个箭头），结论把这对函数打包成 Iff。
-- soko:hint 目标形态：结论形如 A ↔ B，而 Iff 展开就是 (A → B) ∧ (B → A)。
-- soko:hint 关键件：用 And.intro，参数写 (A → B)、(B → A)，把 mp 与 mpr 放进两个证明位。
example (A : Prop) (B : Prop) (mp : A → B) (mpr : B → A) : A ↔ B := by
  sorry

-- ─────────────────────────────────────────────────────────────
-- 11.8 练习 B：评阅一份被闭包拒绝的模块
--
-- 下面这份「第二个模块」被拒绝了，错误来自**整个闭包**而不是它自己：
--
--   -- Library2.sokonanoda
--   theorem and_comm (A : Prop) (B : Prop) (h : A ∧ B) : B ∧ A :=
--     And.intro B A (And.right A B h) (And.left A B h)
--
-- 报错是 import-name-collision：`and_comm` 在第一个模块里已经声明过（消息会点名
-- 两个来源模块）。提示：正解不是改名躲开，而是**不要重复声明**——用 import
-- 进来的那一份。
-- ─────────────────────────────────────────────────────────────

-- 练习 5（评阅 R）：本文件就是「第一个模块」：and_comm 已经声明在这里。请写一条
-- 新导出，把活交给共享的那条 and_comm（这就是上面那份模块的正确写法）。
-- soko:hint 思路：不要重抄证明；本文件已有 and_comm，直接调用它。
-- soko:hint 目标形态：目标 B ∧ A；把 A、B、h 三个参数显式喂给 and_comm。
-- soko:hint 关键件：函数应用写成 `and_comm A B h`（把和演示里一样的三个参数按顺序补全）。
theorem and_comm_via_lib_x (A : Prop) (B : Prop) (h : A ∧ B) : B ∧ A := by
  sorry

-- ─────────────────────────────────────────────────────────────
-- 11.9 练习 C：综合
--
-- 最后这道题把 And 的消去与 Or 的构造接起来（两者都是 prelude 自带的连接符），
-- 用上 let 与 match（单元⑥/③ 的老朋友）。先想清楚「目标长什么样」，再决定
-- 每一步选哪条规则。
-- ─────────────────────────────────────────────────────────────

-- 练习 6（综合 ★★）：分配律的一个方向——「A 且（B 或 C）」推出
-- 「（A 且 B）或（A 且 C）」。
-- soko:hint 思路：用 let 把 h 的右半（B ∨ C）单独命名，再对它 match 分情况；两种情况下都要把 A 与对应的那一半重新打包。
-- soko:hint 目标形态：外层 fun (h : A ∧ (B ∨ C))；let bc : B ∨ C := And.right A (B ∨ C) h; 然后 match bc。
-- soko:hint 关键件：从 h 用 And.left 取 A、And.right 取分支证据；inl b 支用 And.intro A B 打包再交给 Or.inl，inr c 支用 And.intro A C 打包再交给 Or.inr。
theorem and_or_imp_x (A : Prop) (B : Prop) (C : Prop) : A ∧ (B ∨ C) → (A ∧ B) ∨ (A ∧ C) := by
  sorry
