-- ==========================================================================
-- unit1-expressions-types-solution.sokonanoda —— 单元① 解答钥匙（agent 专用）
--
-- 与学习者画布 course/unit1-expressions-types.sokonanoda 一一对应，
-- 所有 ??? 已填入经完整内核验证的答案。勿直接发给学习者。
-- ==========================================================================

-- 演示（与画布一致）：字面量就是 Nat 的合法值。
def one : Nat := 1

-- 自测：#reduce 让内核把表达式算出来。1 + 1 就是 2。
#reduce 1 + 1

-- 练习 1：two := 1 + 1（内核把它算成 2，两个答案都对）。
def two : Nat := 1 + 1

-- 练习 2：Nat 的类型就是 Sort 1 —— 类型名本身是住在 Sort 1 里的表达式。
example : Sort 1 := Nat

-- 练习 3：four := 4（或 2 + 2、2 + 1 + 1，都是合法的 Nat）。
def four : Nat := 4

-- 练习 4：等式是命题 —— Eq.{1} Nat (1 + 1) 2 住在 Sort 0（= Prop）里。
example : Sort 0 := Eq.{1} Nat (1 + 1) 2

-- 额外自测（钥匙内部核对）：内核逐个回判这些值。
#reduce two
#reduce four
#reduce Eq.{1} Nat (1 + 1) 2
